target-version = "py311"
line-length = 100

[lint]
# Repository-wide correctness baseline. Critical maintained modules additionally
# enforce E4/E7/F/I/B through scripts/check_quality.py.
select = ["E9", "F63", "F7", "F82"]

[lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query", "fastapi.Header"]
