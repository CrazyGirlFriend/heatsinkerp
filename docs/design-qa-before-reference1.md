# 第二张柔光工作台：设计与交互核对

## 视觉来源与证据

- 选定源图：`docs/design/soft-workspace-selected.png`，对应本轮第二张生成结果 `exec-a8f36995-b253-49d2-9a0c-538435d7aae5.png`。
- 最终实现截图：`output/playwright/soft-workspace/detail-final.png`；关闭详情为 `list-final.png`。
- 源图与最终实现均为 1487 × 1058 像素；CSS viewport 为 1487 × 1058，deviceScaleFactor 1，没有缩放或边框差异。
- 同一状态：TEAM 研磨班组，第一条待接收转料单 TL20260906000001 展开详情。
- 本轮在同一次图像输入中同时查看源图与最终截图，另检查 `detail-region.png` 放大的详情区域和 `status-region.png` 状态区域。源图全分辨率中的对应区域文字清晰，无需额外裁切源图。
- 浏览器为独立终端 Playwright；没有调用 Codex 内置浏览器。

## 对比历史与修正

1. 首次实现截图 `detail-initial.png` 显示文字偏小、紧凑条码编号偏小。表格流向、数量、状态及详情字段调整到 15px；辅助流水号为 14px，紧凑条码编号为 13px。`detail-refined.png` 和 `detail-final.png` 已复核。
2. 首帧详情宽度、状态标签与后续 DOM 不一致，核实为热更新与面板过渡过程中的截图，淘汰该帧的几何结论。后续重开真实页面、等待状态和过渡稳定后截图；最终详情 x=1008.91、w=446.09，左区 x=136、w=848.91，与源图主要面板比例一致，标签正常可见。
3. 窄桌面额外的转料人/时间列按可用宽度隐藏，避免挤压主列；剩余列再根据容器宽度转换为完整记录行。1000px 窗口下表格 clientWidth 与 scrollWidth 均为 814px，无隐藏横向内容。手机保持记录行与原生详情抽屉。

## 五项视觉检查

- 字体与排版：系统字体栈、30px 页面标题、20px 详情标题、15px 主要字段与数字、14px 辅助流水号形成清晰层级；数量等宽数字、单位单独降低字号。无批次、金额、状态或按钮文字截断。
- 空间与层次：104px 窄导航、80px 顶栏、32px 主区留白；标题跨列，左侧状态区与列表、右侧详情独立排列。主要面板 15px 圆角、24px 列间距、20px 状态与列表间距、轻阴影符合目标。详情底部操作固定，字段独立滚动。
- 色彩与控件：近白紫灰背景、白色面板、紫色渐变主按钮、淡紫选中行与流向区已落地；语义状态使用文字并辅以琥珀、绿色、灰色，未扩大为大面积饱和色。
- 图像与图标：真实 Code 128 由 JsBarcode 生成，图标使用项目已有 Element Plus Icons。概念图示例头像继续采用真实账号首字作为应用头像，未引入虚构的人像身份。标准图标不是生成图的逐像素复刻，归为 P3 风格细节。
- 文案与数据：保留实际转料、扫码、班组、确认、打印业务。数字来自当前过滤范围的服务端总数；最终演示为 28 / 10 / 14 / 4，源图的 28 / 12 / 14 / 2 为概念数据，不硬编码。班组与操作权限均取自接口。

## 核心交互与响应式

- 状态计数读取服务端 total，覆盖整套筛选结果；切换状态不会错误重算为当前页条数。搜索、班组筛选、分页和重置可用。
- 研磨接收班组筛选返回 14 行，总数 14；搜索 HS-20260906-001 返回 3；无匹配查询显示空状态和 0。计数服务模拟失败显示“—”，重试后正确恢复为 3。
- 首条待接收动画在 420ms 内从 translateX 22.331px 移动到 55.4477px；已接收、作废无信号；减少动态效果时信号 display:none。
- 模拟接收 503 后仍为待接收、继续流动。真实本地演示接口确认 TL20260906000009 后，列表与详情信号同时移除，pending 从 11 变 10，received 从 13 变 14。
- 编辑表单、扫码定位首条记录、两份打印联及两份真实条码验证通过；未调用实体打印机。
- 侧栏展开时主内容宽度由 1383px 变 1279px；关闭详情恢复列表宽度。
- 1366 × 768 接收按钮 y=623、h=54；390 × 844 下 y=746、h=48，按钮均完整可见，手机无横向溢出，导航 Escape 可关闭。手机表单底部可见。
- 截图：`desktop-1366.png`、`narrow-list.png`、`mobile-list.png`、`mobile-detail.png`、`mobile-create.png`、`sidebar-expanded.png`、`edit-form.png`、`receipt-stopped.png`、`empty-state.png`。
- 结构化观察：`responsive-results.json`、`motion-results.json`、`filter-results.json`、`final-results.json`。

## 构建与控制台

- 6 个相关测试文件、28 项测试通过，见 `tests.log`，包括计数范围、迟到响应和接收后数字更新验证。
- 最终 TypeScript 检查 exit 0，见 `typecheck.log`；生产构建 exit 0，见 `build.log`。
- 新开的独立 Playwright 标签页依次访问班组长管理、班组管理、扫码和转料页面，0 error / 0 warning；最终保留首条待接收详情供查看。
- 故意注入的 503 与一次测试脚本中不受 CLI 沙箱支持的 URL 构造函数单独记录，不混入真实应用结论；测试脚本改为字符串匹配后全部断言完成。

## 范围与剩余细节

本轮落地选定转料页与共享主题，使用回环地址的内存演示接口，没有部署或写入业务数据库。既有账号鉴权、角色边界、确认幂等与 A4 双联打印保持原逻辑；实际现场扫码枪、实体打印机和真实后端联调不包含在本轮浏览器验证里。

P3：标准图标、账号首字头像与效果图的示例图形存在细小差异；部分按钮字号按实际中文可读性统一。不存在尚未处理的 P0/P1/P2 视觉或交互问题。此结论是实现验收，不替代用户的审美判断。

历史柔光工作台验收结果：passed。

---

# 2026-09-12 独立动态大屏：机器人细化

## 对照与证据

- source visual truth path: `output/playwright/factory-live-3d-pass3.png`。用户最新反馈认可这版方向，只要求更精致；不是重新复刻初期静态概念图。
- implementation screenshot path: `output/playwright/robot-polish-final.png`。
- 两图均为 1672×940 像素，CSS viewport 1672×940，截图密度 1，无缩放归一化。WebGL 内部画布 DPR 上限 1.5 不改变截图尺寸。
- 登录后的 /factory-live 原生全屏，同一真实批次 CK9ADB4056047C4905B8D19C3D（检验→库房），相同库存与八个班组。最终图暂停在递料动作中；源图双臂下垂，姿态差异是用户要求的动作细化，不作为布局误差。
- 源图、最终全图及 `robot-polish-detail.png`（530×540）在同一次图像输入中比较；重点核对头身比例、面罩、手掌托盘连接、关节、脚部和动作按钮。源图原尺寸机器人清晰可辨，无需再裁切。
- 独立 Chrome 会话 `heatsink_robot_polish_20260912`；未使用 Codex 内置浏览器。

## Findings / 对比历史

1. [P1，已修正] 最早整图浮动不满足真实动作；改用真实 GLB 骨架和动画，配合双臂 IK，托盘连续存在，无显示/隐藏切换伪造递送。
2. [P1，已修正] `factory-live-3d-pass3.png` 身体有明显低多边形棱角、手臂和头身比例粗糙。按最新反馈保留圆头黑面罩方向，细化圆角外壳、胸口面板、耳环、金属关节和夹持手。`robot-polish-pass1.png` 展示第一次细化，最终图确认头部略缩小、双臂弯曲托盘、脚部落在原平台范围。
3. [P2，已修正] 初版设备底色遮标题及 ECharts 无障碍描述出现 NaN；缩小设备图、保持标题层级及明确连线描述，最终八个班组完整可读。
4. [P2，已修正] 第一轮细化的面罩反光在部分姿态覆盖眼睛（`robot-polish-wave.png`）。最终单独压低面罩环境反射，保持外壳和金属反光；`robot-polish-final.png` / `robot-polish-detail.png` 中眼睛轮廓清晰。
5. 手机 resize 立即截屏时拍到原侧栏关闭过渡（`robot-polish-mobile.png`），不当作稳定布局结论。稳定帧 `robot-polish-mobile-stable.png` 及滚动后的 `robot-polish-mobile-teams.png`、`robot-polish-mobile-feed.png` 确认侧栏退出、内容无横向溢出。

## 五项视觉检查

- 字体：保留系统中文字体、34px 大屏标题、24px 班组标题及 22–28px 主数字；桌面 16:9 等比缩放，手机采用原生尺寸排版。没有新增大段说明文字或更改业务术语。
- 留白布局：两侧各四个班组，四项指标、底部记录及批次卡位置保留；中心可动区域 530×540，机器人不遮班组和批次卡。1440×900 窗口以完整画布缩放；390×844 为纵向浏览，不挤压八班组到一屏。
- 色彩：保留深蓝背景、青色边界和待接收橙色；机器人白色外壳、深色玻璃及金属关节分别采用独立材质，避免原先大片平白棱面。真实业务状态及非实时示意声明保留。
- 图像：原八张独立设备图片和工厂背景保留。机器人是用户明确要求的真实可驱动三维主体，不以 CSS/SVG 图画或静态图片充当动作。圆角网格跟随导入骨骼；机械夹持手和托盘是本轮细化，不添加额外角色或装饰。
- 文案内容：抬手/点头/递料按钮提供真实动作；“动作示意”说明展示性质。没有声称现场设备正在运行或机器人真实位置。库存和批次信息来自原接口。

## 交互、响应式和资源

- 浏览器验证抬手、递料、全局播放/暂停、减少动态效果、全屏/退出全屏、返回原统计总览。暂停与减少动态效果期间 motion-time 保持不变。
- 离开动态页后 canvas 数量为 0，旧 WebGL 上下文 isContextLost() 为 true。
- 1672×940、1920×1080（`robot-polish-1920.png`）、1440×900（`robot-polish-window.png`）、390×844 均已截图检查。手机 body/root scrollWidth 均为 390，机器人 x=14、宽=362，不溢出。
- 后台隐藏暂停由页面单元测试覆盖。本机自动化切换标签页未产生 document.hidden=true，真实系统隐藏事件没有完成浏览器复验；没有将该尝试写为通过。
- 前端完整 288 项、后端完整 147 项通过；新增机器人 3 项测试覆盖递料全程掌心误差 <0.002、连续位置变化、真实抬手/点头关节及转向变换。生产构建通过。第三方弃用警告及 >500KB 分包提示不影响运行。
- 浏览器应用控制台 0 error / 0 warning。为测试创建的临时标签均通过 finally 关闭，最后关闭且核验本任务独立 Chrome 进程；保留本地服务。

## 剩余 P3 与范围

- 造型精致度最终仍需用户主观确认；手部是简化机械夹持结构，不是五指仿生手。
- 尚未做电视设备长时间 GPU 压力测试及真实操作系统后台事件复验；代码已限制 30 FPS、DPR 1.5 并在卸载释放资源。
- 原统计大屏未删除，没有部署、清库、重新填充数据或修改转料业务规则。

实施检查完成，无待修复的 P0/P1/P2。

final result: passed

# 2026-09-12 转料表联动改版（当前轮次）

- source visual truth: `docs/design/factory-live-table-selected.png`，1672×940。
- 用户明确修订：删除所有机器人周围的流转线，保留真实动态机器人，以表格当前首条记录触发。
- implementation pass 1: `output/playwright/flow-table-pass1.png`，1672×940 CSS / 像素，DPR 1；同一次双图输入比较原图与实现，数据为本地真实演示库而非效果图的示例数。
- [P1，已修正] 班组栏的默认最小高度撑大了中间网格，遮住播报和表格标题。显式设置可收缩网格轨道、班组栏 min-height: 0，并限定内部行高。`flow-table-pass2.png`、`flow-table-2k-pass2.png`复验：2K班组栏底部919.43，播报顶部934.74，间隔15.31px，不再重叠。
- 最终实现：`output/playwright/flow-table-final.png`（1672×940）与`flow-table-2k-final.png`（2560×1440）。源图与原生1672×940最终图在同一次多图输入中比较，截图DPR 1，无截图拉伸。2K仍为等比虚拟画布，并提高WebGL绘制精度；机器人画布只有一张，无连线画布。
- 状态：管理员、真实本地演示库、完整屏幕、暂停在递料姿态。原图为示例数据和研磨→线切割，实际数据为检验→库房及不同批次；这属于明确的真实数据替换，不据此判为文案/高亮位置错配。
- 聚焦证据：`flow-table-detail.png`（2506×377），完整8条、完整CK编号、材质/流水号数/件数/重量/状态/时长可读。机器人沿用用户明确认可的真实三维模型，与源图的静态渲染质感差异为预期，不用静态图片假装动作。

## 当前轮次五项视觉检查

- 字体：PingFang SC / Microsoft YaHei中文字体，30px标题、21px班组标题、34px指标、13px表格，2K等比放大；长CK用12px数字排版，必要时省略并可点击查看。与源图的大小接近且层级完整，未引入大段口语解释。
- 间距：保留四项指标、左右各四班组、中心机器人、横向播报与底部表格的结构；表格固定表头、8行均完整显示。首轮遮挡已消除。普通窗口有外部应用导航，所以画布按可用区域适配；手机纵向排列八班组，表格在自身容器内横向滚动，body/root宽度均390，无整页横向溢出。
- 颜色：深蓝底、青白文字、橙色待处理及青绿已确认保留；按用户要求去除所有送料曲线、路径粒子和复杂工厂背景。选中上/下序同时有文字标签，不只依赖颜色。
- 图像：八种独立设备素材保持正确，线切割与雕刻不同；机器人为实际骨架、关节与托盘动作，非静态图或CSS图形替代。无新增角色；沿用既有设备原图，不裁改图片。
- 内容：表格数据来自整批聚合，流水号按distinct计数，多材质明确显示种数；无固定工艺路线。待交接指标含对外出库确认，所以没有照搬效果图的“待接收”来误称数据。每日数据按工厂时区；示例数未写入数据库。原统计总览保留。

## 当前轮次交互及验证

- 浏览器确认首条CK从CK481D49DBF1684929A1B47F3C切换到CK3D9E6BACA6C2477A95EF43D0，机器人activityKey与摘要同时变化，播报在约0.5秒整行过渡后对应新材质。
- 真正运行时motionTime递增；鼠标移入表格后65.564保持不变。系统reduced-motion由运行状态切入后暂停，时间冻结。空记录、未变化刷新不重触发、暂停期间新数据延后应用由单元测试覆盖。
- 点击表格CK打开实际扫码详情；退出大屏后旧WebGL上下文isContextLost=true、机器人节点为0，自动退出全屏。
- 已检查1672×940、2560×1440、1440×900普通窗口、390×844；证据为flow-table-final、flow-table-2k-final、flow-table-window、flow-table-mobile、flow-table-mobile-feed。未开展4K测试。
- 前端310项、后端153项通过，最终类型检查及构建通过。浏览器控制台0 error / 0 warning；测试仍有既有组件的watch/路由解码警告及第三方弃用提示，构建保留>500KB分包提示，均未扩大范围修改。
- 没有数据库迁移、清库、重播写入或线上部署。记录数仍433转料/376出库/192丢失。独立Chrome检查结束后关闭并核验退出；本地服务保留。

## 当前轮次剩余P3 / 边界

- 大屏加载最近100条而非全量历史，界面已如实标明；业务列表仍20/50/100分页。
- 普通非全屏小窗口表格字体会等比缩小；大屏主验收为2K全屏。电视设备长时GPU压力及系统真实后台切换未在本轮完成，隐藏逻辑有自动测试。
- 没有未解决的P0/P1/P2项，最终复验通过。

final result: passed
