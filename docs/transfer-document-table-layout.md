# 转料详情与打印表格化

2026-09-13，已随 `20260912T195806Z` 部署。表格化本身仅调整前端展示，不修改库存核算、单据权限及确认流程；配套日期筛选与加急功能的数据库升级和备份见 [服务器部署](server-deployment.md#2026-09-13-配套发布)。

## 展示

- 独立 TL 单和手工入库详情：条码、单据资料表、流转记录表；取消交接卡片、数量大卡片及详情标签页。来源、流水号、材质、数量重量、说明和已有补充字段保留，长说明独占一行。
- CK 整批详情：共用一个条码，单据资料在表格中；物料集中在同一张明细表，表尾列出服务端合计，不含作废行。作废行仍保留。行内“详情”展开完整资料与修改记录，编辑和作废仍按原权限显示。
- 桌面弹出详情宽度 960px；工作区宽度达到 1660px 后使用 900px 并排详情。窄屏资料表变为单列标签/值，物料与记录表可横向滚动，操作栏固定在底部。
- CK 打印为单联 A4 表格，元数据、明细、合计和说明分区排列；保留签字栏。历史 TL 双联打印模式不变，将原 CSS 模拟资料格转换为标准表格。

## 验证

- 全套前端测试：50 个测试文件、360 项测试通过。新增表格字段配对、长说明、零件数、整批行展开以及无卡片/标签页断言；原接收、修改、作废、版本冲突和打印前刷新测试通过。
- TypeScript 与 Vite 构建通过。保留原有大屏图表资源大小提示。
- 独立 Chrome 检查 1440×1000、2560×1440、390×844；修正长条码导致窄屏网格超宽的问题。390px 屏宽下正文可用宽度 350px，资料表宽度 350px，物料表仅在自身容器横向滚动。
- A4 PDF 实际渲染：手工入库双联为 1 页；现有 3 行 CK 单为 1 页、1 个条码；隔离的 20 行测试样例为 1 页、1 个条码。条码、全部行、合计、说明与签字栏完整可见。
- 截图和 PDF 检查文件：`output/playwright/document-tables-20260913/`。20 行样例仅在浏览器内临时挂载，未写入系统业务数据。
- 最后局部复测 37 项通过，浏览器控制台 0 错误、0 警告。独立浏览器会话已关闭，确认本任务的进程与临时配置目录关联进程退出；其他任务及用户浏览器未操作。
