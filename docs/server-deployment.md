# 服务器部署

换服务器或首次安装请看 [新服务器部署与整套系统迁移](server-migration.md)。本页保留当前服务器布局与历史发布记录，不要将历史版本号直接当作新迁移的运行版本。

当前异步/MQ 版本包含 RabbitMQ 和数据库迁移，不能按旧版“仅换源码”操作。配置、升级顺序及消息备份见 [异步接口与消息队列](async-notifications.md)。

## 当前布局

- 服务器：`122.200.93.68`，SSH 用户 `ubuntu`。
- 项目目录：`/opt/heatsinkrep`。
- 源码版本：`/opt/heatsinkrep/releases/<版本号>`；`current` 指向当前版本。
- 私密配置：`/opt/heatsinkrep/.env`，权限 `600`，不打入源码或镜像。
- 项目专属 Compose：`/opt/heatsinkrep/bin/docker-compose`。
- 网页入口：`http://122.200.93.68:8082`；既有 80、8080、8081 站点不变。
- Compose 项目名：`heatsinkrep`；MySQL 使用 `heatsinkrep_mysql_data` 卷，RabbitMQ 使用 `heatsinkrep_rabbitmq_data` 卷。
- 当前前端发布：`20260926T175302Z`（程序提交 `f94700e`，玻璃登录页与鼠标动效）；后端保持 `20260926T142408Z`（`a5aa28a`，班组承接业务）；数据库版本保持 `20260926_0019`。
- 玻璃物料大屏：`http://122.200.93.68:8082/factory-live`，通过访问锁与账号验证后独立铺满页面。展示全厂及班组分类库存、实际多对多流转、班组间待接收及最近批次；后台“大屏展示”入口保留全屏功能。

使用独立的 `docker-compose.server.yml`，不要与本机 Compose 文件合并。只有前端 Nginx 的 8082 端口对外；后端、MySQL 和 RabbitMQ 都不发布主机端口。容器限制内存与日志大小，设置异常/重启后的自动恢复。

若服务器无法连接 Docker Hub，可通过 `MYSQL_IMAGE`、`PYTHON_BASE_IMAGE`、`NODE_BASE_IMAGE`、`NGINX_BASE_IMAGE` 配置 Docker 在 Amazon ECR Public 发布的官方镜像地址，例如 `public.ecr.aws/docker/library/mysql:8.4`。不改全局 Docker 镜像源，不影响其他项目。

## 访问保护

入口为纯白页面，默认只显示中央锁图标。点击锁后展开口令输入，按回车或右侧箭头提交；按 Escape 可收起。验证通过后才显示原有账号登录页。口令只保存在服务器 `.env` 的 `SITE_ACCESS_PASSWORD` 中，不写入前端代码。

后端使用 12 小时有效的 HttpOnly、SameSite=Lax 签名 Cookie 校验访问资格。没有正确 Cookie 时，登录接口、业务接口、已有账号令牌及接口文档都不能绕过访问锁。更换 `SITE_ACCESS_PASSWORD` 或 `SITE_ACCESS_SECRET` 后，旧 Cookie 会失效。仍然需要正常管理员/班组账号登录，站点口令不替代账号权限。

每个来源 5 分钟内最多 10 次错误尝试，此外有进程级总限额。前端 Nginx 丢弃请求方自带的 X-Forwarded-For，再将客户端地址传给专属网络内的后端。

当前 IP 入口为 HTTP，口令、Cookie 与业务数据在网络传输中不加密，仅适合演示验收。正式使用应绑定域名、配置可信 HTTPS，将 `APP_ORIGIN` 改为 HTTPS 地址、`SITE_ACCESS_SECURE_COOKIE` 改为 `true` 后重建后端容器；同时更新前端转发 HTTPS 协议的配置。HTTP 环境不要设 Secure=true，否则浏览器不会在 HTTP 请求中发送访问 Cookie。

## 2026-09-27 玻璃登录页与鼠标动效发布

前端发布 `20260926T175302Z`，程序提交 `f94700e267f03ea97a0f6ddf0dd90f87cb72dba9` 已推送 `codex/login-release-20260927`。此发布基于线上 `5b31aa9` 独立构建，与当前后端配套使用；主工作区对应登录页提交为 `f7437d7`。名称统一为“热沉事业部综合管理系统”，采用无损金属背景、磨砂玻璃表单、桌面纵向留白和鼠标悬停浮起/掠光，支持触屏与减少动态效果；登录校验、重复提交保护和密码显隐一起发布。

隔离发布源码前端 **75 文件 / 545 项测试通过**，ESLint、格式检查、TypeScript 和生产构建通过。交互和响应式布局在开发阶段经独立 Chrome 验证，详见 `design-qa.md`；本次发布核验使用静态资源哈希、页面入口、接口与服务健康检查，未将其称作线上视觉验收。

服务器验证完整清单 592 个文件，复用 541 个、传输 51 个；增量包 2,690,535 字节，SHA256 `1b4e5c2d97535e4f757960f3d5385621d338f492ba893766007df495d64b21de`。仅更新前端容器，后端、MySQL、RabbitMQ 的容器 ID、启动时间、镜像前后一致，无迁移或物料写入。配置备份为 `/opt/heatsinkrep/backups/pre-20260926T175302Z/config.env`，权限 600。

服务器 98 个静态文件、15 个页面入口、Nginx 配置、访问锁、账号鉴权、总览 API、八班组材质概览、16 次库存查询和 SSE 首帧/心跳通过；本机公网另验证 41 个变更资源和 15 个入口。核验仅创建并注销验证会话。四容器 healthy、重启数 0，`deployment.exit=0`，部署锁已释放。前端镜像 ID `sha256:fb3e6b23d5b8ccf8d3f1456b3d14e5127807e1ea4536ebfdb4ac04642007f7ea`，提交标记为 `f94700e`；`current` 和 `FRONTEND_RELEASE_TAG` 已切换，后端 `RELEASE_TAG` 保持 `20260926T142408Z`。

回退只恢复 `FRONTEND_RELEASE_TAG=20260926T152633Z`，使用旧目录 Compose 执行 `up -d --no-build --no-deps frontend`，并回指旧 `current`；不回灌数据库。发布包、完整清单、组装及部署脚本和日志均保留在新版本目录。本节为部署后的文档补记。

## 2026-09-26 侧栏结构与状态修复发布

程序提交 `5b31aa97c619b25fb5690c15b7c46c71b2a88b0e` 已推送 GitHub，仅前端发布为 `20260926T152633Z`。首页与业务页统一菜单树和样式，保留八班组及三级导航；按账号记住展开与收起状态，搜索、分页和目录刷新不再强制切换分支；菜单溢出时提供上下滚动提示，自动显示当前班组分支。未改变业务数据或权限。

修改阶段 26 项导航、访问门禁与偏好测试通过，TypeScript、生产构建、ESLint、格式检查通过；独立 Chrome 验证了首次加载、刷新、收起恢复、窗口聚焦、1366 × 768、2560 × 1440 及手机抽屉。部署从提交归档独立构建，未启动浏览器，不将资源校验当作线上视觉验收。

服务器按完整清单验证 589 个文件，复用 541 个、传输 48 个；增量包 604,964 字节，SHA256 `9a870345df89407e9eeac3d10adc97804a059217f5c4cbfc10d4de48e3ffea24`。仅执行前端 `up -d --no-build --no-deps frontend`，后端、MySQL、RabbitMQ 容器 ID、启动时间与镜像前后一致。没有迁移、清库或导入演示物料；配置副本位于 `/opt/heatsinkrep/backups/pre-20260926T152633Z/config.env`，与正式配置均为 600 权限。

服务器核对 97 个静态文件、15 个入口、Nginx 配置、访问锁及账号鉴权、首页 API、八班组材质概览、16 次库存查询及 SSE 首帧和心跳；公网另核对 40 个变更资源与 15 个入口，均通过。校验仅创建并注销临时会话，物料写入 0；线上仍为空库存，不将空库检查称作有料业务验收。四容器 healthy、重启数 0，`deployment.exit=0`，部署锁已释放。

前端镜像 ID `sha256:d9f6eb5231b747231cddf3ace4b0f8690782f4b7348aad11da96aa29eda2c4fb`，提交标记为 `5b31aa9`；`current` 与 `FRONTEND_RELEASE_TAG` 已切换，`RELEASE_TAG` 保持 `20260926T142408Z`。发布脚本、清单及日志保留在版本目录。回退只恢复前端标签 `20260926T142408Z`，使用旧目录 Compose 单独更新前端，并回指旧 `current`；不重建其他服务或回灌数据库。本节为部署后的文档补记。

## 2026-09-26 班组承接业务发布

程序提交 `a5aa28af796cabe3e6c90f107f46feecb1fee08b` 已推送 GitHub，前后端配套发布为 `20260926T142408Z`。工作台提供可见的“班组设置 → 承接业务”入口，各班组自行维护；上序逐行选择下序业务。待接收、库存、收发记录、详情及打印统一业务名称，搜索使用历史名称快照，改名/停用不会改写旧单。修复编辑空名称时误用新增草稿的问题，保留本班组权限、配置审计和版本校验。

从提交归档隔离构建，没有包含工作区中正在进行的其他侧栏改动。后端定向 61 项、前端 8 文件 / 100 项通过，TypeScript 和生产构建通过；独立 Chrome 的配置、两行业务分别选择、实际转料及接收方搜索已在隔离 SQLite 库验证，证据见 `docs/team-business-and-opening-stock.md`。本次未修改登录页。

服务器逐文件校验 598 个发布文件，复用 513 个、传输 85 个；增量包 654,842 字节，SHA256 `0ab2bc0c6eaeb923ff2af79d5606ca205ad4e59e96242ce2197516b16a9bab50`。复用现有运行时离线构建，候选后端 78 个应用、迁移与依赖文件哈希一致，依赖和 Nginx 配置检查通过。没有依赖或数据库结构变更。

停写备份 `/opt/heatsinkrep/backups/pre-20260926T142408Z/database.sql.gz`，SHA256 `18abfa7b8431e2272787857202ff0c69596c73c8ad9138e144000fce2444f862`；配置副本同目录，目录 700、文件 600。应用启动前后全部表内容一致；HTTP 验收仅增加/注销验证会话和对应通知，其他业务表保持一致。MySQL 和 RabbitMQ 容器身份、启动时间不变，未导入或清理物料数据。

服务器 97 个静态文件、15 个页面入口、访问锁、账号鉴权、25 次承接业务相关只读查询、16 次班组库存查询、总览 API、SSE 首帧与心跳及 MQ 连接校验通过。线上仍为空库存，本次没有将空库读检查称作有料业务验收。本机公网另核对 43 个变更静态资源和 15 个入口，健康与未解锁保护均通过。四容器 healthy，前后端镜像提交标记均为 `a5aa28a`，`deployment.exit=0`，部署锁已释放，`current` 与两个发布标签一致。

回退应用可恢复后端 `20260926T125922Z`、前端 `20260926T131950Z`，使用对应 Compose 单独重建后端和前端，将 `current` 回指旧前端目录；不用数据库降级或回灌。源码、清单、发布脚本、构建及发布日志保留在发布目录。本节为部署后的文档补记。

## 2026-09-26 材质归类面板高度发布

前端 `20260926T131950Z` 已发布“材质归类”高度修复 `0e5b2ce`：面板至少填满可用高度，空态居中、分页在底部，有数据时保持正常行高并支持整页滚动。保留刚上线的全厂总览及交付计划，不改变字段、库存算法或权限。

程序及文档已推送 GitHub；从 `68f3d49d67fc6a851362b3291b11a95bde60ee1a` 冻结源码重新执行 TypeScript 和生产构建。修改阶段 39 项定向回归、桌面 / 2K / 手机及空态 / 多行布局检查见 `design-qa.md`，本次部署未启动浏览器。服务器按完整清单核对 587 个文件，复用 540 个，增量传输 47 个；包大小 559,993 字节，SHA256 `d3ec750cb76881dc9c5d116b01989e60da82a737d447808d9567ce77a88d983d`。

仅重建前端容器；后端、MySQL、RabbitMQ 容器 ID、启动时间和镜像前后一致，没有数据库迁移、物料写入或演示数据导入。配置备份 `/opt/heatsinkrep/backups/pre-20260926T131950Z/config.env` 与正式配置均为 600 权限。`RELEASE_TAG` 保持 `20260926T125922Z`，`FRONTEND_RELEASE_TAG` 和 `current` 切换为新版本。

服务器校验 97 个静态文件、15 个页面入口，另验证全厂总览 API、八班组材质概览、16 次班组库存查询、访问锁 / 账号鉴权及 SSE 首帧 / 心跳。本机公网核对 40 个变更资源、15 个页面入口及健康 / 未解锁保护均通过。只建立并注销验收登录会话，不将空库检查当作有料业务验收。四容器 healthy、重启数 0，`deployment.exit=0`，部署锁已释放。

前端镜像 ID `sha256:9fe6650696acf4ed2c3980f9000d8622734ee626b7994c06408b189fe4561667`，提交标记为 `68f3d49`。源码、构建清单、校验脚本和 `publish.log` 保留在发布目录。回退仅恢复 `FRONTEND_RELEASE_TAG=20260926T125922Z`，使用旧目录 Compose 执行 `up -d --no-build --no-deps frontend` 并回指旧 `current`；不重建其他服务或回灌数据库。本节为部署后的文档补记。

## 2026-09-26 侧栏展开样式优化

程序 `0a38d9c` 已推送 GitHub，仅前端发布为 `20260926T124051Z`。班组箭头紧邻名称，展开分支浅底分组、三级功能缩进，只有当前功能高亮；保留八班组、三级导航、原有路由和权限。180 ms 展开动效支持减少动态效果，收起侧栏后的浮出菜单仍可选择三级功能。

修改阶段定向回归 **18 项通过**，TypeScript、生产构建、ESLint 和格式检查通过；独立 Chrome 的桌面、手机及浮出菜单验收见 `design-qa.md`。本次从提交 `0a38d9c6487c77f304bde9add7b825ca76fed161` 冻结源码重新构建，未混入预览自动登录或演示数据。服务器完整校验 **576 个文件**，复用 530 个、传输 46 个；增量包 604,186 字节，SHA256 为 `4f0898efaaa561bad96e74982beb161baa422860af568d5254df1e765fadd843`。

只执行前端 `up -d --no-build --no-deps frontend`，后端、MySQL、RabbitMQ 的容器 ID、启动时间及镜像前后相同。没有数据库迁移、物料写入或数据导入。配置备份 `/opt/heatsinkrep/backups/pre-20260926T124051Z/config.env`，与正式配置均为 600 权限。

服务器验证 **98 个静态文件、15 个页面入口**、Nginx 配置、前后端健康、423 访问锁 / 401 账号鉴权、首页 REST 与 SSE 首帧/心跳、16 次班组库存查询全部通过；本机公网另核对 **41 个变更资源和 15 个入口**。只创建并注销验证会话，线上库存为 0 件 / 0 kg，不将空库检查称作有料业务验收。本次部署没有启动浏览器，也没有重新测量并发或延迟指标。

四容器 healthy、重启数 0，`deployment.exit=0`，部署锁已释放。`current` 与 `FRONTEND_RELEASE_TAG` 已切换，`RELEASE_TAG` 保持 `20260926T093819Z`。前端镜像 ID `sha256:45bff7fa6ad7f73decb3d4be0cfb73e95ec39b204451a26f678876645341a7d1`，提交标记与 `0a38d9c` 一致；发布脚本、清单和验证日志保留在版本目录。

回退仅恢复 `FRONTEND_RELEASE_TAG=20260926T115933Z`，使用旧发布目录的 Compose 单独更新前端，再将 `current` 回指该旧目录；不重建后端、MySQL 或 RabbitMQ，不回灌数据库。本节为部署后的文档补记，不修改已发布应用产物。

## 2026-09-26 三级班组导航与顶部区域精简

程序 `f4408c9`（含 `42cfb9c`）已推送 GitHub，仅前端发布为 `20260926T115933Z`。班组功能改为三级侧栏；桌面展开侧栏时移除重复顶部区域，账号、退出登录及大屏入口置于侧栏底部，班组操作合入各页工具栏。手机和收起侧栏时保留简短页面提示。权限、库存字段、接口及后端逻辑不变。

修改阶段前端全量 **72 文件 / 528 项通过**，最终定向回归 **44 项通过**，ESLint、格式检查通过。本次从提交 `f4408c996ce13f1fd7c0da92d7f86b0bd0ed2d0d` 隔离构建，TypeScript 与生产构建通过。服务器逐一校验完整发布清单 **576 个文件**，复用 515 个、传输 61 个，增量包 645,021 字节，SHA256 为 `85720d8ae6e6a3ceed681ca007255cce436c34977e0634b7a2359d8fe1939203`。没有把本地预览登录或演示数据打入产物。

单独执行前端 `up -d --no-build --no-deps frontend`；后端、MySQL、RabbitMQ 的容器 ID、启动时间及镜像前后完全相同。没有数据库迁移、清库、物料写入或演示数据导入。配置备份 `/opt/heatsinkrep/backups/pre-20260926T115933Z/config.env`，与正式配置均为 600 权限。

服务器验证 **98 个静态文件、15 个页面入口**与冻结构建一致，Nginx 配置、前后端健康、423 访问锁 / 401 账号鉴权、首页 REST 与 SSE 首帧/心跳及 16 次班组库存查询通过；本机公网另核对全部 **43 个变更资源和 15 个入口**。线上库存仍为 0 件 / 0 kg，检查只创建并注销验证登录会话，未将空库检查称作有料业务验收。本轮部署没有启动浏览器；本地视觉与交互验收见 `design-qa.md`。

四容器 healthy、重启数 0，`deployment.exit=0`、部署锁已释放。`current` 与 `FRONTEND_RELEASE_TAG` 为新版本，`RELEASE_TAG` 保持 `20260926T093819Z`；前端镜像 ID `sha256:fbef2540c6cc03aff240ddbd5dc63d07f7de37ea4ef54a7a47980bfd193c8ebd`，提交标记与 `f4408c9` 一致。完整源码、清单及发布/校验脚本和日志保留在新发布目录。

回退仅需恢复 `FRONTEND_RELEASE_TAG=20260926T093819Z`，使用该旧发布目录的 Compose 单独更新前端，再将 `current` 回指旧目录；不重建后端、MySQL 或 RabbitMQ，不回灌数据库。本节为部署后文档补记，不改变已发布程序。

## 2026-09-26 班组分类库存与业务页样式统一

程序 `885eef7`（含 `36fc3d7` 的库存内容调整）已推送 GitHub，前后端配套发布为 `20260926T093819Z`。分类库存按用途、来源及物料分类区分，库龄以仍有结存的批次计算；普通业务页统一浅灰绿配色、字体、控件和间距，修复窄屏库存操作列遮挡与按钮排布。机器人 / 玻璃大屏保留独立样式。本次没有依赖、数据库结构或 Compose 配置变更。

发布前后端全量 **483 项通过**（一条测试依赖弃用警告），前端 **72 文件 / 523 项通过**；TypeScript、生产构建、ESLint、格式及差异检查通过。构建锁定 `885eef7a1a0ec0179b702f1433b092296d745a78` 的隔离源码；服务器按清单核对 575 个文件，其中复用 491 个、传输 84 个。增量包 SHA256：`0356d9a58777a0654f8593d0b107a5fcc6a8b8e88da1cc765cba8d90e11badfd`。复用既有运行时离线构建镜像，后端 76 个应用 / 迁移 / 依赖文件内容校验通过。

切换前在候选镜像执行八班组、共 48 次 MySQL 只读查询，覆盖分类、用途搜索、库龄和日期筛选。停写备份 `/opt/heatsinkrep/backups/pre-20260926T093819Z/database.sql.gz`，SHA256 为 `644e5b5f51e026f0c36406c4dfbbfc46d9c9f457e5392de5b90ab76ac892decd`；配置副本同目录，文件权限 600、目录 700。应用启动前后 31 张表摘要完全相同，HTTP 验收后仅验证登录会话及注销通知表变化；库存、账号配置和其他业务数据未变，没有导入或清理物料。

线上验证通过：97 个静态资源、15 个页面入口、访问锁 423 / 账号鉴权 401、首页 REST 与 SSE 首帧和心跳、16 次班组库存 HTTP 查询；消息队列 `connected`、待发 0。正式库存仍为 0 件 / 0 kg，未将空库接口校验当作有料业务验收；有料分类和流转由上述隔离测试覆盖。本机公网另核对全部 47 个变更静态资源与 15 个入口。此次没有启动浏览器，视觉检查记录见根目录 `design-qa.md`，没有新增并发或 P95/P99 测量。

四容器 healthy、重启数 0；MySQL 和 RabbitMQ 容器身份、启动时间不变。`current`、两个发布标签和镜像提交标记一致，`deployment.exit=0`，部署锁已释放。后端镜像 ID 为 `sha256:9544009922c26698b2bc516d6aaa6c43d1baf4efe9b5fae2cd6b609135d17af4`，前端为 `sha256:5b95b893bd76009f6e3c0c450c8967b2625e7685d690c5d34bc43aedb0183108`。

若需回退本轮应用，恢复备份配置中的后端 `20260924T032557Z` 与前端 `20260926T082637Z`，配套更新这两个服务，并将 `current` 回指 `20260926T082637Z`。不重建 MySQL / RabbitMQ，不回灌数据库覆盖后续业务。完整源码、产物清单及发布 / 检查脚本和日志保留在新发布目录；本节为发布后的文档补记。

## 2026-09-26 首页改版与一屏适配

程序提交 `8999d05` 已推送 GitHub，前端单独发布为 `20260926T082637Z`。首页采用浅灰绿布局，展示全厂在库、内部在途、待接收、今日接收、班组库存、物料类型和重点关注；分类保留件数及重量，可跳转班组和批次详情。页面各模块使用同一实时快照，保留空态、断线提示、缺失班组及核平提示。

普通电脑窗口按可用高度分配两排模块，较矮窗口将类型环图与明细并排；1366 × 768、1280 × 720 等尺寸中八个班组与四条待接收记录一屏可见。较窄 / 极矮窗口、手机及业务告警保留必要滚动。正式公司标识、大屏和其他业务页保留，正式构建不包含本地预览自动登录、演示库或本地通知替代。

发布前前端全量 **71 文件 / 517 项通过**，TypeScript、生产构建、ESLint、格式及差异检查通过。此前独立 Chrome 的十组尺寸与交互验证见根目录 `design-qa.md`；本轮部署没有启动浏览器，不将静态文件校验称作线上视觉验收。GitHub 托管 CI 结果未核验。

上传时采用增量包复用旧发布文件，完整性检查发现工作区并行修改，在任何线上切换前中止了初次组装。随后锁定 `8999d0584d6b1df01dda22c460df397b3350dde6`，从隔离目录重新构建；97 个产物的 SHA256 与最初已验证的构建全部相同。最终完整发布目录的 **573 个文件**逐一按提交源文件和冻结构建校验，复用旧版 519 个文件、传输 54 个变更文件，未混入其他开发内容。增量包 SHA256：`8c9e21c5c16a01517f33159a2e469075d34176705ab4eddd317b490606fa00c6`；首次未切换的组装目录保留为 `20260926T082637Z.assembly-attempt1`。

使用旧前端 Nginx 镜像作为构建基础，仅执行前端 `up -d --no-build --no-deps`。后端、MySQL、RabbitMQ 的容器 ID、启动时间及镜像前后完全相同；没有数据库迁移、物料写入或演示数据导入。只读接口验收时在库为 0 件 / 0 kg、待接收 0 批，临时验证登录会话已注销。配置备份位于 `/opt/heatsinkrep/backups/pre-20260926T082637Z/config.env`，权限 600。

服务器验证：97 个静态文件、15 个页面入口与冻结构建一致，Nginx 配置、前后端健康、访问锁 423 / 账号鉴权 401 通过；首页 REST 与 SSE 首帧 / 心跳正常，全厂及班组物料分类件数与重量核平。四容器 healthy，`deployment.exit=0`，部署锁已释放。`current` 和 `FRONTEND_RELEASE_TAG` 为新版本，`RELEASE_TAG` 保留旧后端标签；新前端镜像 ID 为 `sha256:236ff6a5ec4838d98d9187988b4beea545a0bd072d099cd2c71574fc969f659d`，镜像提交标记与 `8999d05` 一致。

本机公网复核：本次全部 **47 个变更静态文件**的内容哈希、15 个页面入口、前后端健康及未解锁访问保护通过；未变化的大型模型和图片已在服务器完成上述 97 文件全量校验。没有将中止的公网全量大文件下载视为通过证据。

回退只需恢复 `FRONTEND_RELEASE_TAG=20260924T032557Z`，使用旧目录 Compose 单独更新前端并将 `current` 回指旧版本；不重建后端、不回灌数据库。源码、产物清单、增量包、组装及部署脚本和日志保留在新发布目录。本节为部署后记录，不改变已发布的程序。

## 2026-09-24 八班组目录清理

程序 `31f0cc3` 已推送并发布为 `20260924T032557Z`。移除旧“转废”“发货”目录：一条旧报工的报废归属调整到库房，两条旧工序和一条旧工艺路线的归属调整到检验；历史记录、数量和原因保留，原归属写入两条管理审计。当前目录仅保留约定的八个班组，接口阻止重新创建旧类型、编码和名称。操作说明见 [旧班组清理](legacy-team-cleanup.md)。

后端相关回归共 47 项通过（其中清理专项 7 项重跑通过），前端相关 14 项、类型检查、构建与质量检查通过。MySQL 上先执行回滚预演，再停写备份和一次事务清理，重复执行无变更。逐表摘要确认物料、库存、账号及其他非目标表不变，三张旧业务表行数不变。数据库容器未重启，无数据库结构迁移。

备份 `/opt/heatsinkrep/backups/pre-20260924T032557Z/database.sql.gz`，SHA256 `2d5d7914e4cc49ac06779ee5d5860a1d68aa3a7175dffe4355a23ca372295160`；原始记录可从备份及调整审计追溯。发布时发现 macOS 打包的 `._assets`、`._brand` 附带文件导致静态校验失败，程序自动回退，数据清理保留。关闭扩展属性打包并移除这两个本轮生成的文件后重新构建前端，从发布阶段继续，没有重复处理历史数据。

最终 92 个静态文件、15 个页面入口、三路 SSE 首帧/心跳/注销失效及访问锁通过；消息待发 0，四容器 healthy、重启数 0，`deployment.exit=0`，`current`、私密配置与前后端镜像标签一致。HTTP 检查后再核对业务数据未变。本机未启动浏览器。

## 2026-09-24 写入查询及通知延迟优化发布

北京时间 9 月 24 日发布后端 `20260923T170214Z`（标签为 UTC），程序提交 `e195d35`，源码归档提交 `7dfa8c1`，均已推送 GitHub。包含通知调度时间修复、出库查询优化，以及新入库和批量出库重试读取优化。没有依赖或数据库结构变更；前端和 MySQL 容器保持原身份，四容器 healthy、重启数为 0。

全量后端回归 473 项通过。正式候选镜像再次完成隔离 MySQL 的 17 组 HTTP 并发与正确性场景、跨进程消息广播、停机补发和调度时间边界验证，测试资源已清理。新入库 SQL 由 13 次降至 11 次，100 条出库重试由 107 次降至 8 次；本次没有重新测量五分钟混合负载的 P95/P99，不新增并发容量承诺。

停写备份 `/opt/heatsinkrep/backups/pre-20260923T170214Z/database.sql.gz`，SHA256 `9d60c665d45116e6bf28add4b58ae8dc03a7c014db27213862262eba13667f55`。切换时 31 张表摘要完全相同；HTTP 验收仅改变验证会话并增加 4 条已发布的会话注销通知，原通知摘要及全部业务表不变。正式库保持原有零库存，没有导入测试物料。

首次收尾校验漏计注销通知，按脚本保护逻辑临时停止入口。确认业务数据一致后恢复入口，并逐条验证新增通知仅涉及单个会话注销、原通知未变化，再完成发布标记；保留 `deployment.initial.exit` 及原始日志，最终 `deployment.exit=0`。公网前后端健康、423 访问锁、92 个静态文件、15 个页面入口、三路 SSE 首帧/心跳及会话失效验证通过，通知待发为 0。

发布目录保留源码、部署与校验脚本及日志；镜像 ID `sha256:94378467ac565016fa5d1ebde11dcdbb670a69ed9ea83c58509d918cbc4b81a1`。`current`、`.env` 与运行镜像一致。本节为部署后补记，不改变已验收程序。

## 2026-09-23 异步接口、事务余额与消息队列发布

程序提交 `09d62b5`（包含 `c487bd0`、`7e9e806`）已推送 GitHub，后端发布为 `20260923T110054Z`。HTTP 及 MySQL I/O 异步化，当前库存改为同事务维护余额；通知统一经 MySQL outbox 和 RabbitMQ 广播，取消短期库存响应缓存。密码计算和旧主系统 HTTP 适配器在线程中等待，维护 CLI 仍为同步。仍保持一个 HTTP worker，未擅自增加连接池或后端/MySQL 内存限额。

新增 RabbitMQ 4.3.6，使用单独生成的私密密码、512 MiB 内存限额和独立持久卷，不开放主机端口。后端镜像校验固定依赖、应用及迁移文件，镜像 ID 为 `sha256:387542d309626b8f71ebaf13d43c4876143997f1c25d810825112dc6c2c84011`。前端镜像与容器保留 `20260923T080136Z`，仅重启以重新解析后端；MySQL 容器与启动时间未变。

发布前，真实 MySQL 中的旧版 433 条物料升级、重复迁移、11 组并发竞争、跨进程广播、重复消息、死信及停机补发通过。待发布镜像完成 25 独立账号、25 路 SSE、两路连续写入的五分钟混合验证，4,116 次查询、52 个完整循环，无非预期错误，件数重量与原始流水一致；全部 P95/P99 达到本轮建议标准，详见 [性能与容量验收](performance-and-capacity.md)。这些性能数字来自隔离测试，不是向正式库存写入压测数据。后端同一源码全量 447 项通过，本轮前端连接相关 22 项复验通过。

停写备份：`/opt/heatsinkrep/backups/pre-20260923T110054Z/database.sql.gz`，SHA256 `2200d3e4d604997ded4ba6d538754c25fa0bbb0342a0c1b96722a0740d380229`；备份目录 700，数据库文件和配置副本 600。正式执行 `0016 → 0017 → 0018`，增加余额、通知两张表；除 Alembic 版本外，原有 29 张表在切换前后行数/内容摘要相同。HTTP 验收后只增加并注销验证会话及其通知，原业务表、账号配置未改变。正式库存此时为零，未为验收伪造物料。

迁移后的静态资源核验首次误用了上次“后端发布目录”，未找到前端构建产物，入口按保护逻辑暂停。修正为实际前端版本目录并从核验阶段继续，没有重做迁移、回灌备份或改变应用代码。最终服务器及本机公网核对 92 个静态文件、15 个页面入口、健康与访问锁均通过；三路 SSE 首帧/心跳、无关账号不刷新库存、注销使本人连接失效均通过。通知状态 `connected`、待发 0；四容器 healthy、重启数 0，`deployment.exit=0`。

`current`、`.env` 的后端标签和镜像提交标记已一致，前端标签不变。源码包 SHA256 `60e9f60946ce6c4b0b0da04066ff1581b5cc77166bc619cf26d1072246dde067`。发布目录保留初次中止记录、修正后的部署/核验脚本、备份摘要和验证证据；隔离容器、测试卷与网络已清理，未使用浏览器。文档更新为发布后补记，不改变已验收程序镜像。

本版本不能照搬下方旧版的镜像直接回退步骤。旧应用不维护库存余额且不认识新迁移；需要停写后评估兼容性，优先前向修复。禁止在已有新版业务后直接启动旧写入程序或回灌旧备份覆盖新增业务，消息数据卷也必须纳入后续迁移和备份方案。

## 2026-09-23 大屏精简计算与混合负载验收

程序提交 `08f1d05` 已推送 GitHub，后端发布 `20260923T090917Z`。机器人大屏不再计算最终未展示的趋势、排行榜、库龄和库存矩阵，每次构建少 13 次查询；全厂分析仍保留完整功能，所有大屏数据字段、库存公式和权限规则不变。前端镜像及容器保留 `20260923T080136Z`，仅重启原前端以重新解析后端地址；MySQL 容器及启动时间不变。未增加 worker、连接池或资源限额，没有数据库迁移。

发布前后端全量 436 项及质量检查通过；隔离 MySQL 的 557 条物料逐字段核对旧版与候选版，大屏查询由 35 次降为 22 次，完整概览也保持一致。另完成 25 独立账号的九组 HTTP 写入竞争验收，以及优化前后各五分钟的混合查询、两路连续写入和 25 路 SSE 复验，最终库存均与原始流水一致。性能数值与测试限制见 [性能与容量验收](performance-and-capacity.md)；测试脚本分别提交为 `9184dd4`、`71ae3e2`。

停写备份：`/opt/heatsinkrep/backups/pre-20260923T090917Z/database.sql.gz`，SHA256 `28e126d62b2189ec5cc1357cac56c1589e9eadf0bf6cb6f3c587d8cebfcb45ef`。配置与备份权限 600、备份目录 700。应用切换前后 29 张表内容摘要完全一致，HTTP 验收后仅临时登录会话表变化，物料业务、账号配置及数据库结构未改动；正式库没有写入测试物料。

服务器与本机公网均核对 92 个静态文件、15 个页面入口及健康检查。三路 SSE 首帧、心跳、无关登录不触发库存刷新、注销立即失效，以及 423/401 访问保护通过。三容器 healthy、重启数零，`deployment.exit=0`，`current` 和 `RELEASE_TAG` 已切换，`FRONTEND_RELEASE_TAG` 保持不变。未使用浏览器。

源码包保存在发布目录的 `source.tar.gz`，SHA256 `1f710817489c76e25e2f0b9f80f59438d0653b2f5a4e43f9c049418689642162`；同目录保留部署和核验脚本、日志、`APPLICATION_COMMIT` 及 `validation-evidence.tar.gz`。隔离容器、临时数据、专用网络和负载进程已清理。此处为发布后文档补记，不改变已验证的应用产物。

回退时停前端入口，将 `RELEASE_TAG` 恢复为 `20260923T080136Z`，使用旧目录的独立 Compose 执行后端 `up -d --no-build --no-deps backend`；后端健康后启动原前端，恢复 `current` 并核验健康。前端标签不变，不回灌数据库覆盖后续业务。

## 2026-09-23 通知范围与班组共享查询发布

四次修复提交 `99733bb`、`f2f02ba`、`40c9789`、`d77eeb0` 已推送 GitHub，前后端配套发布为 `20260923T080136Z`。登录/退出不再让全厂库存重算，前端只刷新相关班组及确实变化的目录/身份；相同班组、筛选及分页的汇总和库存明细合并计算。跨日通知、每次请求鉴权、写入事务及库存公式保留；未增加 worker、连接池、内存限额或修改数据库结构。

发布前本地前端 518 项、后端 418 项全量回归通过，收尾相关专项 53 项通过（有交集），类型、质量和生产构建检查通过。新镜像在隔离 MySQL 8.4 的 433 条物料中核对八班组库存、列表及全厂矩阵；25 路相同班组汇总与库存明细各只构建一次，分别执行 30 / 29 次 SQL，全部响应一致。临时验证库及网络随后清理，未导入正式物料。

停写备份：`/opt/heatsinkrep/backups/pre-20260923T080136Z/database.sql.gz`，SHA256 `c655ff5733cdad2dfab33847b6ce3b7f309a3fe285b264a9ffe34b3f9b0baccd`。配置、备份权限 600，备份目录 700。应用切换前后 29 张表的行数及摘要完全相同；HTTP 验收后仅临时登录会话表变化，正式物料、账号及配置不变，MySQL 容器未重启。

服务器和本机公网均核对 92 个静态文件、15 个入口、Nginx 及 API 健康。三路 SSE 首帧、心跳、423/401 保护通过；额外登录/退出不再触发库存帧，原会话退出后已有连接及时失效。三容器 healthy、无重启，`deployment.exit=0`，`current`、`RELEASE_TAG`、`FRONTEND_RELEASE_TAG` 均已切换。未使用浏览器，不把静态文件校验写成视觉验收。

部署包 SHA256 `a6158f6b7d6cf0b5d21cd27816580ab69d0c34a42104730125e401f9f9dbbb22`。发布目录保留脚本、日志、完整提交标记及验收结果；性能对照和局限见 [性能与容量验收](performance-and-capacity.md)。

回退时停前端入口，将 `RELEASE_TAG` 恢复为 `20260923T065006Z`、`FRONTEND_RELEASE_TAG` 恢复为 `20260922T203935Z`，使用旧源码目录的独立 Compose 执行后端、前端 `up -d --no-build --no-deps`，恢复 `current` 并核验健康。旧镜像、源码及备份保留；没有结构变化，不要回灌旧库覆盖新增业务。

## 2026-09-23 后端性能优化发布

三项优化独立提交 `a4cf826`、`bc0cac4`、`0021eba` 已推送 GitHub，后端发布为 `20260923T065006Z`：共享实时快照、限制列表关联数据加载、复用班组材质汇总。仅替换后端镜像；前端镜像与容器保留 `20260922T203935Z`，MySQL 容器 ID 和启动时间不变。单 worker、内存、连接池及访问保护配置保持原样，没有依赖变更或数据库迁移。

发布前在无公开端口的隔离 MySQL 8.4 中生成 433 条物料记录，验证八班组库存、列表字段与查询次数、全厂矩阵，以及两类汇总各 25 路并发读取共享一次快照；全部通过，临时数据库及网络已清理。样例未进入正式库存；正式库在发布前后物料记录均为零。后端质量检查、差异检查及镜像 `pip check` 通过，相关回归和测量边界见 [性能与容量验收](performance-and-capacity.md)。

停写备份为 `/opt/heatsinkrep/backups/pre-20260923T065006Z/database.sql.gz`，SHA256 `a8ff589fd123e88152826dd23f7297b1056e57663ca796552a96df17bec4705b`；目录权限 700、私密配置与数据库备份权限 600。29 张表在应用切换前后行数、内容摘要全部一致；HTTP 验证和短时复测后再次核对，仅临时登录会话表变化，业务表、账号及配置未改动。

线上八班组库存和库存矩阵一致；三路 SSE 的首帧、真实会话事务提交通知、心跳与 423/401 保护通过。服务器及本机公网均核对 92 个构建文件、15 个页面入口，Nginx 与前后端健康检查通过。本轮未使用浏览器，不将页面入口和静态文件校验表述为视觉验收。

短时公网复测中 10 / 25 模拟客户端完成，查询 P95 为 1.16 / 2.57 秒；50 档 P95 4.59 秒，触发保护停止，未继续加压。全部查询成功、提交推送均收到，临时会话已注销；这是空库存、共享会话的 API 读取短测，不是正式容量承诺。三容器最终 healthy、重启数为零。

`deployment.exit=0`，`current` 和 `RELEASE_TAG` 已切换，`FRONTEND_RELEASE_TAG` 仍为 `20260922T203935Z`。后端镜像短 ID `18e567de5218`，镜像标记完整程序提交 `0021eba3242fa0d8cf0e4c6626f67a94d563e02f`。源码及部署包 SHA256 `76e2a292ac410b0214336a25ea2bf812f55d6ff2fa76d3ee2d39c1c8ceb00f36`；发布目录保留验证脚本、日志及脱敏负载结果。本节为发布后文档补记，不改变已验证的程序产物。

若需回退：停前端入口，将 `RELEASE_TAG` 改回 `20260922T203935Z`（前端标签无需改变），使用该旧目录的独立 Compose 执行 `up -d --no-build --no-deps backend`；后端健康后重新启动原前端容器，使 Nginx 重新解析后端，再将 `current` 回指旧目录并核验健康。旧镜像、源码及备份保留；数据库结构未变，不回灌旧备份覆盖后续业务。

## 2026-09-23 全厂库存矩阵与实时汇总发布

程序提交 `395f53d` 已推送 GitHub，前后端配套发布 `20260922T203935Z`。新增“全厂总览 → 库存明细”（`/factory-stock`）：正式八班组为行、具体材质及牌号为动态列，每格显示件数和 kg，包含行、列及全厂总计。按当前实物在库统计，含废料、不含在途；复用首页 SSE，业务事务提交后更新并高亮变化，不依赖定时刷新。本提交同时收录上一版已上线但尚未提交的品牌、登录页、紧凑工作区改动及截图版用户手册。

发布前本地前端 71 文件 / 514 项测试、后端库存矩阵及实时推送相关 63 项测试通过；后端质量检查、前端 ESLint、格式检查、TypeScript、生产构建及差异检查通过。页面已在本地独立 Chrome 验证 1440、2560（2K）及 390 像素布局，检查结束关闭自建进程；本轮部署未使用浏览器。

依赖和迁移未变，使用已验证的运行镜像依赖层构建，后端保持单 worker。停写备份位于 `/opt/heatsinkrep/backups/pre-20260922T203935Z/database.sql.gz`，SHA256 `fbd3b32f33188c00e11546991d0b9579a70fa6c0db6d3335a7c18f7783448575`；同目录保存权限 `600` 的配置及逐表摘要。应用切换前后全部表行数、内容摘要完全一致，MySQL 容器 ID 与启动时间不变，没有清库、结构变更或测试物料导入。

通过真实 MySQL 来源库存核对全部矩阵单元、行列合计、全厂总计和首页库存。核验时线上八班组当前库存均为零，材质列为空，未为显示效果伪造库存。三路 SSE 的首帧、实际会话事务提交通知、心跳及 423/401 访问保护通过；HTTP 验收只创建并注销两个验证会话，无物料业务写入。

服务器及本机公网的 92 个构建文件、15 个页面入口与产物逐一匹配，Nginx 配置及前后端健康检查通过。三容器 healthy，`deployment.exit=0`，`current`、`RELEASE_TAG` 和 `FRONTEND_RELEASE_TAG` 均已切换；前后端镜像均标记程序提交 `395f53d`。源码及构建包 SHA256 `52d7002b1725a2142cf72ee4c0125e7bf2ef0b2bbe3cfbf39107a23e3048b8fe`。版本目录保留部署、只读对账、HTTP/SSE 验收脚本及日志；本节为发布后文档补记，不改变已验证应用产物。

回退时将 `RELEASE_TAG` 恢复为 `20260920T035034Z`，将 `FRONTEND_RELEASE_TAG` 恢复为 `20260922T100012Z`，使用旧源码目录的独立 Compose 执行 `up -d --no-build --no-deps backend frontend`，再将 `current` 回指 `20260922T100012Z`。旧版本、镜像与备份保留；数据库无结构变化，不应回灌旧数据覆盖上线后的业务记录。

## 2026-09-22 品牌、登录页及紧凑工作区发布

前端独立发布 `20260922T100012Z`，包含安泰天龙官方标识、保留原背景的登录页调整、系统设置与流转查询的紧凑布局。全链路追踪默认显示在系统侧栏内，右上角“全屏查看”仅放大全链路画布；退出全屏保留流水号和当前查询。班组及班组长管理将新增按钮并入筛选栏，主系统配置合并重复状态区，转料记录合并状态和操作栏，扫码查询改为单行输入区加最近查询表。原独立机器人大屏保留。

本地前端 70 文件 / 507 项测试通过，最后调整后的全链路专项 24 项复验通过；ESLint、格式检查、TypeScript 和生产构建通过。使用 Playwright 独立 Chrome 检查六个修改页面，以及 1440、2560（2K）和 390 像素布局，验证扫码详情、缩放、全屏/退出及流水号保留。未使用 Codex 内置浏览器；检查用 Chrome、演示后端和临时前端进程均已退出，线上未导入演示数据。

发布使用当前前端运行镜像作为构建基础，仅执行前端服务的 `up -d --no-build --no-deps`。`RELEASE_TAG` 仍为 `20260920T035034Z`，只将 `FRONTEND_RELEASE_TAG` 更新为 `20260922T100012Z`；后端与 MySQL 容器 ID、启动时间在发布前后完全一致。没有数据库迁移、清理、恢复或业务数据写入。配置备份为 `/opt/heatsinkrep/backups/pre-20260922T100012Z/config.env`，权限 `600`。

服务器及本机公网复核：89 个构建文件、14 个页面入口与产物逐一匹配；Nginx 配置、前后端健康接口和未认证访问锁通过。前端镜像短 ID `aabc74c68eac`，`deployment.exit=0`。发布目录保留 `publish-frontend.sh`、`verify-assets.py`、`deployment.log` 及容器前后对照。

源码包由本地 `45b1c38` 基线与当前工作树修改构建，并非新增 Git 提交；本次仅部署，没有提交或推送 GitHub。源码及构建包 SHA256：`ac998783c1543d00bbbbfa0f2a71e9d80e8dcdf4af8371d91fa0b3123cb6f02a`。本节为发布后补记，不改变已验证的前端产物。

如需回退，只将 `FRONTEND_RELEASE_TAG` 恢复为 `20260920T035034Z`，使用旧发布目录的 Compose 执行 `up -d --no-build --no-deps frontend`，再将 `current` 指向旧目录；不要重建后端、还原数据库或降级迁移。

## 2026-09-20 代码规范、诊断日志与管理审计发布

整改提交 `e164d27` 及静态资源权限修复 `623dac7` 已推送 GitHub，前后端配套发布 `20260920T035034Z`。新增请求 ID、脱敏诊断日志、前端集中异常处理、账号与班组管理审计，以及 ESLint / Prettier / Ruff / CI 检查；整理核心模块格式、关键注释和大屏响应模型。物料维护脚本改为当前七表白名单。本次发布没有再次清理物料，也没有导入测试库存。

本地前端全量 70 文件 / 506 项、后端全量 380 项通过，前端 lint / 格式 / 类型 / 生产构建、后端基础及核心严格检查通过。部署前在隔离 MySQL 8.4 中验证 `0015 → 0016`、重复升级、原有身份保留、六项管理审计及审计失败时业务回滚；临时验证容器已清理。GitHub 托管 CI 的运行结果未核验，不以本地通过代替托管结果。

生产迁移只增加管理审计表及索引。迁移前备份在 `/opt/heatsinkrep/backups/pre-20260920T033731Z/database.sql.gz`，SHA256 `3d4424ef18999fe4b07737cc3eeb7de1f21d4eba8773754295aef7920b0fc2b0`。最终切换另建停写备份 `/opt/heatsinkrep/backups/pre-20260920T035034Z-retry/database.sql.gz`，SHA256 `641240df52f43b43412c504b62ca03814c4c183252766b6c8b97befa88ecf7fa`；同目录保留私密配置副本、逐表行数/内容摘要与数据库容器对照。迁移保留全部原有表数据；最终切换前后全部表内容一致。MySQL 容器 ID 和启动时间未变，账号、班组、用途及其他配置保留。

发布经历两次自动回退，记录均保留：`20260920T033731Z` 首次验收发现打包目录权限过严，Nginx worker 无法读取静态资源；`623dac7` 在两份前端 Dockerfile 中明确设置静态文件可读权限，重建后在切换前验证全部资源。`20260920T035034Z` 首次发布的资源与请求追踪检查通过，但旧验收脚本给空库存的加急状态、日期筛选传入无效文本，接口返回 422；修正为合法枚举与日期，先在旧服务完成空库存预检，再使用同一应用镜像重新发布。两次回退均保留新增数据库结构，没有回灌数据库；第二次失败日志见 `publish.attempt1.log` 与对应 exit 文件。

最终公网 89 个构建文件、14 个页面入口与发布构建一致，Nginx 配置检查通过；八班组台账、库存、来源、用途、期初状态及全链路接口通过，152 项空资料字段查询通过。全厂与八班组在库、在途件数及重量均为零。三路 SSE 首帧、真实会话提交通知和心跳通过，访问锁 423、账号鉴权 401 通过。验收只创建并注销临时登录会话，没有物料写入。本轮未使用浏览器。

请求诊断验证覆盖 8 个关联 HTTP 请求；后续日志抽查取得 301 条结构化请求记录，Nginx 与后端请求 ID 可关联，查询探针未进入日志，抽查中未发现应用失败或数据库健康异常。另从本机直接访问公网验证健康接口、生成的请求 ID、CORS 暴露响应头，以及首页、大屏、全链路入口与构建哈希一致。

三容器 healthy，`deployment.exit=0`、`publish.exit=0`；`current` 与前后端镜像标签均为 `20260920T035034Z`，部署锁已释放。后端镜像短 ID `0c4cf45ad2ca`，前端 `50c62709dbfc`。初始源码及构建包 SHA256 `b0e631206a0db2fd41b41d19a090b1f7066288000cf255b0de0220e1bf9df476`；后续仅修正部署验收脚本，最终脚本清单 `deployment-scripts.final.sha256` 在服务器逐项校验通过。本节为发布后的文档补记，不改变应用产物。

如需回退应用，将 `RELEASE_TAG` 与 `FRONTEND_RELEASE_TAG` 恢复为 `20260920T012302Z`，使用旧目录 Compose 配合本次目录 `rollback-compose.yml` 执行 `up -d --no-build --no-deps backend frontend`，再回指旧 `current`。覆盖文件跳过旧 Alembic，避免旧源码不认识 `0016`；保留审计表及记录，不自动降级或回灌数据库。旧版不会生成新增的管理审计，回退后应限制管理修改并尽快修复前进。

## 2026-09-20 用户确认清空物料业务

用户明确确认“物料业务全部清空，库存归零”，保留账号、班组和用途配置。应用仍为 `20260920T012302Z`，本次未部署新应用代码。维护操作为 `material-reset-20260920T022934Z`，脚本与日志保留于 `/opt/heatsinkrep/operations/material-reset-20260920T022934Z`，`operation.exit=0`。

停写后完整备份至 `/opt/heatsinkrep/backups/material-reset-20260920T022934Z/database.sql.gz`，SHA256 `2b71d48ffeae06bac37b1f51ec4264834c78b252e0950f4645ffd65005da93a5`，gzip 与 SHA256 校验通过；目录权限 700、备份权限 600，同目录保存配置副本、逐表摘要、删除数量及数据库容器标识。

限定单事务清理当前物料模块：433 条物料批次（包含入库、班组转料、对外出库和检验发货）、376 条出库提交、192 条损耗、645 条关联物料审计、1 条加急状态和 1 条加急审计；期初提交表原本为 0 条。未调用会额外删除工单/产品的旧 `reset_business_data.py`。保持外键约束开启，按来源关系从叶节点删除，不重置编号计数器。清理前的专项测试覆盖保留表、重复执行、循环引用回滚及未知依赖阻断。

事务提交前校验其余全部表的行数和内容摘要一致：9 个账号、10 条班组配置、用途/授权配置、登录会话、主系统配置、条码计数器及旧工单模块均保留。恢复后八班组在库、可用、在途、待接收的件数和重量全部为零，40 项已认证班组列表查询、大屏 REST 和 SSE 零库存首帧通过。验证仅创建并注销临时登录会话。三容器健康，MySQL 未重启，公网健康检查通过，维护锁已释放。

本次操作报告见备份目录 `reset-report.json`；代码规范、注释及日志检查另见 [2026-09-20 代码检查](code-quality-review-20260920.md)。如需恢复旧物料，应先停写、另备份当前库并评估清理后的新增记录，不能直接回灌旧库覆盖新业务。

## 2026-09-20 全链路时间泳道发布

程序提交 `eb7b903` 已推送 GitHub，前后端配套发布 `20260920T012302Z`。管理员“流转查询 → 全链路追踪”（`/material-trace`）更新为同屏时间泳道：八班组、跨天缩放与拖动、批次定位、悬浮明细、点击关联高亮。接口增加真实审计历史与 `observed_at`，根据转出、改量、作废退回和丢失重建在库停留区间，并与当前库存核平；停留不冒充加工耗时。保留现有玻璃大屏、班组工作台、访问锁、账号权限与 SSE，不恢复全链路抽屉。

本次提交前前端全量 69 文件 / 500 项、后端全量 361 项通过，TypeScript 和生产构建通过。前端单 worker 顺序执行；部署未启动浏览器，视觉与交互证据见 `design-qa.md`。依赖及迁移与前一版本一致，镜像复用原依赖层，后端 `pip check` 通过。

切换前在新镜像只读核对线上 25 个流水号、433 个批次、645 条审计事件，件数、重量、历史、丢失与台账一致；同一提交的前端模型处理全部快照，得到 485 个停留区间、0 个未核平批次，当前区间合计逐流水号匹配 MySQL。首次预检辅助脚本把服务层日期对象当成 HTTP 字符串而停止；改用与 API 一致的 JSON 编码后全量复验通过，未改应用镜像，也未切换当时运行服务。首次日志保留在 `build.first-attempt.log`。

停写备份：`/opt/heatsinkrep/backups/pre-20260920T012302Z/database.sql.gz`，SHA256 `a6eafe0f7c2572bb4cacb8e6f2d2bd82a0c665d9110e8d345193cffb8b07be75`。同目录保留私密配置、逐表摘要及数据库容器记录。应用切换前后所有表行数、内容完全一致；MySQL 容器 ID 与启动时间未变。没有数据库迁移、清库或导入测试物料；后续 HTTP 验收只创建并注销验证会话。

切换后再次核对全链路及八班组分类库存，首页和玻璃大屏合计一致：全厂在库 17,298 件 / 1,729.8 kg，内部待接收 184 批 / 1,648 件 / 164.8 kg。公网 89 个构建文件、14 个页面入口与发布构建一致；128 项字段匹配、24 项空资料查询、三路 SSE 首帧/真实会话提交通知/心跳及 423/401 保护通过。本机公网健康检查返回服务和数据库正常，`/material-trace` 入口哈希与本地构建一致。

三容器 healthy，`publish.exit=0`，`current` 与前后端镜像标签均已切换。初始源码及构建包 SHA256 `b56caac4605d5da0e881403f5d3623bc04137a1eca046e707fbadd6512e1682d`，不含私密配置、密钥或数据库；预检辅助脚本修正后另行上传，最终脚本摘要见版本目录 `deployment-scripts.sha256`。后端镜像短 ID `b067e1d2b981`，前端 `12d86af3b4a4`；构建、发布与核验日志保留。本节为发布后的文档补记，不修改已验收应用产物。

回退应用时，将 `RELEASE_TAG` 和 `FRONTEND_RELEASE_TAG` 均恢复为 `20260919T164117Z`，使用该旧目录的 Compose 执行 `up -d --no-build --no-deps backend frontend`，再回指旧 `current`。数据库结构与业务写入规则未变，不回灌旧备份；旧源码、镜像及备份保留。

## 2026-09-20 玻璃物料大屏与分类库存接口发布

程序提交 `1ab9934` 已推送 GitHub，前后端配套发布 `20260919T164117Z`。玻璃厂区大屏沿用 `/factory-live`、现有访问锁、账号权限及 SSE；全厂和八班组均按物料类型显示件数、重量，分类与合计来自同一次分组查询。线路按实际班组关系生成，待接收时光点移动，全部接收后停止；线路与最近批次可进入原业务记录。保留仅有重量的物料及未分类数据，断线时保留上次数据并停止动效。发布包仅从该提交生成，未包含工作区另行修改的全链路追踪页面及接口。

该提交的独立源码副本完成前端 68 文件 / 487 项全量测试、后端全厂总览 / 大屏 / 实时推送相关 57 项测试，TypeScript 和生产构建通过。前端单 worker 顺序运行。依赖及迁移文件与上一发布一致，服务器基于上一版镜像依赖层构建，后端 `pip check` 通过。切换前，新镜像对线上 MySQL 只读核对全部分类库存及实际线路；上线后再次核对八班组库存、来源批次、内部待接收、REST 与 SSE 新字段。全厂在库为 17,298 件 / 1,729.8 kg，内部待接收为 184 批 / 1,648 件 / 164.8 kg，共 8 条实际线路。

停写备份：`/opt/heatsinkrep/backups/pre-20260919T164117Z/database.sql.gz`，SHA256 `a06171d3b52658d3b2cf5e14f6c278f2b229f2dcca6b3add2f6a2a2218afafbb`。同目录保存私密配置、切换前后逐表摘要及数据库容器标识；应用切换前后所有表的行数、内容完全一致，MySQL 容器 ID 与启动时间不变。本次没有数据库迁移、清库或导入本地测试物料。后续 HTTP 验收仅创建并注销验证会话，没有物料业务写入。

公网 89 个构建文件和 14 个页面入口与提交构建一致；三路 SSE 首帧、真实会话事务通知、心跳及 423/401 访问保护通过，另完成 128 项字段匹配、24 项空资料查询。三容器 healthy，`publish.exit=0`，`current` 与前后端镜像标签均已切换。本轮部署未使用浏览器；此前本地已用独立 Chrome 验证大屏显示、动效及业务跳转。

源码及构建包 SHA256 `e1573afe0c854c3b3e5af7fd6f3e89fa3f7f8d720eb52ea9a211d7bbf61f666f`，不含私密配置、密钥或数据库。后端镜像短 ID `24624ca46da9`，前端 `5b5ea1613dc2`；构建、发布、只读核验脚本及日志保留在版本目录。本节是发布后的文档补记，不修改已验收应用产物。

回退应用时，将 `RELEASE_TAG` 与 `FRONTEND_RELEASE_TAG` 均恢复为 `20260919T104537Z`，使用旧目录 Compose 执行 `up -d --no-build --no-deps backend frontend`，再回指旧 `current`。数据库结构和业务写入规则未变，无需回灌旧备份；旧版本、镜像及备份保留。

## 2026-09-19 班组期初库存、用途与收发时间画布发布

程序提交 `e7598a3` 已推送 GitHub，前后端配套发布 `20260919T104537Z`。班组长维护本班组转料用途，上序开单选择下序用途；管理员可授权尚无入账记录的班组提交一次期初库存，每行独立批次。班组“收发历史”改为按用途、来源批次分轨的时间画布，保留件数、重量、历史期初期末与节点详情，支持绘制动画、缩放、平移和全屏。全链路入口移到管理员“流转查询 → 全链路追踪”，前端路由及 API 均限制管理员；班组账号回到本班组历史。

发布前后端全量 358 项通过；前端并发首跑有 6 项超时，降低为单 worker 顺序运行后 67 文件 / 492 项全部通过，没有放宽测试断言或超时。TypeScript 和生产构建通过；独立 Chrome 的前期视觉与交互验收见 `design-qa.md`。本次部署没有使用浏览器，依赖不变，沿用此前已验证镜像依赖层，后端 `pip check` 通过。

上线前将现有数据库副本恢复到同服务器的隔离 MySQL 8.4 容器，不开放主机端口。验证 `0014 → 0015`、重复升级与新约束、索引、外键；24 张原有业务表的全部旧字段、行数和内容摘要不变，新用途/审计/期初提交表为空，历史用途字段保持空值、期初授权默认关闭。首次隔离验收因辅助脚本模块搜索路径缺失停止，补充 `PYTHONPATH=/app` 后完整复验通过，正式服务未受影响；两轮副本备份与日志保留。临时数据库容器、临时数据和内部网络已清理。

正式停写备份：`/opt/heatsinkrep/backups/pre-20260919T104537Z/database.sql.gz`，SHA256 `d455000db145eb3574ff6973f082b1d20a9691e1e0110066aa946e74b204f22d`。同目录保存私密配置、前后逐表摘要和数据库容器标识。正式迁移后再次核对所有旧业务字段内容完全一致；仅新增结构、默认值与迁移版本，没有清库、补造用途或导入本地演示物料。MySQL 容器 ID 与启动时间未变。

开放前在实际 MySQL 对账全部八班组分类库存、来源批次、首页和机器人大屏；193 组班组流水号收发历史、25 个流水号全链路的件数和重量均与库存台账一致。开放后验证新用途/期初状态/收发历史/管理员追踪接口，128 项字段匹配及 24 项空资料查询、访问锁与登录保护。三路 SSE 首帧、真实登录会话提交通知和心跳通过；仅创建并注销验证会话，零物料业务写入。当时全厂在库 17,298 件，内部在途 1,648 件。

服务器侧公网 84 个构建文件、14 个页面入口与本地构建逐项一致，三容器 healthy，`publish.exit=0`，`current` 与前后端标签均已切换。本机公网健康检查通过；首次无查询参数的入口哈希不一致，使用带本次版本参数且要求不缓存的请求重新检查入口与两份新增画布脚本，不将首次响应作为发布验收依据。

初始源码及构建包 SHA256 `7a274f5064971b131e695838efa4fe8de25817b1106feaca4d682cc8cb1a900f`，不含密钥、私密配置或数据库；部署辅助脚本随后修正并单独上传，最终摘要见版本目录 `deployment-scripts.sha256`。后端镜像短 ID `ebf94c383dcf`，前端 `16fc2f0de354`。发布、迁移、对账脚本和日志均保留在该版本目录。本节为发布后的文档补记，不修改已验证应用产物。

回退注意：新版可能产生用途快照和期初入账，旧程序不认识这些业务。仅在尚未开放、没有新版业务写入时，可使用旧目录 Compose 加本次 `rollback-compose.yml` 跳过旧 Alembic，恢复 `20260918T064256Z` 应用并保留新增字段。已开放后不得直接自动降级；先停写备份、评估数据兼容性，优先前向修复，禁止回灌旧备份覆盖新业务。旧镜像、源码和备份保留。

## 2026-09-18 八班组分类库存统一发布

程序提交 `a9954f3` 已推送 GitHub，前后端发布 `20260918T064256Z`。八班组共用分类库存明细，按流水号、材质、规格、物料类型和来源分行；普通班组显示上序班组，库房保留外部来料/退回/车间来源。原始批次在二级明细，行内出库限定所选来源分组。保留自选列、字段搜索、日期、加急、分析条件及实时推送；不新增业务页、不改出库扣减及权限规则。

后端 332 项、前端 430 项全量测试和后续库存组件 11 项测试通过；发布前 TypeScript/生产构建再次通过。前一轮已完成独立 Chrome 的库房/普通班组、2K 和窄屏检查；部署过程不使用浏览器。依赖未变，镜像沿用上一版依赖层，后端 `pip check` 通过。

发布前在新镜像中只读核对线上 MySQL 的全部八班组：分类行数为 29、24、28、28、28、28、28、28；各组来源批次件数/重量与该分类余额、班组合计及大屏完全一致。切换后再核对共享分类库存接口及来源详情、128 项字段匹配与 24 项空资料查询。全厂在库 17,298 件，内部在途 1,648 件；没有写入测试物料。

停写备份：`/opt/heatsinkrep/backups/pre-20260918T064256Z/database.sql.gz`，SHA256 `5752b53efb84f97194c656792c374adc0c4235f34c5f87cc61128e2525484e5c`。同目录保存私密配置、前后逐表摘要及数据库容器标识；包括迁移版本在内的所有表行数和内容哈希在应用切换前后完全相同，MySQL 容器 ID/启动时间不变。本次没有新增迁移，不清库、不改号、不导入演示数据。

82 个公网构建文件、12 个页面入口与本地一致；三路 SSE 首帧、真实登录事务通知、心跳、423/401 访问保护通过。只创建并注销验证会话，不修改物料业务。三容器 healthy、`publish.exit=0`，`current` 与前后端镜像标签已切换，本机公网健康检查及入口哈希匹配。

发布包 SHA256 `929e761367794fbb5357805997f91f35d8deb707cbabd55cc30dd28607c3551a`，不含密钥、私密配置或数据库；后端镜像短 ID `c2018f764912`，前端 `b2c34f848b17`。发布和验收脚本、日志保留在版本目录。此节为发布后的文档补记，不修改已验证程序产物。

回退应用时，将 `RELEASE_TAG` 和 `FRONTEND_RELEASE_TAG` 均恢复 `20260918T045500Z`，使用旧目录 Compose 执行 `up -d --no-build --no-deps backend frontend`，再回指旧 `current`。数据库结构与写入规则没有变化，不需恢复旧备份；禁止回灌覆盖上线后的业务记录。旧镜像、源码与备份保留。

## 2026-09-18 独立批次、合并打印与库房分组明细发布

程序提交 `6fdf069` 已推送 GitHub，前后端发布 `20260918T045500Z`。每条物料独立批次号与 Code 128，不再新建 CK 整单号；分别接收、编辑、作废，可合并打印。历史编号及来源关系保留，旧 CK 仅兼容查找。库房按流水号、类型、材质规格和来源分组，出入库记录支持废屑等类型与日期筛选；仅有重量和已经出完的记录仍可查。所有出库提交即扣库存，确认不再重复扣减，首页与机器人大屏统一口径。

后端全量 323 项、前端全量 440 项通过，后续库房/独立批次 17 项及历史抽屉 7 项复验、生产构建通过。独立 Chrome 核对出库列表、详情及两批同纸打印后已关闭，没有使用内置浏览器。隔离 MySQL 8.4.11 验证重复迁移、旧数据保留、并发扣库、重复提交、独立接收/修改和来源分组；测试数据仅在无网络端口的临时容器内，容器已清理。修正 MySQL 严格分组查询，不修改数据库严格模式。

停写备份：`/opt/heatsinkrep/backups/pre-20260918T045500Z/database.sql.gz`，SHA256 `996233b4eaf48d8fc661141934bebcf6deb20aec3c1416788e9b32de2ddce30e`。同目录保留私密配置、前后逐表摘要和数据库容器标识。迁移仅使技术提交记录的编号字段可空；除 Alembic 版本外，所有原有表的行数及内容哈希完全一致，MySQL 容器 ID/启动时间不变。不清库、不改号、不导入本地测试物料。

上线前只读验收迁移、八班组余额和库房来源分组。上线后 82 个构建文件及 12 个路由与本地一致；八班组 128 项字段匹配与 24 项空资料查询通过；六种具体材质的件数/重量、待接收明细、库存首页和机器人大屏与 MySQL 一致。此时全厂在库 17,298 件、内部在途 1,648 件。三路 SSE 首帧、真实登录事务通知、心跳与访问锁/身份验证通过，仅创建并注销验证会话，零物料写入。

三容器 healthy，发布脚本成功退出；本机公网 `/api/health` 返回服务/数据库正常。源码及构建包 SHA256 `d8f4b27c0be7df44dbd4191a7fef387c7e7fcc3e735d4dd8f91b0f9e2cf21dde`，不含密钥、私密配置或数据库。后端镜像短 ID `f1306627bd36`，前端 `415f20ecd921`。发布脚本、验收脚本与日志在版本目录保留；本节是部署后的文档补记。

回退注意：旧程序不认识 `0014`，且可能不能处理新建的无 CK 提交记录。仅在未重新开放、尚无新版业务写入时，可用旧目录 Compose 加本次 `rollback-compose.yml` 跳过旧 Alembic，恢复 `20260917T094309Z` 应用并保留新字段。开放后不自动回退：先停写评估兼容性，优先前向修复；禁止回灌旧备份覆盖新业务记录。

## 2026-09-17 机器人大屏转料滚动与材质汇总发布

前后端已配套发布 `20260917T094309Z`，程序提交 `68cfb0e` 已推送 GitHub。机器人及背景保留，两侧八班组显示各自待接收转料、连续匀速滚动；顶部按具体材质/牌号每2秒切换一组，全厂在库、内部在途、材质及每条流转信息均显示件数与重量。仍用 SSE 实时推送，暂停轮播不暂停接收业务更新。旧统计大屏及业务页面未删除。

本地前端全量55文件438项、后端相关50项、TypeScript及生产构建通过，独立Chrome的2K与390px检查见根目录 `design-qa.md`。此次发布未启动浏览器。依赖与数据库迁移未变，沿用上一版镜像依赖层、更新完整应用源码及前端构建，后端 `pip check` 通过。

停写备份为 `/opt/heatsinkrep/backups/pre-20260917T094309Z/database.sql.gz`，SHA256 为 `c12541f3f44816adc544c6b91f7a9ec7b7d703b2fd50af0db6af2d54e6d542a6`。同目录保存私密配置、前后逐表摘要及数据库容器对照；包括迁移版本在内的所有表在应用切换前后完全一致，MySQL 容器 ID 和启动时间不变。未清库、导入本地演示数据或修改物料业务，验收仅创建并注销登录会话。

三容器 healthy，`publish.exit=0`，`current` 和前后端标签均已切换。线上 MySQL 核对6种材质、8个班组共184条待接收流水号记录，各条件数/重量及材质汇总均一致；在库17,334件 / 1,733.4kg，内部在途1,648件 / 164.8kg。三路 SSE 首帧、事务提交通知、心跳、423/401访问保护通过；八班组128项匹配查询、24项空资料查询及分页检查通过。

服务器侧公网82个构建文件及12个页面入口与本地构建逐项一致，本机公网健康接口正常、大屏入口哈希一致。发布包 SHA256 为 `25df39a96c062c78710c98d49f22f32c6aaf2820136522e040b738640a120120`，不含私密配置、密钥或数据库。后端镜像短 ID `29ab4fbd683c`，前端 `778934bdb282`。发布、只读验收脚本与日志留在版本目录。本节为部署后的文档补记。

回退时将 `RELEASE_TAG` 与 `FRONTEND_RELEASE_TAG` 均恢复为 `20260916T055126Z`，使用旧目录 Compose 执行 `up -d --no-build --no-deps backend frontend`，再回指旧 `current`。没有数据库迁移，不需回灌数据；旧镜像、源码和备份均保留。

## 2026-09-16 库房来源、物料性质与废料分账发布

前后端已配套发布 `20260916T055126Z`，程序提交 `405e07c` 已推送 GitHub。库房统一显示外部来料、外部退回与车间转入；物料性质按批次明细保存，正常可用库存排除废料，废料余量单独计量。包括同来源拆分物料、废料去向限制、库房退回核对及来源信息打印。功能说明见 [库房来源与物料性质](warehouse-classification.md)。

本地后端 282 项、前端 430 项测试通过，生产构建通过；前一轮已完成独立 Chrome 页面检查。本次部署不使用浏览器。依赖未变化，镜像沿用上一版依赖并更新应用源码及完整前端构建，后端 `pip check` 通过。

发布前从线上创建一致性副本，在无外部网络、无主机端口的临时 MySQL 8.4 容器中验证 `0012 → 0013`、重复升级及旧字段逐表内容摘要相同；验证容器与临时数据已清理，副本备份保留于 `/opt/heatsinkrep/backups/check-20260916T055126Z/`。正式迁移只添加四个可空字段，不清库、不导入本地测试数据、不猜测历史来源。

正式停写备份：`/opt/heatsinkrep/backups/pre-20260916T055126Z/database.sql.gz`，SHA256 为 `cd4e7564551f5e1e6a16418b13ace30550c609b24adc45275171e6af2df8e097`。同目录保存配置副本、前后逐表摘要与数据库容器记录。除迁移版本外，所有原有字段和行完全一致，新增字段均为空；MySQL 容器 ID 与启动时间不变。验证仅创建并注销登录会话，不写入物料业务。

三容器 healthy，`publish.exit=0`，`current` 和前后端镜像标签已切换。线上验证通过库房来源筛选、新来源字段、性质汇总与废料筛选；八班组 128 项匹配查询及 24 项空资料查询通过。首页与机器人在库 17,334 件、内部在途 1,648 件一致，三路 SSE 首帧、事务通知、心跳及 423/401 访问保护通过。服务器公网 84 个构建文件及 12 个路由校验一致，本机公网健康接口正常、入口哈希匹配。

初始源码包 SHA256 为 `74fd52da602f6fa65cc1fd55f8161e5de0cbd094c3e651d086886b246566ed85`；验收脚本随后增加了来源/性质检查。后端镜像短 ID `3988ece209f8`，前端 `472efdb18125`。包中无私密配置、密钥或数据库；发布脚本、验收脚本和日志保存在版本目录。本节为部署后的文档补记。

回退时将前后端标签均恢复 `20260916T040657Z`，使用旧目录的 Compose 配合本次目录的 `rollback-compose.yml` 执行 `up -d --no-build --no-deps backend frontend`，再回指旧 `current`。覆盖文件跳过旧 Alembic，避免旧版本不认识 `0013`；保留新增字段，不降级删除、不自动回灌数据库。若新版已产生废料分类或退回核对业务，旧版不具备对应限制，应先停写评估，不可直接恢复旧版业务操作。

## 2026-09-16 库存显示列与按字段搜索发布

前后端已切换至 `20260916T040657Z`，程序提交 `db89aef` 已推送 GitHub。八班组库存明细新增显示列选择、排序与当前浏览器按账号/班组保存；17 个一级字段支持文字、数值、日期或状态查询，指定搜索字段临时置于第一列，清空后恢复原显示设置。批次等二级记录仍留在详情，默认十行分页与实时更新保留。

前端全量 421 项、后端 277 项通过，最终前端专项 15 项、TypeScript 与生产构建通过。前一轮已用独立 Chrome 检查实际页面；本次发布不使用浏览器。依赖和数据库迁移没有变化，镜像基于上次已验证镜像更新应用文件及已构建前端，后端依赖检查通过。

完整停写备份位于 `/opt/heatsinkrep/backups/pre-20260916T040657Z-retry/database.sql.gz`，SHA256：`0ae49ec44f49f3e0d892c004fc3b4ba1ffeb34296176edd75cee9e0ac699a2fb`。同目录有私密配置、前后逐表行数/内容摘要及数据库容器记录；包括迁移版本在内的所有表在切换前后一致，MySQL 容器 ID 和启动时间不变。没有清库、导数或修改物料业务。

首次发布验收误把空资料字段也算作必须命中的查询数量，实际 112 项匹配检查通过后触发了自动回退。修正验收脚本，为 24 项空资料字段增加有效查询检查后重发；程序代码和镜像没有因此修改。首次备份 `pre-20260916T040657Z`、`publish.first-attempt.log/exit` 保留，没有回灌数据库。

最终三容器 healthy，`publish.exit=0`，`current` 及前后端标签均已切换。线上 MySQL 完成八班组 112 项匹配查询与 24 项空资料查询，完整余额一致；三路 SSE 首帧、事务通知、心跳和 423/401 访问保护通过。在库 17,334 件、内部在途 1,648 件，验证仅创建并注销登录会话。服务器侧公网核对 82 个构建文件和 12 个路由全部一致。本机入口首次哈希曾不匹配，随后重新获取的入口与本地构建一致，健康接口返回正常；不将首次响应作为验收依据。

初始源码包 SHA256：`536a736cd829fc2a719dfdc4ee1ef6acc042f5dce3affc6296707e041da339fd`；验收和重试脚本随后单独更新于版本目录。后端镜像短 ID `9f66fcf6221d`，前端 `ec88411d89b6`。包中无私密配置、密钥或数据库。此发布记录为部署后的文档补记。

回退应用时，将 `RELEASE_TAG` 和 `FRONTEND_RELEASE_TAG` 均恢复为 `20260916T015209Z`，使用该旧目录的 `docker-compose.server.yml` 执行 `up -d --no-build --no-deps backend frontend`，再将 `current` 回指旧目录。数据库版本兼容，无需覆盖启动命令；不要回灌数据库。旧源码、镜像与两次备份保留。

## 2026-09-16 库存页面合并与主系统配置发布

前后端均切换至 `20260916T015209Z`，程序源码为 Git 提交 `8e8a20b`。流水号台账与库存明细合并为一个库存页面，每行一个流水号，来源批次与收发、丢失明细收进详情；保留有权限班组的单批、批量出库和丢失登记。旧台账链接兼容跳转，首页、两种大屏和实时推送保留。

主系统配置页及查询、快照入库后端随本次配套发布，当前仍停用：没有主系统地址、令牌或访问白名单。仅生成并私密保存配置加密密钥；真实主系统联调、入库表单接入和全字段纸质打印尚未完成，不能视为主系统已接通。

首次隔离 MySQL 检查发现单行配置表的固定主键约束与自增冲突，修正模型和未上线迁移后重新构建。最终后端 262 项、前端 409 项通过，前端生产构建与后端依赖检查通过。隔离 MySQL 8.4 从旧版本升级至 `20260915_0012` 及重复升级均通过，临时验证容器已移除。部署过程未使用浏览器。

停写后完整备份位于 `/opt/heatsinkrep/backups/pre-20260916T015209Z/database.sql.gz`；同目录保存原私密配置 `config.env`、含新加密密钥的 `config-with-key.env` 和前后逐表摘要。除迁移版本记录外，所有原有表行数与内容哈希完全一致，仅新增空表 `main_system_configuration`。MySQL 容器 ID 和启动时间未变，未清库、未导入测试数据。验证只创建并注销两个登录会话，未写物料业务。

数据库备份 SHA256：`773f7ad0ad28dcd03e87881d95b4c0d114c234fde2f7f5e097688a3057b52cb0`。

三容器均 healthy。八班组库存接口与机器人大屏一致，在库 17,334 件、内部在途 1,648 件；默认十行分页、可用库存筛选通过。三路 SSE 首帧、事务提交通知、心跳及 423/401 访问保护通过。服务器经公网地址校验全部 82 个构建文件和 12 个单页路由，与发布构建一致。本机外网检查在切换期曾连接失败，随后部分 GET 被本地网络重定向至 `1.1.1.3/proxytool/remind.htm`，不能将该响应当成应用文件；完整资源验收采用服务器侧公网地址，未声称本机所有网络路径均通过。

源码包 `source-final.tar.gz` SHA256：`ebb5b6f971dae2685d072f867de7fb580de44377761fb24a9779a18a93080e5b`。前端镜像短 ID `da9b53b43d4d`，后端镜像短 ID `6dae036d4864`。脚本和 `publish.log` 留在发布目录，`publish.exit` 为 `0`；`current` 与 `.env` 的前后端标签均已切换。本节是发布后的文档补记，不修改已验证程序产物。

若需回退应用，保留数据库与新加密密钥备份，将后端标签恢复为 `20260914T152856Z`、前端标签恢复为 `20260914T165812Z`，用旧发布的 `docker-compose.server.yml` 配合新目录 `rollback-compose.yml` 执行 `up -d --no-build --no-deps backend frontend`，并将 `current` 回指旧前端目录。覆盖文件让旧后端直接启动服务，避免旧 Alembic 不认识已升级的 `0012`；不要对新表执行降级或自动回灌数据库。旧源码和镜像均保留。

## 2026-09-15 全站表格对齐与实时同步发布

前端 `20260914T165812Z` 已发布表格字号、表头、行距、内边距及居中对齐，备注和原因保留左对齐；管理页操作按钮同排显示。全局转料、追踪、管理列表和只读详情接入已有轻量变更推送，保留筛选、页码、编辑草稿及打印核对保护。首页玻璃卡片、机器人和分析大屏、纸质打印样式不变。

本地全量前端 402 项通过，最后样式修正后专项 31 项通过，TypeScript 与生产构建通过。独立 Chrome 完成业务表格、详情、2K 与窄屏检查并关闭。部署过程未使用浏览器。

本次只更新前端镜像 `heatsinkrep-frontend:20260914T165812Z`（短 ID `df9876e391a3`）；后端和 MySQL 的容器 ID、启动时间切换前后完全一致。无迁移、清库或物料业务写入。完整备份位于 `/opt/heatsinkrep/backups/pre-20260914T165812Z/database.sql.gz`，同目录有私密配置和容器对照记录。

公网 74 个构建文件哈希、11 个路由入口与本地构建一致，三容器 healthy。三路 SSE 首帧、事务通知、心跳、423/401 鉴权通过；八班组与机器人大屏接口一致，在库 17,334 件、内部在途 1,648 件。检查仅创建并注销两个登录会话，未改动库存。

源码包 SHA256：`a1dc0cfbaf84c2b8ab3be689c07ac3de397c84102c8b8e47c0fbb69219a0944c`。解包时忽略 macOS 扩展属性，不影响内容；包中无私钥、数据库或私密 `.env`。发布脚本、验证脚本和日志保存在对应 releases 目录。本段及关联说明为部署后的文档补记。

回退只需恢复 `FRONTEND_RELEASE_TAG=20260914T152856Z`，使用旧源码 Compose 执行 `up -d --no-build --no-deps frontend`，并将 `current` 回指 `/opt/heatsinkrep/releases/20260914T152856Z`；`RELEASE_TAG` 不变，不回灌数据库。旧源码及镜像保留。

## 2026-09-14 班组台账与机器人大屏实时推送发布

`20260914T152856Z` 已配套发布前后端，首页原有推送、白紫样式和两种大屏保留。新增机器人大屏完整快照与班组工作台轻量变更通知；后台更新保留当前筛选、页码及未提交表单。跨日更新、断线重连与暂停播报规则见 [台账及大屏实时推送](workspace-realtime.md)。全厂分析和全局转料查询仍使用原更新方式。

最终回归：后端 193 项、前端 389 项，TypeScript 与生产构建通过。生产 Nginx 三路 SSE 的首帧、事务通知、心跳及 423/401 鉴权检查通过；八班组库存接口一致，十行分页通过。验收时在库 17,334 件、内部在途 1,648 件。只创建并注销两个验证登录会话，未进行物料业务写入；未使用浏览器。

切换完成后，公网全部 75 个构建文件的内容哈希、11 个单页应用路由入口与本地生产构建一致，三容器均 healthy。切换过程中的第一次资源检查曾命中旧入口，服务就绪后完整复查通过。

切换时仅停止并重建应用容器，MySQL 容器 ID 和启动时间未变。完整备份：`/opt/heatsinkrep/backups/pre-20260914T152856Z/database.sql.gz`；同目录保存私密配置、数据库容器记录及切换前后逐表行数/内容摘要，摘要完全一致。数据库迁移版本不变，不清库、不导入测试数据。

数据库备份 SHA256：`666730925f582dbb8fce56c5d70baf8533f83d6b7bf068e4be77a54058acbe56`。

源码包 SHA256：`69ffe1028f4c1817f423e09bc3c676e98ff2ff4b26d0c74153d40508a683e773`。发布脚本、检查脚本及日志保存在该版本目录；包中无私钥、数据库或私密 `.env`。本节及使用说明是发布后的文档补记，不修改已验证程序产物。

回退应用时，将 `RELEASE_TAG` 恢复为 `20260914T092908Z`，`FRONTEND_RELEASE_TAG` 恢复为 `20260914T123620Z`，使用旧源码的 Compose 执行 `up -d --no-build --no-deps backend frontend`，并将 `current` 回指 `/opt/heatsinkrep/releases/20260914T123620Z`。旧源码与镜像保留，不回灌数据库。

## 2026-09-14 浅白紫与全项目一致性发布

`20260914T123620Z` 已发布用户确认的浅白紫首页和普通业务页共用样式：导航宽度与图标对齐、中文字体、表头、页签、登录页和流水号详情阅读字号；详情默认每页 10 条。首页玻璃卡片、八班组设备、SSE 和两种大屏保留。

本次仅更新前端镜像 `heatsinkrep-frontend:20260914T123620Z`（短 ID `3e59ceab3774`）。后端和 MySQL 的容器 ID、启动时间切换前后完全一致，没有迁移、导数、清库或业务写入。`FRONTEND_RELEASE_TAG` 和 `current` 已切换，`RELEASE_TAG` 仍为 `20260914T092908Z`。

本地审查结果见 `ui-consistency-review-20260914.md`；部署前 TypeScript/生产构建再次通过。共享中文字体 URL 添加内容版本 `?v=c14d9651f390`，避免原 7 天静态缓存继续命中旧子集。公网 11 个路由的入口 HTML、全部本次构建 JS/CSS、白紫背景、设备图、字体及机器人模型/背景均与本地构建逐项哈希一致。

三容器 healthy；访问锁、账号鉴权、Nginx SSE 首帧和心跳检查通过。八班组在库 17,334 件、在途 1,648 件，首帧约 218ms（不是变更推送 SLA）。认证检查只创建并注销一个登录会话，不修改库存。此次未使用任何浏览器。

配置备份与后端/数据库容器对比记录：`/opt/heatsinkrep/backups/pre-20260914T123620Z/`；发布日志在新版本目录的 `publish.log`。旧前端 `20260914T102337Z` 和旧源码保留，前端回退只需恢复对应标签并以 `up -d --no-build --no-deps frontend` 重建前端、回指旧 `current`，不回灌数据库。

源码包 SHA256：`b9e1dfa7765c7ef546d42f603448ec2603b742ec4d47d023128757300c2b8a6c`。服务器解包有忽略 macOS 扩展属性的提示，不影响文件内容，包中没有私钥、数据库或私密 `.env`。

按用户追加要求，从此服务器直连 `101.6.160.125:9997`：TCP 成功（约 9ms），HTTP HEAD `/` 返回 200、`text/html`，Server 为 `nginx/1.30.4`。仅证明网络端口与 HTTP 入口可达，未登录或验证目标业务。

## 2026-09-14 确认效果图首页发布

`20260914T102337Z` 已将用户再次确认的 `exec-3935fd40` 首页效果图落地：八种独立立体设备、
浅色蓝紫波纹、玻璃卡片、对应字号和四列两排布局；数字来自既有库存接口与 SSE，不写死效果图数字。
实际截图、同尺寸对照和手机/2K检查见根目录 `design-qa.md` 当前段落。

仅重建前端容器，后端及 MySQL 的容器 ID 和启动时间切换前后完全一致；没有迁移、导数或库存写入。
新前端镜像 `heatsinkrep-frontend:20260914T102337Z`，镜像短 ID `5df27b2c6a61`。
`RELEASE_TAG` 仍为 `20260914T092908Z`，`FRONTEND_RELEASE_TAG` 为新标签。
三容器 healthy，公网 HTML、入口脚本、首页脚本/样式、全部新设备图、字体和原机器人模型/背景逐一哈希一致。

通过前端 Nginx 再验 SSE：八班组、在库 17,334、在途 1,648，首帧约 230ms，心跳与鉴权均通过；
首帧时间不是变更推送 SLA。该检查只建立并注销一个认证会话，不修改业务数据。

配置回退副本及容器不变记录：`/opt/heatsinkrep/backups/pre-20260914T102337Z/`。
旧源码和前端镜像保留；若只回退此次外观，将 `FRONTEND_RELEASE_TAG` 改回 `20260914T092908Z`，
用旧目录 Compose 执行 `up -d --no-build --no-deps frontend` 并回指 `current`；不回灌数据库。
本次源码包 SHA256：`7fd35af713884c2101d6efc2bfb47912a95fcc041acb27638941336494dae3bc`。

## 常用命令

先切换到当前源码目录：

```bash
cd /opt/heatsinkrep/current
```

查看状态：

```bash
sudo /opt/heatsinkrep/bin/docker-compose --env-file /opt/heatsinkrep/.env -p heatsinkrep -f docker-compose.server.yml ps
```

启动或应用配置变更：

```bash
sudo /opt/heatsinkrep/bin/docker-compose --env-file /opt/heatsinkrep/.env -p heatsinkrep -f docker-compose.server.yml up -d
```

查看后端日志：

```bash
sudo /opt/heatsinkrep/bin/docker-compose --env-file /opt/heatsinkrep/.env -p heatsinkrep -f docker-compose.server.yml logs --tail=100 backend
```

首次配置本期正式八班组（先完成数据库备份与当前 `20260907_0010` 迁移）：

```bash
sudo /opt/heatsinkrep/bin/docker-compose --env-file /opt/heatsinkrep/.env -p heatsinkrep -f docker-compose.server.yml exec backend python -m app.configure_material_teams
```

该脚本只补缺少的正式班组，并执行已确认的“扎板 → 轧制”更名；不创建班组长、不更改旧账号绑定、不删除旧组。遇到目录冲突会退出，须先核实冲突数据。

一期演示验收工具为 `scripts/verify_phase1_server.py`，依赖显式演示流水号 `DEMO-DIRECT-FLOW-001`。只在隔离的演示环境使用；访问口令、管理员密码、所选演示班组长共同密码分别从 `HEATSINK_ACCESS_PASSWORD`、`HEATSINK_ADMIN_PASSWORD`、`HEATSINK_TEAM_PASSWORD` 环境变量读取，不要写入命令历史或日志。示例地址应替换为隔离环境的回环入口：

```bash
python3 scripts/verify_phase1_server.py --base-url http://127.0.0.1:8080
```

脚本检查访问锁、账号认证、转料单及权限拒绝。它会创建并注销测试登录会话，也会发送预期被拒绝的写请求，因此不是纯 GET 巡检工具；不要直接当作真实生产环境的无副作用检查。旧 `verify_server.py` 工单验收脚本已删除，通用 HTTP 客户端保留在 `scripts/smoke_client.py`。

只有明确需要演示数据的隔离环境才运行 `python -m app.seed_material_transfer_showcase`。脚本复用已有班组长，不创建账号、不设置密码；具体条件见项目 README。不要把演示数据初始化命令放进生产启动步骤。

## 配置与发布注意

首页实时推送版本需要配套发布前后端，保留单个后端容器和 `--workers 1`，否则进程内提交事件不能跨 worker 传递。Nginx 必须关闭 SSE 缓冲；如增加上层代理也需保持流式转发。此功能已随 `20260914T092908Z` 上线，详见 [首页库存实时推送](inventory-realtime.md)。

数据库、管理员初始密码及签名密钥均使用部署时新生成的随机值；不要沿用开发默认数据库密码。管理员初始密码只在尚无有效管理员时使用，修改环境变量不会重置已经存在的管理员密码。

发布时先将源码放入新的 releases 子目录，设置对应 `RELEASE_TAG`，构建并验证后再切换 current。保留旧源码与镜像，数据库升级前做备份。不要使用 `down -v`，该操作会删除 MySQL 数据卷。回退镜像不等于回退数据库，数据库迁移需单独确认兼容性。

macOS 打包源码时设置 `COPYFILE_DISABLE=1` 并排除 `._*`、`.DS_Store`，避免 AppleDouble 附带文件进入 Linux 发布目录。前后端 `.dockerignore` 也排除这些文件，防止 Alembic 将 `._*.py` 当成迁移导入。

前端单独发布时可设置 `FRONTEND_RELEASE_TAG`，其优先于 `RELEASE_TAG`；后端仍使用 `RELEASE_TAG`。完整前后端升级时要同时更新两者，或删除前端单独覆盖，避免继续使用旧前端。应用前端更新使用 `up -d --no-build --no-deps frontend`，不重启后端/数据库。

旧演示配置与账号说明属于历史快照，见 [历史演示数据说明](history/demo-data.md)，不代表当前正式八班组设置。2026-09-12 已按用户截图清理五个 DEMO 班组及绑定旧演示账号；后续处置其他账号、导入期初库存或改绑班组，仍须单独确认范围。

旧业务接口已删除，库房手工入库对应 `20260906_0008`，库房对外出库与检验发货对应 `20260907_0009`，整批一码和原子确认新增 `20260907_0010`；既有迁移文件没有改写。发布时须使用配套前后端并先备份、升级数据库，不能只替换前端。0010 为原 CK 头添加确认元数据，不更改历史 CK/TL 或来源记录。手工入库与出库不等待公司主系统配置，也不自动导入真实或期初库存。对外确认是独立状态，不要让旧版前端将其解释为内部接收。以上功能已随 `20260906T184154Z` 发布。

手工入库迁移在 MySQL 上需要修改来源列的可空性、添加约束和索引，不承诺无锁；应在发布窗口验证并安排升级。自建 SQLite 验证连接需在迁移事务开始前关闭外键强制检查，迁移后执行 `foreign_key_check`；不要在事务中临时关闭或删除引用记录来绕过检查。标准 Alembic SQLite 连接沿用既有配置。

历史部署版本、备份和验收记录见 [部署历史](history/deployment-history.md)。2026-09-12 的 `20260912T132133Z` 程序发布已完成生产构建、依赖检查、线上 MySQL 只读查询、逐表数据不变校验和公网资源校验，当时未执行业务写入或浏览器测试。该次备份位于 `/opt/heatsinkrep/backups/pre-20260912T132133Z/`，旧源码与镜像 `20260906T184154Z` 保留。

程序发布时没有导入演示数据。随后用户明确要求补充数据，已于 2026-09-12 晚执行增量导入：新增 433 条物料记录、376 张出库单、192 条丢失记录，以及八个正式班组各自的演示账号。导入后、清理前共有 14 个账号、15 条班组配置（包含旧演示配置）、435 条物料记录；导入过程未改变原来 6 个账号的密码、绑定或原有业务记录。

八班组分别有 24～25 个流水号、23 条待接收明细；库存、流水号、出库、丢失列表均已验证默认加载 20 条。新增演示账号见 [演示数据说明](compact-workspace-refresh.md)；口令与本地演示账号一致，不与管理员口令混用。此次为脚本通过实际业务流程生成的数据，不是用 SQLite 覆盖线上 MySQL。

数据导入前的新备份为 `/opt/heatsinkrep/backups/pre-showcase-20260912T133253Z/database.sql.gz`。逐条核对原有数据未改变；条码计数器只正常递增。演示数据以 `TEST-20260911-` 流水号及“测试数据”备注标记，重复执行导入会跳过已完成的数据集，不覆盖用户后续操作。

此前 `20260912T142714Z` 发布只更新前端，并按用户截图清理 `DEMO-CUT`、`DEMO-CNC`、`DEMO-SURFACE`、`DEMO-QC`、`DEMO-PACK`，同时删除其 5 个旧演示账号、24 个旧登录会话、2 条 `DEMO-DIRECT-FLOW-001` 转料和 15 条关联旧工艺模板。当前为 9 个账号、10 条班组配置、433 条物料记录、376 张出库单、192 条丢失记录。八个现用班组及账号逐条校验未改变；14 张旧工单历史保留，其已删除身份外键按原有规则置空。截图之外的 `FACTORY-SHIP`、`FACTORY-SCRAP` 未删除；它们不在当前八班组工作台中。

此次清理前完整备份为 `/opt/heatsinkrep/backups/pre-live-cleanup-20260912T142714Z/database.sql.gz`，同目录有配置副本、SHA256 和清理结果。清理在单个事务中完成，不关闭外键检查，不重建数据库；本次后端、MySQL 未重启。314 项前端测试及生产构建通过；独立 Chrome 已验证 2K、笔记本与手机布局，以及一键全屏和返回操作，检查后完整关闭，未使用 Codex 内置浏览器。仅回退页面可恢复旧前端标签；恢复被删数据需从此次 SQL 备份审慎提取，不能用整库覆盖随后新增的业务数据。

此前 `20260912T145538Z` 按用户修正统一内部转料扣库口径：提交即扣上序库存，未接收量单列为内部在途，下序确认只增加下序库存；改单、作废和重复确认均有回归覆盖。班组汇总、材质/流水号图表、全厂总览及机器人大屏一致，内部转出趋势按提交时间统计。库房对外出库和检验发货仍保持本班确认规则。

本次不修改存储的业务记录、不迁移数据库，仅从既有单据计算新余额。停写备份为 `/opt/heatsinkrep/backups/pre-transit-20260912T145538Z/database.sql.gz`；旧、新镜像及启动后均逐表核验数据不变，线上 MySQL 汇总、流水号、图表和大屏查询一致。发布时在库 17,334 件 / 1,733.4 kg，内部在途 1,648 件 / 164.8 kg，合计仍为 18,982 件 / 1,898.2 kg。159 项后端、315 项前端测试及构建通过。独立 Chrome 检查后已关闭；程序回退应同时恢复旧前端 `20260912T142714Z` 与旧后端 `20260912T132133Z`，不要回灌数据库。

## 2026-09-13 配套发布

`20260912T195806Z` 已发布统一阅读样式、默认 10 行及整页滚动、日期筛选、流水号动态加急标记、转料详情与 A4 打印表格化。两套大屏和原工厂背景均保留，端口、访问口令和账号密码不变。

新增加急表与索引，数据库从 `20260907_0010` 升至 `20260912_0011`。发布前在隔离 MySQL 8.4 验证旧镜像建库后的升级、空库初始化和重复升级；修正 0011 与历史基线 `create_all` 重复建表的兼容问题，既有数据不覆盖，冲突索引会报错。隔离容器及其临时数据库已清理，正式 MySQL 容器未重启。

停写完整备份：`/opt/heatsinkrep/backups/pre-20260912T195806Z/database.sql.gz`，SHA256 为 `a6293334fc9a91f7aa381ee4c7920cf23eafd5af3d3a475d76c6c2388f471a29`；同目录保存私密配置副本、迁移前后及服务启动后的逐表摘要、查询检查结果。21 张原有数据表的行数与内容哈希完全一致，保留 9 个账号、10 条班组配置、433 条物料、376 张出库单、192 条丢失记录。两张加急表初始为空，不上传本地演示加急标记。

前后端及数据库均 healthy；公网首页、业务路由、构建资源、机器人模型和背景核对通过，访问锁仍拒绝未解锁的业务请求。发布日志位于当前版本的 `publish.log`。本次未使用任何浏览器自动化。

旧版 `20260912T145538Z` 源码、镜像和备份保留。0011 只增加表，旧程序可保留这些表运行，但旧 Alembic 不认识新修订号；紧急回退旧镜像时需使用新发布目录的 `rollback-compose.yml` 跳过旧迁移启动命令，并保留该覆盖配置直到重新发布。不得为回退界面直接回灌整库，也不得删除加急审计记录。


## 2026-09-26 全厂总览真实接口发布

`20260926T125922Z` 已替换首页，程序提交 `24d8331`。包含班组材质库存、成品率逐级下钻、按件的流水号发货折线、分批交付计划及重点关注。规则和管理入口见 [总览说明](factory-dashboard.md)。玻璃大屏保持原样，保留已发布侧栏层级样式。

仅增加 `serial_delivery_plans` 表，数据库升级到 `20260926_0019`。隔离 MySQL 验证从旧镜像升级及重复升级，原表行数和内容哈希不变，JSON 计划写入通过。首次上线因验收脚本缩进错误触发自动应用回退；修正并检查脚本后完成第二次发布。回退和重试未回灌数据库。

升级前完整备份在 `/opt/heatsinkrep/backups/pre-20260926T125922Z/`；重试前另备份至 `/opt/heatsinkrep/backups/pre-20260926T125922Z-retry1/`。均包含数据库压缩备份、校验和、私密配置副本及逐表摘要。原业务表完全一致；验收仅产生登录会话和通知记录，没有物料写入。数据库和 MQ 容器未重启。

线上前后端均 healthy；97 个资源逐文件哈希、15 个 SPA 入口、访问锁、管理员登录、所有新总览查询、16 组班组库存查询、SSE、MQ 连接及公网入口校验通过。当前线上流水号、库存和交付计划均为零，页面显示真实空状态；交期计划需由管理员录入。本次未导入本地 QA 数据。

旧前端 `20260926T124051Z`、旧后端 `20260926T093819Z` 和备份保留。回退需同时指定本发布目录的 `rollback-compose.yml` 跳过旧镜像不认识的新迁移修订号，并保留计划表。发布日志：`publish.log`；首次回退记录：`publish-attempt1.log`。
