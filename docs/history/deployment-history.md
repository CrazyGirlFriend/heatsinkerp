# 历史部署与验收记录

> 本文仅记录已发生的发布、备份和当时验收；不要把历史版本号当成当前发布指令。当前操作见 [服务器部署](../server-deployment.md)。

## 库存首页与实时推送发布（北京时间 2026-09-14）

- 用户明确要求部署当前版本，最终前后端与 `current` 均切换为 `20260914T092908Z`。入口仍为 `http://122.200.93.68:8082`，账号、口令与业务数据保留，未改其他项目及端口。
- 包含当前浅色玻璃库存首页、数字变化动画，以及替代 10 秒轮询的 SSE 实时推送；后续确认的新效果图尚未落地。原数据分析页、独立机器人大屏及机器人背景保留，不将它们的刷新机制宣称为此次已改造。
- 首次候选 `20260914T091235Z` 在上线 SSE 检查中发现 MySQL Decimal 无法由原始 JSON 编码，自动恢复旧版 `20260912T195806Z`。修复为与 REST 一致的编码方式，新增回归复现后通过；最终推送/总览/访问保护在锁定依赖环境中 52 项通过。前端沿用此前已通过 380 项测试及类型检查的生产产物。
- 依赖与迁移不变，数据库仍为 `20260912_0011`。服务器基于原已验证镜像离线构建，单个后端以 `--workers 1` 启动，Nginx 关闭 SSE 缓冲。正式 MySQL 容器 `f58ccf948953` 自 2026-09-03 起运行，本次未重启。
- 停写备份 `/opt/heatsinkrep/backups/pre-20260914T092908Z/database.sql.gz` 完整性通过，SHA256 `5620bcb106d01e5914de6e709474bb06d137029effa500c3c9a62d9a729f4510`。同目录保存私密配置、切换前后全表摘要。24 张表行数及内容哈希完全一致，433 条物料、376 张出库单、192 条丢失记录、9 个账号保留；HTTP 验证只创建并注销账号会话，不写入业务数据。
- 生产 Nginx SSE 首帧约 260ms，八班组及汇总与 REST 一致，在库 17,334 件、内部在途 1,648 件，心跳正常；访问锁与账号验证分别拒绝未授权请求。该耗时不是生产变化推送 SLA，未执行生产库存变更或容量压测。
- 公网首页、旧数据分析及机器人大屏 3 个路由、12 项 JS/CSS/字体/图片/模型资源均与本地产物一致，两层健康接口和访问锁检查通过。未使用任何浏览器。
- 后端镜像 `sha256:cafb0aa2a1ca82fa6783d3c0d62c358088941767a138ae0bc2c575d9b20bad28`；前端镜像 `sha256:7b2d1807b5025240aed6b806dd7306691ed8b7da4a21f5cd9a2e96c645783cab`。上传归档 SHA256 `d2da8f8f6570cc90a13917e6437639f7279825c33471056becf66a69684dbfef`，验收后同步本条文档，不改已上传归档。发布日志在版本目录 `publish.log`。
- 旧源码、镜像及两次尝试的备份保留。由于数据库版本未变，程序回退可同时恢复前后端 `20260912T195806Z` 和对应 `current`、发布标签，不应为回退程序覆盖数据库。

## 首次部署记录（2026-09-03）

- 初次源码：`/opt/heatsinkrep/releases/20260903T073402Z`，保留用于回退参考。
- 前端已在服务器构建。后端复用本机同架构的已验证依赖镜像，覆盖当前源码后上传，规避远端 PyPI 下载缓慢；直接依赖版本与 `backend/requirements.txt` 完全一致。具体版本与镜像校验记录保存在当前版本目录的 `pip-freeze.txt`、`image-verification.txt`。
- MySQL 初始迁移 `20260901_0001` 已执行，三个容器均健康；只发布网页 `8082`，不发布 API/MySQL 端口。服务器原有 Nginx、MinIO 未改动。
- 前端 53 项、后端 48 项代码测试通过；线上只读接口验收 10 项通过。实际工单为 2 张待开始、8 张执行中、2 张已完成。
- 独立 Chrome 实测错误口令被拒绝、正确口令后显示登录页、班组账号可查看完整 12 条流转记录。未使用 Codex 内置浏览器。
- 浏览器移除访问 Cookie 后，即使账号仍处于登录状态，刷新也会回到访问锁；重新输入正确口令可恢复原页面。浏览器验收发现并修正了较窄桌面窗口的扫码框/筛选项重叠，将既有分行布局断点调整为 1650px。
- 5 个班组账号均绑定正确班组，均可读取总表、均被禁止访问账号管理接口（HTTP 403）。`DEMO-WO-012` 六序完成、报工锁定、上下序关系、16 条有序时间线和两条分钟精度打印记录均通过只读检查；测试创建的账号会话已注销。
- 首份数据库备份：`/opt/heatsinkrep/backups/initial-20260903T073402Z.sql.gz`，权限 `600`，压缩完整性检查通过；这不是自动定时备份。

## 分批流转升级（北京时间 2026-09-04）

- 当前版本：`/opt/heatsinkrep/releases/20260903T162610Z`，`current` 已切换；前后端、MySQL 三个容器均健康，入口端口不变。
- 新增动态班组侧栏、班组工单产量、分批报工/接收、转废/丢料/其他异常、完整批次明细和整单数量守恒展示。
- 本地后端 104 项、前端 78 项测试通过，前端生产构建通过。独立 MySQL 8.4.11 验证旧版迁移、1:N 关系、幂等约束、中文与三位小数、空库升级和重复迁移；旧数据逐行保留。
- 上线前暂停本项目入口并生成备份：`/opt/heatsinkrep/backups/pre-batches-20260903T162610Z.sql.gz`，完整性检查通过，SHA256 为 `c39a0a5ca3beba18a075e7585e16a4ff8968b5ba4a19ec383d2f8ca709fb5be4`。同目录有私密配置副本和旧表数据核对快照，均权限 `600`。
- MySQL 迁移到 `20260903_0002` 后、导入新示例前，生产数据库全部 12 张旧业务表逐列逐行核对完全一致。
- 幂等导入 9 个工厂班组及 1 张分批示例；没有创建或重置账号密码。现有 13 张工单：待开始 2、执行中 9、已完成 2。
- 新版只读上线验收 13/13 通过，包括访问锁、管理员会话、未登录权限拒绝、班组排序、示例三笔报工/分次接收、原因追溯和 97 在制 + 2 转废 + 1 丢料 = 100 守恒。
- 独立 Chrome 实测公网纯锁入口、口令解锁、原有班组账号登录、13 条总表和扎板产量 98/已转 68/待转 30 均正常；普通班组账号不显示账号/班组管理菜单。浏览器仅有既有 favicon 404，无页面脚本运行错误。
- V1.1 用户手册（16 页）已同步当前版本，逐页中文和排版检查通过；服务器 DOCX SHA256：`74e4c2a3e1727b2e37dcc472057129e3ece4e98ff9820778e3afa2d260f46ae7`。
- 0002 不支持无损原地降级。若必须回退，需暂停网站，将上述 SQL 备份恢复到本项目数据库，并同时恢复旧配置/镜像；仅切换旧镜像不足以完成回退。

## 导航与列表前端更新（北京时间 2026-09-04）

- 当前源码：`/opt/heatsinkrep/releases/20260903T170310Z`；`current` 已切换，前端镜像同名标签。
- 后端保持 `20260903T162610Z`，容器 ID `bbe345d11d4a` 未变化；MySQL 容器 `f58ccf948953` 未变化。本次没有迁移数据库、导入数据或修改业务记录。
- 新导航按生产作业、班组产量、基础设置分区，工厂/演示班组分别折叠；新总表默认收起，固定两端列、显示整单数量并适配手机卡片。入口速查见 [新版导航](../navigation.md)。
- 前端 106 项测试通过，TypeScript 和生产构建通过；独立 Chrome 完成 1440/1280/390 像素布局及主要入口实测，视觉 QA 记录在项目根 `design-qa.md`。
- 上线只读验收 13/13 通过，13 张工单及 97 在制 + 2 转废 + 1 丢料示例数量保持原状，测试会话已撤销。三个服务均健康。
- 镜像 SHA256：`67e65b994f1ddd6bad82cc3e269c0a939dfe1a58908db8bd8458a48aedc8101b`；镜像传输文件 SHA256：`1bf4e737d559a5794db537054cbdff243c755b12b8fa613cc8df153938e6eca5`；源码传输文件 SHA256：`b347b74d166e2c4d382635ea57aac77799cd374457cad4eed68dc7191b48b2a0`。发布后补充本条记录，源码包校验值仅代表上传归档。
- 私密配置副本 `/opt/heatsinkrep/backups/pre-ui-20260903T170310Z.env`，权限 600。旧版本与镜像保留；本次回退只需恢复原前端标签，不需要恢复数据库。
- 独立 Chrome 公网班组账号复核通过：纯锁、解锁登录、13 张流转记录、“我的班组”、基础设置隐藏及扫码页内切换异常登记均正常；未提交业务数据。浏览器仅有既有 favicon 404，无应用脚本错误。

## 前端架构与视觉升级（北京时间 2026-09-05）

- 当前源码：`/opt/heatsinkrep/releases/20260904T184652Z`；`current` 已切换，前端镜像为 `heatsinkrep-frontend:20260904T184652Z`，镜像 ID `sha256:642551be8bae4608d46d287144ea96993987f2b0c5920460cb80b1009bd90b2c`。
- 后端继续使用 `20260903T162610Z`，容器 ID `bbe345d11d4a` 未变化；MySQL 容器 ID `f58ccf948953` 未变化。本次没有数据库迁移、数据导入或业务记录写入，13 张工单仍为待开始 2、执行中 9、已完成 2。
- 前端数据流已统一为 Vue 3、Element Plus 自定义工业主题、Pinia、Axios 和按需延迟加载的 ECharts；Code 128 继续由 JsBarcode 生成。架构边界见 [前端与系统架构](../frontend-architecture.md)。
- 班组作业新增工单产量结构、交接与异常两张图；图表明确区分“当前页柱图”和“筛选累计”，具备加载失败重试、读屏摘要、键盘滚动、窄屏布局和减少动画模式。状态标签、核心按钮、卡片和账户菜单使用统一主题，深橙主色满足小字号白字对比度要求。
- 流转总表保留一单一行和页内扫码弹窗，长工单号条码不再被强制压缩；班组录入在 320 像素宽度下改为单列表单。移动导航补充跳到正文、焦点恢复与背景隔离。
- 本地前端 19 个测试文件、137 项测试通过，TypeScript 与生产构建通过；后端 104 项测试通过。独立终端 Chrome 完成 1440、390 和 320 像素视觉及交互复核，未使用 Codex 内置浏览器。
- 新镜像先在项目专属 Docker 网络内完成 `/healthz`、首页和 `/api/health` 金丝雀检查，再单独替换前端容器；上线只读接口验收 13/13 通过。公网复核了纯锁入口、口令解锁、班组账号登录、13 张流转记录、Code 128 页内扫码弹窗、两张产量图和班组绑定录入，随后注销测试会话；未提交业务数据。
- 源码传输包 SHA256：`bb7f035cc6e1c637a631624686791cc8283d82c214ac0f7e55f649f9b7db0f5d`。UI 单独发布使用 `frontend/Dockerfile.prebuilt` 将本地已测试的 `dist` 覆盖到既有 Nginx 运行镜像，避免服务器重复下载 npm 依赖。
- 发布前私密配置副本：`/opt/heatsinkrep/backups/pre-frontend-20260904T184652Z.env`，权限 `600`。回退时恢复旧 `FRONTEND_RELEASE_TAG=20260903T170310Z` 和旧 `current` 指向即可，不需要恢复数据库。
- V1.3 用户手册共 17 页，已逐页检查中文、表格、页眉页脚和分页；DOCX SHA256：`914d0d424be8abce26f4ddc94e5767066aec328f15805621b9ae41561c9f5437`。

## 转料批次与流水号升级（北京时间 2026-09-05）

- 当前源码：`/opt/heatsinkrep/releases/20260905T154436Z`；`current` 已原子切换。后端镜像为 `heatsinkrep-backend:20260905T154436Z`（`sha256:ee94a16c42bc6778b193f5875d3c96fb5c0b6a139dd74a890d0ce58d3b98482c`），前端镜像为 `heatsinkrep-frontend:20260905T154436Z`（`sha256:589accdfe8dd6f485cc73c8f9ea63bec77bc1e4ff50094f354643c0fb6b2adf5`）。MySQL、后端和前端均健康，公网端口仍为 `8082`。
- 上线前完整备份为 `/opt/heatsinkrep/backups/pre-transfer-batches-20260905T154436Z.sql.gz`，权限 `600`，压缩完整性检查通过，SHA256 为 `56f6690d0599af3122d15fbea076a7bc441650900a18f8b5d2a48021eaa17dd8`；同目录保留升级前 `.env` 私密副本，访问口令、账号和其他密码均未变更。
- 在与线上数据一致的隔离 MySQL 副本上，先将 `20260903_0002` 升到 `20260905_0003`：升级前后均为 13 张工单，4 张新表全部存在。再连续两次执行转料示例补种，第一次新增 4 批，第二次幂等跳过 4 批；最终为 5 条物料明细、4 条关联收料、13 张带流水号的演示工单。隔离容器、网络和卷验收后已删除。
- 线上补种只处理脚本明确标记且结构未被改动的演示数据：工单 `DEMO-BATCH-001`、流水号 `DEMO-SERIAL-BATCH-001` 现有 `TL20260905000001` 至 `TL20260905000004` 四个真实批次，状态为 1 个已接收、2 个部分接收、1 个待接收。正式工单未被补种脚本改写。
- 后端实现持久化批次号、Code 128、同一报工拆多批、多物料行、数量/重量在途预留、分次接收、首次接收锁定、未锁定批次编辑/作废、打印审计和外部库存动作边界；公司主系统 ERP/WMS 接口仍等待真实接口文档，不伪造外部数据。
- 前端新增转料批次列表、批次扫码接料、流水号追踪、批次详情与权限操作；新工单流水号必填，旧无流水号工单禁止直接创建新批次。转料单默认 A4 上下双联（存根联 / 随货联），补打强制填写原因；末序保留现有完工兼容入口。
- 本地后端 119 项测试通过；前端 24 个测试文件、153 项测试通过，TypeScript 和生产构建通过。线上只读验收 15/15 通过，覆盖访问锁、认证、13 张工单、9 个工厂班组、整单数量守恒、4 个转料批次、批次条码、同报工拆批、多物料行、分次接收、批次精确扫码和流水号追踪；公网健康检查与默认锁定状态正常。此次验收未使用 Codex 内置浏览器，也未通过接口修改业务记录。
- V1.4 用户手册共 12 页，逐页渲染检查通过；DOCX SHA256 为 `91cd66ad5e6af297ce91e5b13ecd841201f5aba1c948e86b1713297a62c0b51d`。源码传输包 SHA256 为 `89eab4d5868d459b8b56b1e9395fc1dbb699ed335f92f54e76f2eb5f125f488e`。
- `20260905_0003` 不支持原地降级。若必须回退，应暂停本项目入口，恢复上述 SQL 备份和升级前 `.env`，再同时恢复旧后端/前端镜像与旧 `current`；只切换旧镜像不能回退数据库结构。

## 库房边界与界面统一升级（北京时间 2026-09-06）

- 当前源码：`/opt/heatsinkrep/releases/20260905T171033Z`，`current` 已原子切换。后端镜像为 `heatsinkrep-backend:20260905T171033Z`（`sha256:652d77d76a366891df9160f52014345ebabde1f1b5b894c28620a55aab98d180`），前端镜像为 `heatsinkrep-frontend:20260905T171033Z`（`sha256:138e78154e1790ad3032aba9c868a1eb71bdeee33927f756dd59700ac0eea078`）；三个容器均健康，公网端口仍为 `8082`。
- 上线前完整备份为 `/opt/heatsinkrep/backups/pre-warehouse-ui-20260905T171033Z.sql.gz`，权限 `600`，压缩完整性检查通过，SHA256 为 `cb63647fe78a6ade816eb392f706cc8f4e48d8f48cdd4832dd1290bb11aed1ed`；同目录保留升级前 `.env` 私密副本。
- 新增唯一系统库房 `FACTORY-WAREHOUSE`。所有升级后新建工单均以“库房发料”为固定起点，以“库房入库”为固定终点；库房不执行人工报工，且不能被改名、改类型、停用或删除。历史工单不改写，流水追踪页为历史记录补充只读边界提示。
- 转料批次继续只绑定一次相邻交接，即一个来源节点和一个接收节点；批次详情、条形码扫描、打印与接口均不携带整条多工序路线。`DEMO-WAREHOUSE-001` 用作新版直观示例，共 10 个路线节点，批次 `TL20260906000001` 仅演示“库房发料 → 扎板”并已接收。
- 前端统一为 Vue 3、Element Plus、Pinia、Axios 与 ECharts：业务表格、表单、弹窗、状态反馈和时间线已统一组件；桌面工作区固定在可视高度内，长表格和明细改为区域内滚动；文案改为简洁正式；页面切换、侧栏展开、表格反馈与图表使用克制动效，并兼容减少动画设置。
- 本地后端 121 项、前端 27 个测试文件共 166 项测试通过，TypeScript 类型检查与生产构建通过。上线后无浏览器只读验收 16/16 通过，覆盖访问锁、管理员认证、14 张工单、唯一有效库房、9 个业务班组、数量守恒、转料批次、条形码扫描和流水号追踪；公网健康与默认锁定状态正常。
- 公司 ERP/WMS 的真实出入库接口仍等待正式接口文档。当前库房发料数量由工单计划数量建立内部待转量，只表示本系统小循环的首尾交接，不伪造公司主系统的出入库结果。

## 一期纯物料转料收口（北京时间 2026-09-06）

- 当前源码：`/opt/heatsinkrep/releases/20260906T040104Z`，`current` 已原子切换。后端镜像为 `heatsinkrep-backend:20260906T040104Z`（`sha256:1e5c6f0fe994afd70c0904cbee00219821440a30fc49a396b84fc45e8c248019`），前端镜像为 `heatsinkrep-frontend:20260906T040104Z`（`sha256:5d068089fbf1465385b3ce9c0f1ded50f683f377b43cce9daae4906ce8922ecd`）。MySQL、后端和前端均健康，公网入口仍为 `http://122.200.93.68:8082`。
- 一期新转料不再依赖产品、工单、工艺、工序、报工或产量。班组长直接填写流水号、件数、重量、接收班组和备注；转出班组由账号自动确定。接收班组扫码后整批确认，确认后永久锁定。管理员只管理班组和班组长，对转料只读。
- 新增独立 `material_transfers` 表和迁移 `20260906_0004`。升级前完整备份为 `/opt/heatsinkrep/backups/pre-phase1-direct-transfer-20260906T040104Z.sql.gz`，权限 `600`，压缩校验通过，SHA256 为 `bb725f3eddedcd7bbfcd5932a4de0ed9db468995c8efc10ad7faa80870555d84`；私密配置副本同目录保存且权限为 `600`。
- 迁移先在临时 MySQL 8.4 中恢复线上备份，再连续两次升级到 `20260906_0004`。升级后新表为 26 列，旧数据计数保持 14 张工单、5 张旧转料批次不变；临时容器和网络已清理。线上迁移完成后再次确认版本为 `20260906_0004 (head)`。
- 本地后端完整测试 130 项通过；前端 31 个测试文件共 191 项通过，TypeScript 类型检查及 Vite 生产构建通过。上线后旧数据只读验收 16/16、一期专项无写入验收 8/8 通过，覆盖访问锁、认证、纯转料字段、Code 128、角色与班组权限、已确认锁定及拒绝请求不改数据。
- 已添加流水号 `DEMO-DIRECT-FLOW-001`：`TL20260906000002` 已接收，`TL20260906000003` 待接收，两张均为 24 件、4.800 kg。演示脚本只复用原有有效班组长，不创建或重置账号密码；第二次执行确认幂等复用。
- 一期主导航仅保留转料记录、扫码接收、流水追踪，以及管理员的班组管理和班组长管理。旧业务表与旧记录均保留，旧 URL 安全重定向，不执行数据删除。全部部署和验收均未使用 Codex 内置浏览器。

## 班组库存与整批一码发布（北京时间 2026-09-07）

- 发布源码：`/opt/heatsinkrep/releases/20260906T184154Z`；`current` 已切换，前后端版本标签均为 `20260906T184154Z`，入口仍为 `http://122.200.93.68:8082`。
- 后端镜像 ID：`sha256:cc1e141cb6bb24dced34ee16057631eb4c777d649c71caad95582782f38b11a8`。前端镜像 ID：`sha256:d0b51214977659cae4202d61bb94dfaa203ff1d0c5860d514581f8bff6bc1d47`。镜像均在服务器构建，未复制本地数据库。
- 停止本项目写入后完成数据库备份：`/opt/heatsinkrep/backups/pre-batch-release-20260906T184154Z.sql.gz`；配置备份：`/opt/heatsinkrep/backups/pre-batch-release-20260906T184154Z.env`。升级将数据库从 `20260906_0004` 迁移至 `20260907_0010`。旧源码、旧镜像和 MySQL 数据卷保留。
- 本次发布包含固定八班组页面、材质数量/重量纵览、库存与丢失记录、批量出库、下序待接收、库房手工入库/对外出库、检验发货，以及 CK 整批一条形码、整批确认和 A4 汇总打印。内部交接由下序确认；库房对外出库和检验发货由本班组确认；保留历史 TL 编号及明细追溯。
- 正式班组配置脚本仅将“扎板”改为“轧制”，另外七个正式班组复用；未删除旧班组、改绑旧账号、创建班组长或重置任何密码。历史记录保留，未导入本地测试数据。既有演示账号仍保留原班组绑定，正式班组长由管理员配置。
- 首次迁移命令在加载迁移文件时被 macOS AppleDouble 文件 `._20260906_0004_material_transfers.py` 阻断，当时尚未执行迁移。已为前后端 Docker 构建排除 `._*`，重建后端镜像并完成升级；该附带文件移至 `/opt/heatsinkrep/backups/appledouble-migration-20260906T184154Z`，原迁移文件未改写，升级前备份未覆盖。
- 按用户“无需验证，直接上传部署”的要求，本次没有运行额外测试套件、隔离迁移演练、业务写入验收或浏览器操作；仅完成构建、数据库升级、服务启动及入口可用性检查。不将此前本地测试结果表述为此次线上业务验收。
- 服务器其他站点、Nginx、MinIO 和原有端口配置未改动。回退须协调数据库结构及备份恢复，不能仅切回旧镜像。

## 紧凑台账与动态大屏发布（北京时间 2026-09-12）

- 用户在 SSH 直连恢复后明确要求发布当前版本。本次仅发布程序，未沿用之前暂停的清库/补种计划。
- 源码版本：`/opt/heatsinkrep/releases/20260912T132133Z`；`current` 已原子切换，前后端标签与 `.env` 的两个发布标签均为 `20260912T132133Z`。公网入口保持 `http://122.200.93.68:8082`。
- 后端镜像：`sha256:f88ec7aad4cd37ab98a7108114ae4eafd39b04a51d532f21aefb17004a23a977`；前端镜像：`sha256:663af8f1ca29de6939ec017ddfbb2e1d2f91ac083b614a48d327c36191a01d5b`。与旧版本依赖文件及迁移文件逐一比对相同，后端基于已有依赖镜像替换应用源码；前端使用本地新构建产物，构建过程不访问外部镜像仓库。数据库仍为 `20260907_0010`，本次无表结构迁移。
- 包含选定的第一张风格：两级侧栏、八班组流水号台账、紧凑汇总及筛选、统一控件和文案、20 / 50 / 100 条分页；同时发布之前尚未上线的班组分析、全厂总览与动态机器人大屏。机器人模型、不同班组设备和首条动态信息联动保留。
- 源码包 SHA256：`566c492e4004dd3abdcac84a713a36a11e1e6579f11492b02e897b550f035d52`。上传后验证一致，再在新目录构建，不覆盖旧源码。归档的 macOS 扩展属性产生了 tar 忽略提示，无 AppleDouble 迁移文件，构建成功。
- 切换前仅停止本项目的前后端，MySQL 及其他服务保持运行。数据库完整备份：`/opt/heatsinkrep/backups/pre-20260912T132133Z/database.sql.gz`，gzip 完整性检查通过，SHA256：`aca89b19ab328bdf81b1aa2cfd35783f36678b54b5a6ef699fc5c425ebe70d72`。同目录保存 `environment.env` 及逐表内容摘要，权限均为 `600`，目录权限 `700`。
- 新镜像在切换前后分别对现有 MySQL 执行只读检查，覆盖八班组库存、流水号聚合分页/可用库存筛选、班组统计、全厂统计及动态大屏查询，均通过。逐表记录数及完整内容摘要发布前后完全一致，包括账号、密码哈希、班组、会话及旧业务记录；未导入本地演示数据，未创建/改绑账号，未删除旧班组。
- 公网健康接口返回应用及数据库均正常；访问锁保持启用且默认锁定，未解锁的业务请求返回预期的 `423 site_access_required`。首页、班组及大屏路由、主 JS / CSS、大屏脚本、机器人 GLB、雕刻及线切割设备图片与本地发布产物 SHA256 一致。
- 本次重新运行前端类型检查和生产构建成功；之前该版本已完成 311 项前端及 7 项相关后端测试，本次没有重复运行完整套件或浏览器。验收无业务写入、无 Codex 内置浏览器使用。
- 三个项目容器均健康，数据库容器未重启；旧版本 `20260906T184154Z`、其镜像及本次备份保留。由于本次无数据库结构或内容改变，应用回退可恢复旧发布标签和源码指向；不要为了回退程序覆盖发布后新产生的业务数据。回退前应再次核对届时的数据和配置变化。

## 八班组演示数据增量导入（北京时间 2026-09-12 晚）

- 程序发布完成后，用户明确要求“把数据也堆上去”。本次增量补充测试数据，不清库、不覆盖本地 SQLite，也不更改原有账号或历史记录。
- 操作前停止本项目前后端，保持 MySQL 和其他项目运行，并重新备份至 `/opt/heatsinkrep/backups/pre-showcase-20260912T133253Z/database.sql.gz`。gzip 校验通过，SHA256 为 `5254f2dfdcb6e54a356e6f3eb61ebe03e1eeddb6fb685021e27ac7587f2d3f35`；备份目录权限 700，SQL 及演示密码文件权限 600。
- 使用已测试的 `app.seed_team_material_showcase`，通过库房手工入库、内部转料、整批确认、库房对外出库、检验发货和丢失登记等业务函数生成。新增 433 条物料记录、376 张出库单、192 条丢失记录和 8 个演示班组长；叠加原来的 2 条一期转料后，总物料记录为 435 条、账号总数为 14。
- 八个演示账号为 `demo_warehouse`、`demo_roll`、`demo_anneal`、`demo_grind`、`demo_wire`、`demo_engrave`、`demo_plate`、`demo_inspection`，绑定约定的八个正式班组，演示密码与本地一致。原有 6 个账号的密码和班组绑定不变，15 条班组配置保持原样。
- 导入前后按主键逐条比对原有表数据摘要，旧记录无修改或删除；仅允许新增测试数据及条码序号正常递增。校验报告为备份目录下的 `verification.json`；再次执行种子函数确认幂等跳过，不产生重复记录。
- 每班组包含 6 种材质、23 条待接收明细、24 条丢失记录；库房 25 个流水号，其余班组 24 个。默认流水号、库存、出库、丢失列表均有完整 20 条，全部库存余量非负。动态大屏可加载八班组和最近 100 条批次动态。
- 相关独立内存库回归测试通过。恢复服务后，八个演示账号实际 HTTP 登录及流水号分页通过，库房库存/出库/丢失/入库四个接口和动态大屏接口通过；测试登录均主动退出，仅新增并注销验证会话，没有额外业务写入。未使用任何浏览器。
- 公网应用及数据库健康检查通过，访问口令锁保持启用。前后端继续运行 `20260912T132133Z`，MySQL 未重启，未修改其他站点或端口。

## 独立机器人大屏与旧演示班组清理（北京时间 2026-09-12 晚）

- 用户明确要求删除截图中的五个旧 DEMO 班组、恢复机器人背景，并使大屏脱离后台页面布局。本次不删除截图之外的班组、不重置数据库、不更改现用账号密码。
- 当前源码切换至 `/opt/heatsinkrep/releases/20260912T142714Z`，前端标签为 `20260912T142714Z`、镜像 ID 为 `sha256:7b4fd3bf8cd4ac4a95c6fdde3237abb34e30a5266f9ddc65afb8ca3ca18ad7fe`。后端保持 `20260912T132133Z`，MySQL 和后端均未重启，无数据库结构迁移。
- `/factory-live` 为登录后独立全视口路由，不再嵌在侧栏、顶栏内部。后台“大屏展示”点击即同步请求原生全屏；直接访问或刷新仍为独立页面，浏览器未允许原生全屏时可用页面全屏按钮。返回系统总览恢复业务布局。原工厂背景与中央平台恢复，动态三维机器人、八个班组、首条转料联动和原统计大屏保留，不恢复机器人周围流转线。
- 清理前短暂停止本项目前端入口，生成完整备份 `/opt/heatsinkrep/backups/pre-live-cleanup-20260912T142714Z/database.sql.gz`，SHA256 为 `032b4e4ddb1a88d66bff6254d66be9b8c02785c51d092e589b07134352bc43dd`；gzip 校验通过。配置副本与清理结果同目录保存，目录权限 700，文件权限 600。
- 先只读核对固定 ID、班组编码、账号绑定、样例流水号和旧工艺归属，再在单一事务中删除 `DEMO-CUT`、`DEMO-CNC`、`DEMO-SURFACE`、`DEMO-QC`、`DEMO-PACK` 共 5 个班组，及绑定的 5 个旧演示账号、24 个旧会话、2 条 `DEMO-DIRECT-FLOW-001` 样例转料、15 条旧工艺模板。未关闭外键检查。14 张旧工单及其历史保留，已删除账号/班组的可空外键依原规则置空。
- 现用八班组及账号、433 条物料、376 张出库单、192 条丢失记录逐条摘要比对不变，条码计数器不变。清理后共有 10 条班组配置、9 个账号；截图外的 `FACTORY-SHIP`、`FACTORY-SCRAP` 保留，它们不属于当前八班组工作台。
- 本地前端 42 个文件 314 项测试通过，类型检查及生产构建通过。独立 Chrome 验证 2560×1440、1366×768、390×844，实测点击入口进入原生全屏、直接 URL 无后台布局、返回恢复后台并释放机器人画布；控制台 0 错误、0 警告。未测试 4K，未使用 Codex 内置浏览器。会话关闭后确认专属浏览器及 profile 进程全部退出。
- 上传前端源码及构建产物归档 SHA256 为 `05660736b051e79dda58731eb9b54479fab344574d083b7a598261a99ad926f8`，服务器核对后基于已有 Nginx 镜像离线构建。归档排除依赖、环境文件和 macOS 附带文件；后端源码复用上一版本。
- 上线后公网首页、大屏路由、主 JS/CSS、大屏脚本、工厂背景、机器人 GLB 与本地产物 SHA256 一致。健康接口正常；访问锁仍启用，未解锁业务请求保持返回 423。八个演示账号实际 HTTP 登录与默认 20 条流水号分页通过，库房库存/出库/丢失/入库列表及八班组、100 条大屏动态接口通过；测试会话均主动注销，没有额外业务写入。
- 旧源码、镜像与备份保留，其他项目和端口未改动。页面回退仅恢复旧前端标签；若要恢复此次删除的数据，须从该次备份定向提取并核对新数据，不应整库覆盖发布后的业务记录。

## 内部转料提交即扣库存（北京时间 2026-09-12 晚）

- 用户明确修正：上序转出后即使下序待接收，上序库存也须减少。内部待接收量新增为 `in_transit_quantity/weight`；`on_hand` 排除内部在途，下序确认只增加下序库存，不重复扣上序。作废退回上序，改单重新校验并调整余量；现有并发锁、幂等、确认锁定不变。库房对外出库和检验发货的本班确认规则不变。
- 前后端发布为 `20260912T145538Z`，current 指向同名源码目录。后端镜像 `sha256:6a1473d9a6c91e3c8affb0d4aa02a43011e767ece484b13dc08a999f2b4a23a1`，前端镜像 `sha256:56a80d48b48965c965f0c1fc0f4def17bfd6fcbddb3256e99938f766cc309789`。依赖和迁移文件与上一版本相同，数据库仍为 `20260907_0010`，MySQL 容器不重启。
- 班组材质、来源批次、流水号汇总、库存排行、类型构成及库龄均复用新的库存计算；全厂总览、八班组卡片及机器人大屏同步。内部转出趋势、去向和“今日转出”按提交时间，接收按确认时间；跨日接收不重复记转出。旧外部确认统计、动画与原背景、独立全屏入口保留。
- 发布前停止本项目前后端、备份至 `/opt/heatsinkrep/backups/pre-transit-20260912T145538Z/database.sql.gz`。gzip 校验通过，SHA256 为 `e8c09d0559465454ff64baf0c4922bc0ede8648fb7a5ab1f101add87c5e6489c`；同目录保留配置、逐表摘要与查询核验报告，权限受限。
- 本次只重新计算，不回填、迁移、删除或覆盖业务数据。以旧镜像拍摄全表内容摘要，新镜像只读查询及应用启动后再次比对，全部表计数与摘要一致。八班组余额满足“新在库 + 内部在途 = 旧账面量”，可用量完全不变；材质、流水号、大屏汇总相同。
- 发布时在库为 17,334 件 / 1,733.4 kg，内部在途 1,648 件 / 164.8 kg，合计 18,982 件 / 1,898.2 kg。433 条物料、376 张出库单、192 条丢失记录不变。库房在库 8,882 件，其余普通班组各 1,210 件、检验 1,192 件；各班内部在途 206 件，不重复叠加到库存。
- 新增回归先复现旧口径失败，再验证即时扣减、待接收不入库、修改与作废、整批确认和重复确认、跨日统计。全量后端 159 项、前端 315 项及 TypeScript/生产构建通过；现有依赖弃用、测试警告及大包提示保留，未扩展处理。
- 独立 Chrome 验证 2560×1440 全厂及机器人大屏、1366×768 库房台账；台账加载 20 条，后台与独立大屏切换正常，控制台 0 错误/警告，机器人背景和动作保留。只读查看未提交演示业务；会话 `heatsink_transit_20260912` 及专属进程已全部关闭，未使用内置浏览器、未测试 4K。
- 源码归档 SHA256 为 `db2df489267cadd5de0a653879333650dd936802b60deeb9b8a9b8123b00ca2f`，服务器核对后离线构建。公网新 JS/CSS、班组/全厂/机器人脚本和背景与本地产物匹配，健康和访问锁正常。八个演示账号 HTTP 登录及 20 条分页、库存/出库/入库/丢失和大屏查询通过，测试会话已注销，无额外业务写入。
- 旧前端 `20260912T142714Z`、后端 `20260912T132133Z` 及备份保留，程序回退应同时恢复对应的两个标签，不回灌数据库。其他项目、服务端口和账号密码未改变。

## 阅读样式、日期加急与表格式单据发布（北京时间 2026-09-13）

- 用户明确要求将当前本地版本发布服务器。前后端均切换为 `20260912T195806Z`，`/opt/heatsinkrep/current` 指向同名源码目录，入口仍为 `http://122.200.93.68:8082`，MySQL 容器未重启。
- 发布包含已确认的全业务页阅读样式、默认 10 条及 20/50/100 分页、整页滚动、日期筛选、管理员流水号加急及动态旗标、TL/CK 详情和打印表格化；原两套大屏、动态机器人与背景保留。
- 本次需配套升级至 `20260912_0011`。隔离 MySQL 演练发现历史初始迁移使用当前 metadata，导致新表提前存在；修正 0011 对既有表及索引的兼容，保留数据，拒绝冲突索引定义。补充 3 项迁移回归，加上日期与加急 9 项，共 12 项通过。真实旧镜像建库后升级、空库初始化及重复升级均在隔离 MySQL 8.4 通过；测试容器和临时数据库已移除。
- 生产停止前后端后备份至 `/opt/heatsinkrep/backups/pre-20260912T195806Z/database.sql.gz`，gzip 完整性通过，SHA256 为 `a6293334fc9a91f7aa381ee4c7920cf23eafd5af3d3a475d76c6c2388f471a29`；配置和逐表摘要同目录受限保存。
- 迁移后和后端启动后均逐表比对，除 Alembic 修订号外的 21 张原有表行数和内容哈希完全相同，保留 9 个账号、10 条班组配置、433 条物料记录、376 张出库单及 192 条丢失记录。仅新增两张空的加急表，不清库、不导入或覆盖本地测试数据，不修改密码。
- 服务端八个班组的库存、流水号、日期/加急组合筛选、图表及全厂/机器人大屏只读查询通过。前后端和数据库全部 healthy，访问锁仍生效。
- 公网共 48 项检查通过：健康与访问锁 3 项；首页、业务路由、全部构建 JS/CSS、机器人模型、背景及设备图等 45 项与本地产物 SHA256 相同。未使用 Codex 内置浏览器或独立浏览器；本次也未执行生产业务写入测试。前端沿用刚完成的 360 项测试、生产构建和 A4/布局验收产物。
- 最终源码归档 SHA256：`4c6a45e6e2179c115ee0fb4cc5f6b3b27081ac9238640075b7dd4596a4c2142d`。发布过程见服务器版本目录 `publish.log`；本地发布检查结果保留于 `/tmp/heatsink-document-release-20260913.XuWBa0/`。
- 旧版本 `20260912T145538Z`、镜像及备份均保留。0011 仅新增表，但旧 Alembic 无法识别该修订号；若需回退旧应用，应保留新表，并使用本次发布目录的 `rollback-compose.yml` 以旧 Uvicorn 入口启动，不能直接用旧迁移命令或整库覆盖来回退。
