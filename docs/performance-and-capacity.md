# 性能与容量验收

功能正确不等于容量达标。在线人数、活跃用户数、并发请求数、接口 RPS/QPS 和数据库 QPS 分开统计；不拿连接数、空库结果或单接口速度承诺整套系统容量。

## 每轮要核对的指标

| 维度 | 验收内容 |
| --- | --- |
| 场景与数据量 | 活跃用户数、请求间隔、页面比例、每次操作接口数量、流水号/批次数和历史跨度；说明是否真实浏览器、是否同一账号 |
| 吞吐 | 测试窗口内完成的请求数与 RPS；业务 TPS 按成功入库、提交或确认分别定义，不混入登录、心跳与查询 |
| 响应 | 查询/提交分别统计 P50、P95、P99、最大耗时和错误率；区分起压阶段与稳定阶段 |
| 实时推送 | 长连接数、首帧等待、事务提交至客户端收到更新的延迟、断线恢复和过期鉴权；长连接存续时长不是查询响应时间 |
| 服务资源 | CPU、内存、连接池等待、慢 SQL、锁等待、连接清理及容器重启/OOM；考虑同机其他服务占用 |
| 库存正确性 | 并发扣减不超扣、幂等不重复入账、确认不二次扣库、作废恢复、失败回滚、数量重量守恒、看板与台账一致 |

使用分级负载和明确停止阈值，不为找极限拖垮共享服务器。线上测试须获得许可，不向正式库存写压测数据；写入、冲突和大数据量测试使用隔离库。验收阈值与数据规模应一起约定，不能用单一“支持多少人”代替。

## 2026-09-23 线上基线

版本 `20260922T203935Z`：共享服务器 8 核 / 约 16 GB；本应用单 worker，后端内存限额 512 MiB、MySQL 1 GiB，应用数据库连接池常驻 10、额外最多 20。没有调整这些配置。

用户允许后执行一次受控 API 短测。线上物料记录为 **0**，共享一个临时管理员会话，约 80% 为班组库存查询、其余为库存总览/机器人大屏。每个虚拟客户端保持一路 SSE；班组端一轮读取库存明细和班组汇总两个接口，结束后等待 5 秒，收到变更通知也刷新。每轮用临时登录/注销事务触发现有提交事件，不修改物料。逐级计划 10 / 25 / 50 / 100 客户端，近期 P95 超过 3 秒或发生错误、资源越界即停止；该阈值是本轮保护条件，不是承诺的服务等级。

| 模拟客户端 | 阶段耗时 | 完成查询 | P50 | P95 | 查询错误 | 收到提交通知 |
| --- | --- | --- | --- | --- | --- | --- |
| 10 | 26.7 秒 | 112 | 124.2 ms | 1,339.0 ms | 0 | 10 / 10 |
| 25 | 17.8 秒 | 171 | 1,295.7 ms | 3,285.8 ms | 0 | 25 / 25 |

25 客户端阶段触发延迟阈值，未继续运行 50 / 100。阶段平均完成请求速率约 4.19 / 9.61 RPS，包含起压、等待和验收开销，**不是最大吞吐量**。脚本原始结果未统计 P99，不能事后编造。三容器仍 healthy、无重启；临时连接关闭、验证会话注销，物料记录仍为 0。该结果不能证明真实用户登录容量、长期运行或出入库写入吞吐。

## 第一项：共享实时快照（已随 20260923T065006Z 发布）

- 同一数据修订版本、同一视图的 SSE 订阅共享已经序列化的快照；并发读取合并为一次汇总与序列化，库存总览和机器人大屏互相隔离。
- 提交事件推进修订号，缓存立即失效；回滚和纯读取不推进。构建期间发生提交时，不将旧事务快照缓存为新版本，连接继续接收下一轮通知。
- 缓存最多两份，不存用户、令牌或权限。每次读取（包括缓存命中）仍独立校验登录；权限数据库会话在等待共享计算之前释放。
- 本地日期变化及两秒最大缓存年龄控制新订阅的时间窗口；过期只在下一次读取时计算，不新增轮询。业务提交不等待这两秒。
- 查询、入库、扣减、确认等业务事务和行锁未改；筛选列表也未加入可能串条件或串权限的共享缓存。
- 结构化请求日志补充 `is_stream`、`first_body_ms`。分析接口 P95 时排除长连接的 `duration_ms`，SSE 单看首帧/连接生命周期；新增 `inventory.snapshot.built` 记录共享构建耗时，不记录库存内容或认证数据。首帧日志不是提交至浏览器的端到端推送延迟。

仍然是单进程提交事件，不能直接增加 worker 或多实例，否则不同进程无法互相通知。没有为本次优化引入额外服务、数据库迁移或新的监控面板。

## 相同数据下的汇总计算对比

`scripts/measure_inventory_fanout.py` 强制使用自动清理的本地 SQLite 临时库，通过真实收发流程生成 433 条物料记录。25 个线程同时调用真实鉴权和汇总，再检查件数重量一致；前后使用相同脚本、数据和读取数量。本测试不经过 HTTP、浏览器或生产 MySQL，是汇总放大倍数的微基准，**不是优化后的线上并发容量**。

| 视图 | 优化前构建次数 | 优化后构建次数 | 优化前 SQL 数 | 优化后 SQL 数 | 本地 P95 前 / 后 |
| --- | --- | --- | --- | --- | --- |
| 库存总览 | 25 | 1 | 525 | 45 | 4,553.8 / 359.0 ms |
| 机器人大屏 | 25 | 1 | 900 | 60 | 5,197.9 / 314.8 ms |

两轮各客户端库存合计一致，未出现请求异常。耗时受本机和 SQLite 影响，仅 SQL 数及重复构建减少能够直接说明本次优化所针对的问题；不得把这些毫秒数写成生产 SLA。

相关后端回归 131 项通过，随后新增“构建期间发生提交通知不丢失”专项 1 项通过；覆盖真实收发/丢失/作废后的快照、失败回滚与幂等、鉴权/访问锁失效、缓存并发合并、失败唤醒、跨日/超龄、日志脱敏与首帧转发。后端质量检查与差异检查通过。本项独立提交 `a4cf826`，已推送并随本轮后端发布上线。

重跑当前版本：

```bash
python scripts/measure_inventory_fanout.py --clients 25 --populated
```

同脚本对比旧源码：

```bash
python scripts/measure_inventory_fanout.py --backend /path/to/previous/backend --clients 25 --populated
```

## 第二项：列表关联数据读取（已随 20260923T065006Z 发布）

分页列表读取来源批次时，原来的 ORM 加载方式会继续加载来源的关联数据，部分分页退化为逐条补查。用真实入库、转出、接收、回库、丢失、加急操作构造 26 条转料记录后，转料列表一页 10 条触发 35 次 SELECT（含鉴权与总数查询）。

现在列表一次联查加急、来源批次号及历史出库单号；来源仅取 ID 和批次号，不展开它自己的来源、班组、加急与其他资料。六类列表共用该读取规则：转料记录、出库记录、可出库来源、库存分组下的来源明细、库房入库记录、历史合单记录。仅更改只读查询的加载方式；详情、打印、写入事务、行锁、字段、排序、筛选和分页规则保持原样。

同一测试场景下，转料列表降至 3 次 SELECT。专项检查每页 1 / 10 / 100 的请求参数（实际返回条数受样本量限制），六类列表分别不超过 3 / 4 / 5 / 5 / 4 / 6 次 SELECT；页数和记录关系不再触发逐条补查。与详情逐字段对照管理员、上序和下序的返回结果，保留前导零、加急、历史单号、权限动作及按需查看的审计/丢失记录；空页和未登录访问也有覆盖。

专项及相关业务回归共 110 项通过，后端质量检查与差异检查通过。这些是隔离 SQLite 中的查询次数与正确性验证，不是生产 MySQL 查询耗时、QPS 或容量承诺。没有新增索引或修改业务数据。本项独立提交 `bc0cac4`，已推送并随本轮后端发布上线。

## 第三项：班组汇总去重（已随 20260923T065006Z 发布）

班组汇总已经按材质取回完整的结存分类，原来还单独执行相同台账的总计查询。现在直接累加未做展示舍入的材质分组件数与重量，避免重复聚合；数据库仍负责材质分组，未改成 Python 字符串分组，不改变 MySQL 的排序/大小写归类规则。物料类型统计保持原查询。空材质也包含在总计中，空班组各余额为零。

库房待接收的批次数复用待接收记录数：批次号有数据库唯一、非空约束，一条记录就是一个批次，合在一次提交里的两个独立批次仍计两批，不需要再次扫描去重。

管理员会话的隔离 API 测试中，库房汇总从 8 次 SELECT 降到 6 次，普通班组从 7 次降到 6 次（均包含鉴权和班组查询）。不缓存班组汇总，也不改变库存公式；三位小数、废料、纯重量、缺失材质、未跟踪历史单、待接收、部分接收及作废恢复均与批次台账逐项核对。

汇总专项、班组库存、全厂矩阵、在途/对外出库及实时推送相关回归共 89 项通过，后端质量检查与差异检查通过。本项独立提交 `0021eba`，已推送并随本轮后端发布上线。以上三组回归有交集，不相加为独立测试数量。

## 2026-09-23 发布与复测

后端程序提交 `0021eba`（含以上三项独立提交）已推送 GitHub，部署为 `20260923T065006Z`；前端保留 `20260922T203935Z`。worker、内存限额、连接池、依赖及数据库结构均未调整。

### 隔离 MySQL 有数据验收

发布前使用同一后端镜像和隔离的 MySQL 8.4，从空库完成迁移，经真实收发流程生成 **433 条物料记录、8 个班组**。逐班组核对分类库存与独立台账聚合、全厂库存矩阵；库存分组、出库、来源和库房入库列表均通过。十条记录的直接 ORM 读取及序列化，默认加载 30 次 SELECT、优化加载 1 次，字段逐项相同；这里不含 HTTP 鉴权和分页总数，不能与前文 API 的 35 → 3 直接相减。每种视图各 25 个并发鉴权读取，库存总览和机器人大屏分别只构建 1 次，客户端结果完全一致。

样例只存在于无公开端口的临时数据库；验证后该容器、临时数据卷和网络已清理，未导入正式数据库。这是正确性及查询放大验证，不是有历史数据的 HTTP 容量测试。

### 公网短时复测

沿用基线的共享管理员会话、页面比例、5 秒操作间隔及会话提交通知场景。线上物料记录仍为 **0**，本次依次运行 10 / 25 / 50 客户端；各阶段最多 25 秒稳定期，近期 P95 超过 3 秒即停止，不运行 100 档。

| 模拟客户端 | 阶段耗时 | 完成查询 | P50 | P95 | P99 | 查询/推送错误 | 收到提交通知 | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 10 | 27.0 秒 | 120 | 372.7 ms | 1,162.2 ms | 1,186.4 ms | 0 | 10 / 10 | 完成 |
| 25 | 27.9 秒 | 248 | 662.3 ms | 2,570.0 ms | 2,696.2 ms | 0 | 25 / 25 | 完成 |
| 50 | 22.4 秒 | 354 | 1,142.9 ms | 4,589.1 ms | 5,295.8 ms | 0 | 50 / 50 | 延迟阈值停止 |

25 档 P95 从基线的 3,285.8 ms 降到 2,570.0 ms（约 22%），本轮完成原定时段；50 档未达到本轮延迟保护标准。10 档 P50 反而高于前一轮，因此不把一次公网短测解释为所有请求都稳定加速。阶段平均完成速率约 4.44 / 8.89 / 15.80 RPS，包含起压、等待及验收开销，不是持续最大 QPS 或业务 TPS。

三档 SSE 首帧 P95 分别为 1,346.1 / 626.9 / 2,086.7 ms；全部收到提交后的事件，但未计量提交至客户端的端到端延迟。资源采样中后端最高 122.5 MiB / 512 MiB，MySQL 720.5 MiB / 1 GiB；后端 CPU 采样最高 111.76%（Docker 多核口径），不是全时段精确峰值。无请求错误不代表 50 档可用容量已经达标。

复测后全部临时连接关闭、验证会话注销，三容器 healthy、重启数为零。29 张表复核：应用切换前后全部内容一致；HTTP 验收后仅验证登录会话表变化，其余表与停写备份完全一致。服务器与本机公网均核对 92 个静态文件、15 个页面入口；三路 SSE 首帧、提交通知、心跳及 423/401 保护通过。未使用浏览器，没有物料业务写入。

原始脱敏结果 `capacity-result.json`、有数据 MySQL 验证日志、复测脚本和发布验收脚本保存在 `/opt/heatsinkrep/releases/20260923T065006Z/`；本机留存于同版本的 `output/releases/`。结论仅适用于这次短时读取场景，不承诺 25 名真实用户长期操作、大规模历史查询或并发出入库能力。

## 容量承诺前仍需完成的验证

隔离 MySQL 中使用代表性的历史数据量，分别测试普通查询、全链路查询、多人真实转料写入、同一来源批次竞争及看板同时在线。固定场景逐级测 25 / 50 / 100 用户，补齐稳定期 RPS、P99、写入 TPS、端到端推送延迟、连接池/锁等待和长时间资源情况；满足库存正确性与响应目标后再报告容量。本地查询次数下降和单次功能回归不等于已经通过线上并发容量验收。

## 后续修复：减少无关刷新（已随 20260923T080136Z 发布）

第一项已完成后端通知拆分：新登录不发布库存变化，注销只唤醒对应令牌的连接进行权限复核，用户资料修改不使库存快照失效；停用、密码重置仍立即关闭失效连接。物料通知附带上序、原下序和新下序的班组 ID，丢失仅影响所属班组；目录变化及跨班组加急保留全局通知。多次变更合并时保留所有受影响范围，保存点回滚不推送。

库存推送、共享快照与新增通知范围测试共 40 项通过，后端质量与差异检查通过。

第二项已完成前端过滤：班组工作台、来源详情和本班组收发图只响应相关班组；管理页分别响应班组目录、账号配置变化，不因转料重新加载。普通库存通知仅更新业务数据，不额外请求账号信息和完整班组目录；自己的账号或目录确实修改时仍及时重新校验。保留断线重连全量同步、跨日刷新、筛选/页码/草稿及编辑保护，继续共用一条通知连接，不改为轮询。旧通知及首次连接仍按全量处理，非法范围触发重连而不是静默漏更新。

前端相关 8 文件 / 84 项测试及 TypeScript 检查通过，包括无关班组零重查、相关班组更新、权限/目录更新、合并通知与草稿保留。以上为本地代码验证，不是新的线上性能实测。

第三项完成班组汇总和库存明细的并发合并读取。同一班组、同一接口、全部筛选条件及分页参数相同，才共享序列化结果；不同来源、类型、日期、加急、搜索字段、页码不混用。每个请求仍先独立鉴权，等候共享计算前释放鉴权事务；缓存不含用户、令牌或用户专属操作权限。最多保存 64 份结果，按需检查两秒最大年龄及工厂日期；库存提交立即推进版本，不等待两秒。回滚不失效，构建期间提交的旧结果不存入新版本，失败会唤醒其他等待请求。未修改库存计算公式、写入事务、数据库结构或 worker 数量。

使用同一脚本、433 条真实收发流程生成的隔离 SQLite 物料记录，以 `d320985` 与修复版分别通过本地 ASGI 测试客户端并发读取库房。25 个请求各自经过真实路由参数验证和鉴权，不经过网络或浏览器，仍共享一个管理员账号：

| 接口 | 构建次数：前 → 后 | SQL 次数：前 → 后（含鉴权） | 本地 P95：前 → 后 |
| --- | --- | --- | --- |
| 班组汇总 | 25 → 1 | 150 → 30 | 1,038.9 → 622.2 ms |
| 库存明细 | 25 → 1 | 125 → 29 | 1,852.2 → 384.3 ms |

两轮各接口的 25 份响应分别完全相同，无请求错误。SQL 次数分别减少 80% 和 76.8%；剩余的 25 次鉴权查询没有跳过。耗时受本机负载及 SQLite 影响，不能据此宣称线上达到某个 QPS 或人数；这轮没有访问、压测或改写正式数据库。隔离数据库和连接随脚本结束自动清理。

重现：

```bash
python scripts/measure_inventory_fanout.py --clients 25 --populated --team-reads
python scripts/measure_inventory_fanout.py --backend /path/to/previous/backend --clients 25 --populated --team-reads
```

**后续线上复测必须调整场景**：原复测用登录/注销触发库存更新，修复后这些操作不再是库存事件，不能沿用该方法验证物料推送，也不能将减少了错误通知的结果冒充真实业务容量。应在隔离 MySQL 中用真实入库、转出、接收提交触发通知，再测混合查询及端到端延迟。前后端需配套发布，旧页面重新加载程序后才会按班组过滤。

收尾补测发现并修复跨日边界：日期改变与局部库存/账号通知同时到达时，必须保留权限标记并升级为全局刷新，否则其他班组的日期筛选及机器人大屏今日数据可能漏更新。先用四项失败用例复现，修复后全部通过。

最终验证：前端全量 **71 文件 / 518 项**通过，TypeScript、ESLint、格式检查及生产构建通过；后端全量 **418 项**通过。全量启动后新增的两项热缓存账号停用测试、四项跨日测试，连同实时推送和共享读取回归另跑 **53 项**全部通过（与全量有交集，不相加）。后端质量检查及 `git diff --check` 通过。仍有既有 AnyIO 弃用警告、Vue 测试提示和 Three/详情组件分包提醒，没有据此修改无关代码。

三个主要修复分别提交为 `99733bb`、`f2f02ba`、`40c9789`，跨日保护为 `d77eeb0`。用户确认后已全部推送，前后端配套部署 `20260923T080136Z`。发布前在隔离 MySQL 中复验 433 条物料、库存矩阵与 25 路共享读取通过；正式库切换前后 29 张表一致，HTTP 验收后仅临时会话表变化。三容器 healthy，三路 SSE 首帧/心跳/注销失效以及无关登录不刷新库存均通过。未使用浏览器、没有改动正式业务数据；不能仅凭发布成功认定线上延迟达标。

## 2026-09-23 同服务器隔离 HTTP/SSE 对照

对比旧后端 `20260923T065006Z`（`0021eba`）与新后端 `20260923T080136Z`（`d77eeb0`），顺序运行，测试前各自重建相同的 **433 条物料、8 个班组**，起始全厂库存均为 **17,298 件 / 1,729.8 kg**。正式物料库不参与测试。

测试服务在同一台服务器的独立 Docker 网络中；应用限制 2 CPU / 512 MiB，临时 MySQL 8.4 限制 1.5 CPU / 1 GiB、缓冲池 128 MiB，数据仅在临时内存文件系统中。应用只开放服务器回环端口，通过本机专用 SSH 隧道发 HTTP 请求，不经过生产 Nginx。两版环境相同，但不等于生产完整链路和资源配置，不能直接与前文空库公网复测的毫秒数比较。

每个模拟客户端保持一路 SSE，约 80% 模拟班组页面、10% 库存总览、10% 机器人大屏；班组端同时读取库存明细和汇总，完成后等待 5 秒再操作。收到事件后按对应版本的范围规则重查，旧版还重读账号/班组目录，新版只重读相关业务数据。使用共享临时管理员会话，并非独立账号或真实浏览器。每档逐步接入 10 / 25 / 50 个客户端，计划稳定运行 25 秒；阶段统计包含接入期及验收开销，不是严格的稳定期分布。

每档在隔离库真实执行一次入库、一次转出、一次接收，共三次业务提交；核对库房净库存不变、轧制增加 1 件 / 0.1 kg，再检查变更通知。HTTP 分位数仅统计脚本发起的查询，不混入 SSE 长连接时长、三次写入或辅助校验；SSE 首帧单独统计。确认收到了提交通知，不等于已经测得提交至浏览器的端到端延迟。存在查询/推送错误、近期 P95 超过 3 秒或内存使用超过限额 85% 时停止，未自动重试失败请求。

### 初次中止记录与测试端调整

首次临时 MySQL 限额为 768 MiB，10 档采样达到 87.68%，在写入探针前触发内存保护；保留 `capacity-before-memory-guard.json`，不纳入改善比例。随后两版统一为 1 GiB，与生产 MySQL 的内存限额相同，未修改正式容器配置。

第一轮 1 GiB 测试中，旧版 50 档发生两条 SSE 超时；新版 10 档出现一次 `ServerDisconnectedError`，服务健康且无重启。检查发现脚本的 5 秒操作间隔与后端默认 5 秒空闲关闭时间重合，连接复用存在边界竞争风险。保留 `capacity-before-initial.json` 和 `capacity-after-connection-guard.json`；之后仅将测试客户端空闲连接保留时间设为 4 秒，两版均从新建数据重新运行。不延长请求超时、不降低保护标准，也不通过重试隐藏错误。下面最终对照只使用同一调整后的脚本结果；这次调整不是生产性能优化。

### 最终对照结果

P95 / P99 是分别覆盖 95% / 99% 查询的耗时阈值，不是平均值、最大值或并发人数。表中单位为毫秒，按最近秩计算。

| 模拟客户端 | 查询数：前 → 后 | P50：前 → 后 | P95：前 → 后 | P99：前 → 后 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 10 | 178 → 112 | 434.5 → 164.4 | 1,084.6 → 610.2 | 1,176.8 → 673.7 | 两版均完成，查询/推送无错误 |
| 25 | 414 → 290 | 577.1 → 124.1 | 1,935.1 → 599.2 | 2,189.7 → 671.8 | 两版均完成，查询/推送无错误 |
| 50 | 627 → 628 | 1,196.8 → 134.9 | 2,937.9 → 968.2 | 3,589.1 → 1,416.4 | 旧版两条 SSE 超时后中止，新版完成且无错误 |

10 档 P95/P99 分别降低约 **43.7% / 42.8%**；25 档分别降低约 **69.0% / 69.3%**。50 档旧版不是完成的稳定期样本，数值保留为中止时观察结果，不据此计算完整容量增幅。旧版只观察到 47 / 50 个客户端收到变更，新版 50 / 50；未观察到不直接等于永久丢失，旧版已在保护条件下提前结束。

请求数量不同是页面刷新范围改变的结果，不是降低了计划客户端数。25 档中，账号/目录查询各从 67 次减少到 16 次；库存明细和汇总仍各完成 129 次。单看同一个业务接口，库存明细 P95/P99 从 **2,051.2 / 2,196.7 ms** 降至 **607.9 / 671.8 ms**，班组汇总从 **1,933.1 / 2,227.6 ms** 降至 **570.5 / 660.7 ms**，并非只是混合统计中多了便宜接口。

SSE 首帧 P95：10 档 **1,508.2 → 1,670.4 ms**，25 档 **1,458.5 → 403.7 ms**，50 档 **1,703.3 → 534.4 ms**。10 档首帧没有改善，不能说所有环节一律加速。新版三档全部收到真实业务提交后的通知，合计 1,030 次查询全部 HTTP 200；每档三次串行业务写入及数量/重量校验通过，但这不是多人同时出库或同一批次竞争的吞吐测试。

旧版阶段耗时 27.3 / 29.5 / 27.3 秒，新版 27.1 / 28.0 / 29.7 秒；阶段平均请求速率分别约 6.52 / 14.03 / 22.97 RPS 与 4.13 / 10.36 / 21.14 RPS。它们受五秒操作间隔、减少无关请求和中止时刻影响，**不是最大 QPS，新版 RPS 更低不代表变慢**。未单独保存稳定期延迟样本，也没有估计多次重复实验的置信区间。

新版资源采样中，后端最高 111.8 MiB / 512 MiB、MySQL 785.9 MiB / 1 GiB，应用 CPU 最高 78.42%（Docker 多核口径）；采样不等于全时段峰值。各阶段间隔五秒，未增加生产 worker、连接池或资源限额。结论是：在这组小规模、重复查询较多的短测中，减少无关刷新和合并相同查询显著缓解了排队；不同筛选、冷查询、大量历史数据、持续写入和长期运行仍需单独验证，不能承诺“最多 50 人”或某个生产 SLA。

最终脱敏结果 `capacity-before.json`、`capacity-after.json`、三份中止/初次记录及 `read_load.py`、隔离库脚本保存在 `/opt/heatsinkrep/releases/20260923T080136Z/`，本机在对应 `output/releases/` 中留存。压测查询会话已注销，写入探针会话随临时库销毁；独立应用/数据库容器、临时数据和网络已移除，专用 SSH 隧道关闭。收尾再次核对正式库 29 张表，仅发布验收登录会话表变化，其余内容与停写备份一致；前后端版本匹配、三容器 healthy、无重启，MySQL 仍是原容器。未留下浏览器或压测进程。

## 2026-09-23 独立账号的 HTTP 并发写入验收

`scripts/verify_mysql_http_concurrency.py` 只允许 `APP_ENV=test`、主机 `validation-db`、库名 `heatsink_capacity_20260923` 的隔离 MySQL，且只能请求容器内回环 HTTP 地址；没有外部 URL 参数。本机不满足保护条件时已验证会直接拒绝运行。25 个临时账号分别通过真实登录接口取得独立会话，写入由对应班组账号发起；使用线程屏障同时发请求，不通过直接调用事务函数代替 HTTP。

当前线上镜像在 433 条种子记录上完成 9 组检查：出库竞争、丢失与出库竞争、丢失竞争、三账号重复出库、三账号重复丢失、三账号同键重复接收、不同键竞争接收、多来源出库失败整体回滚、作废与接收竞争。超额竞争只允许一方成功，其余返回预期的 409；同键重试返回同一结果，不多扣库存。重复接收只有一条 `received` 审计记录；整体失败不留下出库主记录，也不扣减原本足够的另一批库存。

九组通过，未出现 5xx；不是将预期的业务冲突 409 算作服务错误。最后独立读取原始入账、出库、丢失列，逐来源核查没有负余额，并与八个班组 HTTP 汇总及全厂矩阵核对，结果为 **17,823 件 / 1,782.3 kg**。25 个会话均成功注销。本轮约 12.4 秒，探针运行在隔离应用容器内，属于正确性验收，不作为性能容量数字。正式业务库没有写入，无需为此替换生产应用镜像。

## 2026-09-23 五分钟独立账号混合负载

`scripts/measure_mysql_mixed_load.py` 复用上述目标保护，25 个独立账号各保持一条 SSE：23 个班组端查询库存与汇总，另两条接收全厂库存和机器人大屏快照。两路班组写入依次执行入库、转出、接收、丢失；每次转出和接收均等待大屏出现**对应批次及状态**，不是把任意事件算作更新成功。查询每五秒或在相关班组通知后进行，库存明细交替读取两页；没有重试掩盖错误。两路写入并不代表 25 人同时写入。

测试当前 `d77eeb0` 后端，先完成 60 秒预检，再稳定运行 300 秒；预热五秒和 SSE 首帧不纳入以下分位数。五分钟阶段起始 469 条物料记录，完成 44 个写入循环、3,630 次业务查询，没有请求错误、推送断开或五秒内批次不可见。分位数单位为毫秒：

| 操作 | 样本数 | P95 | P99 |
| --- | --- | --- | --- |
| 班组汇总 | 1,815 | 821.0 | 1,115.8 |
| 库存明细 | 1,815 | 815.3 | 1,138.8 |
| 手工入库 | 44 | 940.1 | 1,231.3 |
| 转出 | 44 | 1,346.1 | 1,640.8 |
| 接收 | 44 | 700.3 | 894.5 |
| 丢失 | 43 | 457.1 | 679.7 |
| 提交请求开始至大屏收到对应批次状态 | 88 | 3,724.6 | 4,371.2 |

最后一个丢失请求在统计窗口之后完成，不纳入耗时分位数，仍参与最终对账。独立原始流水核算为 **18,255 件 / 1,825.5 kg**，恰好比开始增加 44 × 8 件 / 44 × 0.8 kg；所有来源余额非负，八班组汇总和全厂矩阵一致，25 个会话成功注销。MySQL 行锁等待增量 17 次、累计 991 ms；未返回死锁计数字段，不将其解释为零。

探针与应用共用隔离容器的 2 CPU / 512 MiB，临时 MySQL 为 1.5 CPU / 1 GiB；流量在容器回环网络，不含公网、Nginx 或浏览器渲染。资源采样最高为应用 391 MiB、MySQL 837.6 MiB，未触发 85% 内存保护；采样不是连续峰值。推送计时**包含 HTTP 写入处理**，不是精确的数据库提交时刻到屏幕绘制时长。这组负载与之前共享账号的短测不同，不能直接计算跨场景改善比例，也不能据五分钟结果承诺长期容量或最大 QPS。

原始记录本机保存在 `output/performance/20260923-mixed/`。本轮定位到大屏仍计算未展示的趋势、排行榜和库龄，后续优化单独验证、提交，不混入此处基线结果。

## 机器人大屏只计算实际展示的内容

`factory_overview()` 新增仅供内部调用的 `include_analytics` 参数，默认仍返回完整分析。机器人大屏关闭该分支，不再查询未展示的五组趋势、两组待接收时长、库龄、库存矩阵及四组排行榜。库存、件数、重量、类型、班组、最近批次、待接收明细和流向等字段原样保留；普通概览与分析页面继续计算完整数据。没有改变权限、写入事务、缓存失效规则、数据库结构或前端样式。

先用失败用例复现大屏调用无用图表计算，修改后 12 项专项回归通过；待接收、接收、作废、丢失状态下，轻量结果与原完整计算结果逐字段一致。另在隔离 MySQL 的同一份 **557 条物料记录**上，固定时间、无并发写入，分别导入实际旧模块和候选模块核对：完整概览内容一致，大屏全部字段一致，构建 SELECT 数从 **35 次降为 22 次**（少 13 次，约 37%）。

预热后交替运行七组，大屏直接构建的耗时中位数 **309.61 → 179.75 ms**；这只是同数据的内部构建微测，不包含 HTTP、鉴权、SSE 排队、浏览器，也不是 P95/P99 或生产容量承诺。原始记录为 `output/performance/20260923-mixed/live-queries.jsonl`。

候选版又完成 300 秒混合负载（含结束时在途请求共 301.4 秒）：25 个独立账号、两路写入、45 个完整循环、3,627 次查询，全部成功；大屏每次对应批次状态均在五秒内收到，八班组和原始流水最终对账为 **18,615 件 / 1,861.5 kg**。25 个会话全部注销。稳定窗口结果如下，单位毫秒；结束后发起的一次接收、丢失和对应推送不纳入分位数，但已完成并参与对账。

| 操作 | 样本数 | P95 | P99 |
| --- | --- | --- | --- |
| 班组汇总 | 1,815 | 763.3 | 1,095.8 |
| 库存明细 | 1,812 | 896.0 | 1,133.0 |
| 手工入库 | 45 | 1,134.5 | 1,174.2 |
| 转出 | 45 | 1,271.0 | 1,874.6 |
| 接收 | 44 | 1,064.3 | 1,195.2 |
| 丢失 | 44 | 895.5 | 1,038.1 |
| 提交请求开始至大屏收到对应批次状态 | 89 | 3,321.9 | 3,738.4 |

这轮起始已有 557 条记录，基线为 469 条，数据库继续保留先前测试的数据和缓存；因此仅作混合读写稳定性复验，**不将两轮 P95/P99 差异当作受控性能改善比例**。部分操作并未变快，不能宣称所有页面和写入都提速。MySQL 行锁等待增量 6 次、累计 965 ms；采样内存最高应用 400.2 MiB、MySQL 845.4 MiB，未触发保护，但五分钟不足以判断长期内存稳定性。两个测试进程均已退出，原始结果为同目录的 `soak-live-optimized-300.jsonl`、`resources-live-optimized.jsonl`。

最终后端全量 **436 项**通过，后端质量、探针静态检查及差异检查通过，仅保留既有 AnyIO 弃用提示。隔离应用、临时 MySQL 数据与专用网络已清理；正式库 29 张表重新核验，仅之前验收使用的登录会话表与发布备份不同，其他表内容一致。正式三容器健康、重启数零，数据库仍为原容器。本次没有启动浏览器或 SSH 隧道。

优化提交 `08f1d05` 已推送并作为后端 `20260923T090917Z` 上线，前端保留 `20260923T080136Z`。线上三路 SSE、权限保护、健康及静态资源校验通过；应用切换前后 29 张表一致，HTTP 验收后仅验证会话变化。完整脱敏验证记录另归档于 `/opt/heatsinkrep/releases/20260923T090917Z/validation-evidence.tar.gz`。上述混合负载数字来自隔离环境，**不是对正式线上进行真实物料写入压测的结果**。
