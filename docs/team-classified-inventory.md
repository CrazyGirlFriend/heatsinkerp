# 八班组分类库存明细

2026-09-18：程序提交 `a9954f3` 已推送，前后端已发布 `20260918T064256Z`。本次不修改数据库结构，不清理或重建业务数据。发布及回退记录见 [服务器部署](server-deployment.md)。

2026-09-26：按“逐页修改”先调整班组库存明细及其来源批次详情，尚未部署此改版。下方列表与接口说明已更新；文末验收、发布记录仍指 9 月 18 日版本。本轮不改全厂总览、大屏、其他业务页，也不修改数据库结构或线上业务数据。

## 列表与操作

八个班组共用一个库存明细组件，默认列为：流水号、材质/规格、类型/用途、来源（普通班组为“上序班组”）、当前结存、累计收发、最早在库接收、操作。件数、重量同时显示；相同流水号合并，材质与规格均相同才合并组合单元格。

- 按 **流水号 + 材质 + 规格 + 物料类型 + 本班组用途 + 来源** 分组。同组多次接收合计余额；不同类型、用途、规格、上序班组分别显示。用途保留开单时的名称快照，未指定时如实显示。批次号仍在二级详情，不增加 CK 单号。
- 库房保留外部来料、外部退回、车间转入及具体来源单位/班组；其他班组直接显示上序班组，库房也可以是上序。
- 默认显示有实物结存的分组，包括只有重量的物料。废料分类规则不变，不因列表统一而允许废料转入普通班组。
- 行内“明细”展示该分组的来源批次、结存、收发、待确认与丢失，含无结存历史；行内“出库”仅提供该分组有余额的批次。丢失登记在实际来源批次上办理。顶部“新建出库”继续允许跨分组选料、向同一下序多批出库。
- 累计已转出 = 已确认转出 + 转出待确认；待确认是其中一部分，不能再扣一次。当前结存 = 累计接收 − 累计已转出 − 累计丢失。状态只描述“尚未转出 / 部分转出 / 无结存”，不推断加工完成。
- 最早在库接收时间取该分组仍有件数或重量结存的批次。新料进来不重置旧料时间，旧批次清零后不再参与最早时间计算。所有批次无结存时显示“—”；在库天数不等于逾期。
- 点击流水号查看完整流水号历史，不把它与单个分组明细混为一谈。
- 保留类型、来源、材质、日期、加急、分析条件、自选列及字段搜索，新增用途与最早在库接收日期搜索。数值按分组余额查询，搜索字段临时置于首列。库存停留按当前分类的最早在库接收时间筛选，不把同流水号其他新到分类混入；其余图表事件筛选仍先选符合条件流水号。
- 普通班组列设置使用独立账号/班组键；库房沿用现有列设置。已有自选列保持原顺序和显示范围，新增列默认不插入；在“显示列 → 恢复默认 → 应用”可切换新布局。待接收量不拆摊到库存行，避免同流水号多个分类重复显示待接收总数。
- 默认每页 10 条，可选 20/50/100 条，整页上下滚动。复用现有白紫样式，不新增库存页或图表。

## 数据和权限

库存由原收发、丢失记录计算：提交转出立即扣减；内部下序接收后计入下序库存；确认不重复扣减；作废未确认批次恢复上序库存。日期用于筛选发生过流转的分组，不生成历史库存快照。实时推送保留搜索草稿、页码和已打开详情。

管理员业务只读；班组账号只能操作绑定班组。新增的共享只读接口不授予出入库权限，库房手工入库、废料去向限制和逐批确认规则保持不变。

## 接口

- `GET /api/team-materials/{team_id}/inventory`：全部班组的分类库存；参数包括 `query`、`search_field`、`search_operator`、`source_team_id`、`material_type`、`availability=current|available|scrap|all`、日期和分页。库房另使用 `receipt_source=external|return|internal`。
- `GET /api/team-materials/{team_id}/inventory/{group_id}/sources`：所选分类/来源内的已接收批次；`current_only=true` 仅保留有余额的来源。
- `group_id` 是该分组最早接收记录的 ID，不是对外批次号。查询校验该记录属于目标班组，不能拿另一班组的 ID 读取本组来源。
- 新增返回 `purpose_id`、`purpose_name`、`current_batch_count`、`oldest_received_at`。现有 `group_id` 锚定的来源查询同步纳入用途，选料不会混入不同用途的批次；件数为 0、重量非 0 仍计入在库批次数。
- 旧 `warehouse-inventory` 两个只读路径继续兼容且仍仅限库房；新前端统一使用 `inventory`。

新旧库存组件合并为 `TeamInventory.vue` / `TeamInventoryDetail.vue`，移除不再使用的 `TeamSerialOverview.vue`；后端仍复用原分组和余额查询，无需数据迁移。

## 2026-09-26 本轮验收

- 分类库存后端 25 项、相关前端 70 项测试通过；TypeScript、生产构建及修改文件静态检查通过。
- 覆盖用途分组、用途改名后的历史快照、限定来源选料、剩余批次最早时间、旧料清零后切换时间、重量型库存、字段搜索置前、自选列兼容、推送保留草稿及权限隔离。
- 独立 Chrome 检查 1440 × 900、1920 × 1080、2560 × 1440；1440 下默认表格内容宽度与容器同为 1192 px。每页 10 条，允许整页纵向滚动。
- 独立 SQLite 副本通过业务 API 插入演示料，核对同流水号“检验 / 去毛刺”、两次接收、分批转出、丢失、外部与车间来源。新入库经本地通知推送自动出现，未刷新页面；班组账号可打开对应行的出库选料。
- 本地预览只用进程内通知替身检验前端订阅链路，不代表本轮测试了生产 RabbitMQ。页面现有路由 Transition 警告未在本次库存改版中处理。
- 截图、独立数据副本及预览脚本位于忽略目录 `output/playwright/team-inventory-20260926/`。未触碰线上业务数据，未使用 Codex 内置浏览器。

## 2026-09-18 本地验收记录

- 后端完整测试 332 项、前端完整测试 430 项通过；TypeScript 检查和生产构建通过。
- 覆盖七个普通班组的同来源合并、不同类型/规格/上序分行、前导零、仅重量库存、筛选分页、跨班组来源拒绝，以及出库、丢失、确认、作废后的余额。
- 本地现有八班组分别有 30、24、28、28、28、28、28、28 条分类库存。逐组核对全部来源批次的件数/重量，再核对班组合计，全部与既有库存口径一致。只读核对，未写入新的演示物料。
- 使用 Playwright 独立 Chrome 检查库房、轧制、退火和来源明细；检查 1440 × 900、1920 × 1080、2560 × 1440、390 × 844。窄屏页面宽度仍为 390 px，宽表内部横向滚动。截图位于 `output/playwright/team-classified-*.png`。
- 浏览器未发现运行错误；仍有既有工作台多根节点导致的页面过渡动画警告。构建保留 ECharts/Three 大分包提示，未在本次分类库存修改中扩展处理。
- 检查会话 `classified-teams-0918` 已关闭，所属 Chrome/辅助进程已退出。没有使用 Codex 内置浏览器，也没有部署或修改线上数据。

## 线上发布验收

- 本地页面验收之后按用户要求发布；部署过程不启动浏览器。
- 线上八班组分别有 29、24、28、28、28、28、28、28 条分类库存。MySQL 逐组来源批次件数/重量、分类合计、班组总量及大屏一致，接口鉴权、来源/类型/数值/分析筛选通过。
- 切换前完整备份数据库；切换前后所有表行数及内容哈希完全相同，迁移版本仍为 `20260918_0014`，MySQL 容器未重建。
- 82 个公网静态文件与 12 个页面入口和构建一致；三路 SSE 首帧、真实事务通知和心跳正常。验收仅创建并注销登录会话，没有物料业务写入。
- 三容器 healthy，发布脚本退出码 0。数据库备份：`/opt/heatsinkrep/backups/pre-20260918T064256Z/database.sql.gz`。此段为部署后文档补记，不修改已验证的程序产物。
