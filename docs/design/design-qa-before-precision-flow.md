# 第一张视觉方案验收 · 2026-09-06

## 比较对象与状态

- 用户选择的原图：`docs/design/selected-style.png`，1487 × 1058 像素，无浏览器边框。
- 当前实现：`http://127.0.0.1:5179/transfer-batches`，截图 `output/playwright/selected-style/desktop-final.png`。
- 最终桌面比较使用 1487 × 1058 CSS viewport、scale=css、1:1；早期截图为 1488 × 1056，差异不用于像素级断言。补充检查 1366 × 768 和 390 × 844。
- 相同班组角色、浅色主题、全部记录状态；示例数据与原图不完全相同。日期来自本地时钟，接口操作使用隔离的内存数据。新建/作废用例操作过额外演示记录；交付前已重置本地内存数据，最终桌面截图与预览恢复初始 28 条示例记录。
- 用户随后明确指出侧栏过宽：展开宽度由 256px 改为 208px，窄桌面为 192px；属于已确认方向修正，不按原图约 266px 的宽度判定偏差。
- 源图与实现截图已在同一次图像输入中比较，见 `desktop-initial.png` 与后续 `desktop-final.png`。各页截图保存在同一目录；界面文字、图标和表格在原尺寸输入中均可读，无需依靠放大的局部图推断。

## 发现与修复记录

1. [P2，已修复] 桌面列表首轮行高约 86px，首屏只能完整显示 7 行，偏离源图的 8 行。改为 74px 左右的双行记录，业务正文提高到 15px、日期 14px。`desktop-final.png` 可完整显示 8 条。
2. [P2，已修复] 初版按钮 32px 与输入框 40px 不一致。使用 Element Plus 全局 large 尺寸统一到 40px，保留已有小尺寸与文本按钮用途。新建弹窗与主列表已复核。
3. [P2，已修复] 手机筛选栏文字被过早省略。扫码、重置改为带完整无障碍名称的图标按钮，释放班组筛选宽度。
4. [P2，已修复] 流水追踪摘要的通用 span 样式覆盖了绿色状态文字。收紧选择器，只对字段标签应用辅助色。
5. [用户反馈，已修复] 侧栏占用过宽。收窄到 208px，并同步缩小品牌字号、图标和内边距。
6. [回归，已修复] 品牌链接重复计入侧栏目的地。品牌区改为静态标识，保留 5 个真实管理导航入口；原测试无需修改。
7. [验收环境，已修复] 本地模拟接口缺少 `/team-directory`，补齐后清洁页面没有相应 404。新增从 Element Plus Box 图标原样渲染的 favicon，修复浏览器图标 404。

## 必查视觉表面

- 字体：中文系统字体栈、600 字重标题、15px 列表正文、14px 日期，辅助文字和字段标签保持清晰；手机记录保留关键数字与班组方向。
- 间距与布局：白色工作区、统一标题与筛选层级、浅边框和 4px 圆角；没有装饰性浮动卡片。抽屉和弹窗内容可独立滚动，操作区固定可见。
- 色彩：深蓝导航 #0b213e、橙色主操作 #c75200、蓝色数据链接；待接收、已接收、已作废使用橙/绿/灰并始终附带文字。输入占位文字已加深。
- 图像与图标：使用现有 Element Plus Icons（Box、Document、FullScreen 等），无手绘 SVG 替代图片。原图品牌标记按项目统一图标库落地。条码仍由 JsBarcode 输出真实 Code 128，打印仍为 A4 双联。
- 文案与内容：一期仅转料记录、扫码接收、流水追踪、班组与班组长管理；不同权限沿用现有业务。原图中未定义行为的班组互换符号未添加为虚假按钮。没有引入工单或生产报工功能。

## 已验证交互

- 新建转料：选择电镀班组，填写流水号、150 件与 25.5 kg，成功生成并打开详情。
- 待接收编辑备注成功；作废后编辑入口消失。
- 接收方整单确认成功，已接收单不再出现确认动作；待接收页签同步移除该条并更新总数。
- 扫码输入 TL 批次号加 Enter 打开对应详情；流水号查询显示完整交接链。
- 手机列表、扫码、追踪的文档宽度均为 390px；1366px 桌面无整页横向溢出。
- 手机抽屉操作区 y=788、h=40，在 844px 高屏幕内；新建表单提交按钮同样保持可见。
- 手机导航 Escape 后焦点回到“打开导航”。
- 管理员不显示新建转料入口，班组和班组长管理页与新增表单可打开。
- 全量测试 193 项中首轮 192 项通过；修复重复品牌入口后，导航 4 项与列表刷新 2 项全部复测通过，合计 193 项均已通过。TypeScript 和生产构建通过；最后样式更新后的构建和 TypeScript 检查也已通过。

## 最终补充复核

- [P2，已修复] 管理表格手机端左右固定列遮住中间姓名与班组。取消手机固定列、保留局部横向滚动并增加简短提示。证据 `accounts-mobile-initial.png` → `accounts-mobile-final.png`、`accounts-mobile-actions.png`，文档宽度保持 390px。
- `list-mobile-final.png` 两个班组筛选已能完整显示；手机扫码弹出框输入批次并回车能进入详情。
- 最终侧栏实测 208px；`desktop-final.png` 与源图在同一次原尺寸图像输入中复核，品牌、菜单、表格和底部分页完整可读。白色工作区的增加符合用户最新反馈。
- 减少动态效果偏好下侧栏 transition-duration 为 0.00001s，导航开关、Escape 和焦点恢复继续有效。
- 空状态 `empty-state.png`、模拟 503 后错误状态 `error-state.png` 已查看；恢复接口并点击重新加载可返回列表。
- 打印调用期间确认存在两联和两个真实条码 SVG，打印样式已激活；未调用物理打印机。
- 页面淡入、抽屉、表单与侧栏开关已交互验证，录屏见 `motion-final.webm`。
- 最后清洁页面控制台 0 error / 0 warning，证据 `console-final.log`。预期失败用例的 503 不混同应用异常。
- 最终构建与类型检查通过，证据 `build.log` 与 `typecheck.log`；测试首轮和修复后复测日志分别保存在 `tests-initial.log`、`tests-recheck.log`，交互断言见 `interaction-results.json`。

## 交付边界

- 当前一期全部可用页面及共享框架、表单、详情应用选定风格；访问锁保持用户此前要求的纯锁入口。
- 无遗留可执行 P0/P1/P2 发现。剩余验证边界为实体扫码枪、实体打印机，以及连接真实后端的端到端联调；本轮使用独立终端 Playwright 和本地内存接口，没有使用 Codex 内置浏览器。
- 本地预览启动方式和演示账号见 `output/playwright/selected-style/README.md`，未部署。


final result: passed

历史验收记录保存在 `docs/design/design-qa-history.md`。


## 本地预览免登录补充验收

- 按用户要求，本地 5179 预览服务增加独立启动入口；无需填写账号密码，默认进入班组视图，管理链接可直接进入班组长管理。
- 使用全新、无 Cookie/无 localStorage 的独立 Playwright 浏览器上下文验证：首次打开、刷新、管理直达、以前的 `/login` 链接均能直接进入页面，页面脚本异常为 0。证据：`output/playwright/selected-style/auto-entry-results.json` 与 `preview-auto-entry.png`。
- 免登录代码仅存在于演示目录的 `preview-vite.mjs` 和 `preview-entry.js`。普通开发入口返回原始 `/src/main.ts`，预览入口才返回演示脚本；正式登录、访问锁和业务源码没有修改。
- 该补充变更未改变已验收的页面样式或业务接口，语法检查通过。

final result: passed


## 侧栏展开与内容联动补充验收

- 按用户反馈，将侧栏、顶栏和内容改为共享网格。宽于 640px 时展开侧栏会同步缩小其余页面；手机继续使用可关闭的导航抽屉。
- 独立终端 Playwright 实测：1366px 窗口展开为侧栏 208px / 内容 1158px，收起为 76px / 1290px；850px 窗口展开为 192px / 658px，收起为 76px / 774px。过渡采样中内容左边界始终贴合侧栏，右边界始终保持在窗口内。采样用于验证几何联动，未据此判断动画帧率。
- 已查看 `push-desktop-expanded.png`、`push-desktop-compact.png`、`push-narrow-expanded.png`、`push-narrow-compact.png` 与 `push-dialog.png`。列表根据实际工作区宽度切换布局，新建弹窗仍以窗口居中。扫码页在 850px 窗口内操作区可见。
- 导航与列表相关 6 项测试、TypeScript 检查和生产构建均通过。证据保存为 `output/playwright/selected-style/push-results.json` 与 `push-tests.log`、`push-typecheck.log`、`push-build.log`。
- 本次通过指布局和交互验证；用户对整体视觉方向仍不满意，新的视觉方向尚待选择。

final result: passed (sidebar layout and interaction)
