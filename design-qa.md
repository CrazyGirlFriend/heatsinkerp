# 库房库存分组明细 · 实现验收

日期：2026-09-18。范围：已确认的库房库存明细效果图及其来源明细、筛选、出库选料；非库房工作台、打印、大屏不作视觉重设计。本地完成，未发布。

## 对照目标与证据

- source visual truth: `/Users/zhangsen/.codex/generated_images/01a05c4b-c7cd-7f71-a20f-344c6cad7f5c/exec-d84efb92-b348-4421-9a32-73d4ef1bc7a0.png`
- implementation: `http://127.0.0.1:15174/team-workspaces/1`
- 同状态对照截图：`output/playwright/warehouse-inventory-20260917-reference-final.png`
- 实际本地数据：`output/playwright/warehouse-inventory-20260917-live-final.png`
- 来源明细：`output/playwright/warehouse-inventory-20260917-detail.png`
- 来源＋类型筛选：`output/playwright/warehouse-inventory-20260917-filtered.png`
- 自选列展开：`output/playwright/warehouse-inventory-20260917-columns.png`
- 平板：`output/playwright/warehouse-inventory-20260917-tablet.png`
- 手机：`output/playwright/warehouse-inventory-20260917-mobile-final.png`

源图及主对照截图都是 1672 × 941 像素，CSS viewport 1672 × 941，截图按 CSS 像素输出，未做缩放或裁剪。另查 1024 × 768、390 × 844。对照截图用独立 Chrome 单会话；未使用 Codex 内置浏览器。

主对照 state：库房班组长、库存页、十条分组明细、000128/000129/000130 三个流水号。为使内容和源图一致，只在该自动化浏览器拦截三个只读接口响应，使用 `output/playwright/warehouse-visual-fixture.js`；没有改账号权限、向数据库录入这些示意数或提交业务单。之后清除全部拦截，恢复真实管理员只读状态并核对真实接口。实际待接收为 21 批／23 行，效果图的 12 是示例数。

full-view comparison：源图与实现截图在同一次图像输入中并列检查了两轮，不是凭代码推断。两图的表头、标签、数值、分组线和工具栏在原尺寸下均可清楚阅读，未额外裁剪放大。抽屉、过滤、自选列、移动端另有实际渲染截图。后续短流水号主对照未变化；长流水号换行的修正由实际数据和手机截图复核。

## Findings 与修正历史

1. **[P2，已修复] 第一列过宽、主标识不突出。** 初稿 `...reference-state.png` 第一列约 270 px，源图约 180 px，挤占规格和来源；正文 16 px，源图更接近 18 px。调整默认列宽、18 px 数据字体、流水号 600 字重。`...reference-final.png` 第一列约 186 px，八列表头与内容对齐，无缺列。
2. **[P2，已修复] 初稿数据行和分页节奏不一致。** 初稿真实页一屏展示数量偏少。仅库房分组表使用 11 px 垂直 padding、约 55 px 普通行和 60 px 表头，分页独立 68 px 区域；主对照完整显示十行。选择更多行后自然向下滚动，不限定内部表格高度。
3. **[P2，已修复] 长流水号截断后末位难区分。** 实际数据中的 TEST 长号不能只保留前缀。允许流水号自然换行，其他较长字段保留表格提示；`...live-final.png`、`...mobile-final.png` 可读完整流水号和末位编号。
4. **[P2，已修复] 手机统计和操作各占一整行。** 库房手机导航改为页签一行、统计与操作共一行，见 `...mobile-final.png`；横向页签、表格仍可滚动，不缩小数字来硬塞八列。
5. **[P2，已修复] 统计面板的可访问名称。** Element Plus 自动替换触发按钮 ID，原 aria-labelledby 目标失效。改用稳定的文本节点 ID；统计页有正确标题，主 tablist 仍保留键盘入口。组件回归验证通过。

## 五项必查表面

- **字体/排版：** 复用项目中文系统字体，不引入不明商用字体。库房数据 18 px、24 px 行距，数字等宽；流水号加粗，说明和管理操作较弱。源图没有可识别的独立字体文件，因此不声称精确字体复刻。长值不丢失，手机不压小正文。
- **间距/布局：** 保持 sidebar → 顶栏 → 页签与操作 → 单行主筛选 → 分组表 → 分页。无新增顶部数据卡片。宽屏一行筛选，窄屏自然换行；八个默认字段及数值居中，分组分隔线明确。顶部仍按项目既有导航宽度 200 px，不改整站。
- **颜色/tokens：** 使用现有白紫底、紫色主操作及浅紫表头；成品绿色、废料类琥珀色，所有标签有文字，不靠颜色单独表达。沿用现有 active/focus/disabled 状态，无新增动态装饰。
- **图像/图标：** 该页无新增插画；现有 logo、侧栏背景和图标系统原样复用。操作图标来自 Element Plus，不以 emoji、自绘装置替代。资产没有拉伸、裁切或模糊占位。
- **文案/内容：** 明确区分当前余额与历史入库量，来源标签不把备注猜成单位；空来源标记“未登记”。规格沿用原资料的单位，故标题为“规格”，不硬加 mm。零件数但有重量的废屑显示正常。没有把效果图的示意数保存到业务数据库。

## 有意保留的差异

- 保留既有字段搜索、数值比较、“更多”和自选列，不能为了图像相似删除用户已要求的功能，因此工具栏比效果图多一个字段选择器。
- 默认操作可见性依真实账号权限：管理员只查看并管理加急，库房账号才有出入库按钮。真实截图不能用班组长示意按钮伪装权限。
- 桌面十行允许自然滚动；有管理操作或长流水号时行高会增加，不强制截断来凑一屏。
- 当前实现的图标、主色、页签背景沿用本项目已有白紫设计系统，不改全站导航以追逐生成图的细微差别。
- 源图不含手机设计。窄屏保留横向数据滚动与固定操作列，顶部筛选和按钮可用；未另造卡片视图。

## 功能与数据验证

- 浏览器真实数据：来源“车间转入”＋类型“成品”只剩四条成品组；返回列表、统计下拉到材质归类、自选列展开/取消、来源批次明细均可用。
- 接口对账：库房分组共 30 条，合计 8,876 件／887.6 kg，与 overview 完全一致；三类来源数量之和亦一致。这是本地测试库数据，不代表正式库。
- 默认包括废料和只有重量的废屑；已出完可查询历史，但不可继续选为出库来源。
- 同流水号不同类型、规格、来源不混算；跨批次相同组求和后分页；行内出库只查对应组的可用原批次。详情含已出完历史。
- 筛选日期不截断后续扣减；所有出库提交即扣库存，确认不重复扣，作废返还。客户资料冲突显示多值，搜索可命中其中任一真实值。
- SSE 刷新保持搜索草稿与打开的详情，失败保留上次数据；迟到响应不能覆盖新班组/新筛选；查询失败、空列表、只读、数字校验、分页和自选列均有组件测试。
- 前端最终全量 **452 项通过**，生产构建通过；库房/库存后端最终定向 **23 项通过**。前期后端全量 **318 项通过**，之后新增一条资料冲突用例及收紧资料匹配范围，由最终定向回归覆盖。
- 浏览器未提交新的入库、出库或丢失；写入、确认、作废规则在隔离自动化数据库验证。未做新的线上验证或部署。
- 控制台检查：开发过程中重启后端曾出现一次请求失败，刷新恢复；最终浏览器页面未报告新增 JS 异常。已有构建包体积提示与测试依赖弃用提示不在此次改版范围。
- 最终控制台 Errors 0 / Warnings 0；自动化会话已关闭，浏览器列表为空，任务创建的 Chrome 进程 81336 已退出。保留本地预览服务，重启到最新后端后只读接口复查正常。

## 2026-09-18 独立批次补充验收

- 出库记录已改为每行一个实际批次，可按物料类型、状态与日期筛选，支持废屑及仅有重量的已出完历史。与此前整单列表的缺口已补齐。
- 每条物料各有批次号及条码，历史 CK 仅查找各批；当前页面不提供整组确认。同纸打印不是合并库存或接收状态。
- 同一独立 Chrome 会话核对真实本地数据：1920 × 1080 合并打印预览、2560 × 1440 出库列表及单批详情；未使用内置浏览器、未在浏览器提交业务写入。
- 截图：`output/playwright/independent-batches-print.png`、`independent-batches-2k.png`、`independent-batch-detail.png`。打印介质截图 `independent-batches-a4.png`：两批两个条码，实际打印内容 190 mm 宽、高约 107 mm，无横向溢出，可放一张 A4。未使用实体扫码枪或打印机验收。
- 后端全量 323 项、前端全量 440 项通过，生产构建通过；历史抽屉忙状态保护调整后，相关 7 项重新通过。保留既有包体积和测试依赖警告。独立 Chrome 无 JS 错误，存在页面多根节点过渡动画的 Vue / Element Plus 开发期警告。
- 本次独立 Chrome 会话已关闭，所记录的任务进程 4580 已退出；未关闭用户普通浏览器。

Implementation checklist：布局、分组查询、来源详情、受限选料、筛选、自选列、余额对账、自动化回归、独立 Chrome 实看完成。未发现剩余可执行的 P0/P1/P2。

final result: passed

## 2026-09-19 全链路时间画布 · 当前验收

范围：独立 `/flow-preview/chain`，按选定的浅色折线画布加入时间、滚轮缩放和平移。不替换原业务追踪页，不修改后端或库存数据，未提交、推送、部署。

### 视觉目标与证据

- source visual truth：`/Users/zhangsen/.codex/generated_images/01a05c4b-c7cd-7f71-a20f-344c6cad7f5c/exec-92229122-cacf-46be-93d6-4a8c8b280477.png`
- 本地实现：`http://127.0.0.1:15174/flow-preview/chain?sample=purposes`
- 最终主对照：`output/playwright/flow-timeline-20260919/09-desktop-final.png`
- 2K：`output/playwright/flow-timeline-20260919/11-2k-final.png`
- 窄屏：`output/playwright/flow-timeline-20260919/10-mobile-final.png`
- 真实业务数据：`output/playwright/flow-timeline-20260919/04-business.png`、`business-1366.png`、`business-2560.png`。

源图与主对照均为 1672 × 941 像素，CSS viewport 1672 × 941，截图与 CSS 像素 1:1，没有额外缩放裁切。源图和主对照放在同一次图像输入中比较；后续修复后再次以 `09-desktop-final.png` 进行完整对照。另查 1366 × 768、2560 × 1440 及 390 × 844；没有进行 4K 测试。字体、手柄和图标在主对照尺寸可直接辨认，无需裁剪后猜测。

数据状态并不相同：源图是跨天示意且含时间、班组标注不一致，不能作为业务数据；实现使用已有只读 QA 快照的真实毫秒时间。该快照仅发生库房与检验之间的三种用途业务，不伪造其他班组路径。真实业务样本另含八个班组，但历史用途未登记，保留“未分类”。因此比较布局、排版、画布和操作，不声称路径坐标或数值的逐像素复刻。源图悬浮提示为示意，实际提示按真实批次显示。

### Findings 与修复记录

1. **[P1，已修复] 初稿纵轴班组名称缺失。** `01-initial.png` 的连续轴边界使刻度落在非整数，名称全部隐藏。改为整数边界和最小间隔，`02-labels.png` 及最终图完整显示八个固定班组；外部去向独立标注。
2. **[P2，已修复] 小于一秒的快照时间刻度不足。** ECharts time scale 的秒级最小刻度不足以区分真实毫秒事件。小于一秒的范围采用真实 epoch 数值轴与时间格式器；没有拉长或捏造业务时间，最终对照可读毫秒刻度。
3. **[P2，已修复] 手柄不明显且窄屏时间滑块过短。** `06-mobile.png` 手柄几乎相邻。增加 20 px 圆形手柄，窄屏滑块独占一行、缩放按钮置于其下；最终手机图两手柄间距约 308 px，桌面仍位于同一底部工具区。
4. **[P1，已修复] 响应式配置清空折线。** 加入 ECharts media 后，原 replaceMerge 将 media 未声明的 series 清除，见 `08-desktop-final.png`。对含 media 的单系列画布保留常规合并；补充组件回归，`09-desktop-final.png`、`10-mobile-final.png` 与 `11-2k-final.png` 均恢复真实路径和事件，窄屏切回桌面正常。
5. **[P2，已修复] 绘制动画参数未生效。** 按所安装 zrender 的路径实现改用 `style.strokePercent`，逐段绘制测试取 6 帧得到 6 个不同路径状态；减少动态效果模式 3 帧保持不变。不是以整体渐显冒充画线。

### 五项必查表面

- **字体与排版：** 复用中文系统字体，标题 28 px、班组 15 px、批次数量 14 px、时间与辅助信息 12 px；数量与重量同时显示。长外部名称可截短，详情提供完整名称；密集标签避让，原批次可由查找入口访问。
- **间距与布局：** 单一白色画布、横向真实时间、纵向班组、细网格与底部时间范围；移除该预览的 KPI 卡片和重复身份行。2K 画布约占页面高度 82%，笔记本控件不溢出。保留 48 px 预览导航以切换本班组/全链路，较源图多一行，属于独立预览入口的有意差异。
- **颜色与 tokens：** 白底、浅紫细网格和紫色主操作；用途颜色与本班组预览保持同一映射，不因切换页面改变含义。未分类、作废、待确认均有文字/线型辅助，不仅依赖颜色。没有新增发光背景或装饰卡片。
- **图像与图标：** 本图是实际业务数据绘制的 ECharts SVG，不是插画替身或静态背景；缩放保持线条、文字清晰。控件采用现有 Element Plus 图标，无新增伪造素材、emoji 或截图贴图。
- **文案与数据：** 说明横轴时间/纵轴班组，快照明确非实时；待接收不补造接收时间，异常时间提示；数量是本次转料量，不当作库存累计。同一时间只沿班组行内错开，横轴不改写。

### 功能与回归

- 单个任务独立 Chrome 会话，未用 Codex 内置浏览器；窗口尺寸检查顺序执行。
- 滚轮以鼠标位置缩放、拖动平移、双击还原、按钮和键盘缩放/还原可用；滑块拖动后倍率由 100% 变为 127%。
- 支持全屏/退出全屏、折线重播、事件点与批次查找选择、路径高亮、明细时间和交接间隔。节点同时呈现件数/重量；不通过详情读数来替代默认双单位显示。
- 真实本地数据查询与只读快照分别检查；自动化没有提交入库、转料等业务写入。SSE 回归覆盖查询草稿、同一画布实例及最新记录更新。
- 前端全量 64 个文件、470 项测试通过；此后新增两个回归用例并完成最终定向 22 项测试。TypeScript 检查与生产构建通过。保留项目既有包体积、动态/静态混合导入提示，本次不作无关重构。
- 本次独立 Chrome 控制台检查：Errors 0 / Warnings 0。未做实体触控设备或超大批次压力测试；这不影响已验证的桌面鼠标交互。
- 最后复查选择批次前后倍率均为 110%，返回本班组为单个 Canvas，切回全链路为单个 SVG；全屏进入/退出正常。任务自动化会话已关闭并核对所属 Chrome 进程退出，保留本地预览服务。

Implementation checklist：真实时间定位、拆批关系、彩色折线、双单位、绘制动画、鼠标/键盘操作、时间滑块、全屏、尺寸切换、只读数据隔离、回归与构建已检查。无剩余可执行的 P0/P1/P2。

final result: passed

## 2026-09-19 Bencho 交互细节适配 · 当前验收

范围：只精修独立全链路预览的工具栏、路径反馈和增量动画。保留本班组桑基图、真实时间定位、批次来源关系、件数与重量。不新增后端接口或写入业务数据，未提交、推送、部署。

### 视觉目标与比较证据

- source visual truth：用户确认的 Bencho 实站截图 `output/playwright/bencho-review-20260919/08-toolbar-detail.png`（366 × 235）和 `07-chart-detail.png`（400 × 331）。前者参考白色悬浮工具、浅灰选中块与操作分组，后者参考细线和重点状态；不是克隆其绘图编辑器或证券数据组件。
- 既有全页布局基线：`output/playwright/flow-timeline-20260919/09-desktop-final.png`（1672 × 941）。保留导航、查询、用途图例、时间/班组画布结构。
- 最终全页：`output/playwright/flow-bencho-20260919/13-desktop-final.png`（1672 × 941）；聚焦区域：`14-toolbar-final.png`（456 × 58，平移选中）。
- 状态证据：同目录 `02-hover.png`、`08-selection.png`、`11-fullscreen-search.png`。
- 响应式：`15-mobile-final.png`（390 × 844）、`16-tablet-final.png`（1024 × 768）、`17-2k-final.png`（2560 × 1440）。本地真实业务数据：`12-business-1366-final.png`（1366 × 768，26 个批次）。没有测试 4K。
- 截图均用 `scale: css`，图片与 CSS 像素 1:1。2K 图片在工具查看时缩放显示，实际文件仍为 2560 × 1440。工具栏源图含上方形状菜单，实现没有这些无关绘图工具；比较源图下方主栏与实际工具栏，而非声称两个裁切框尺寸相同。
- 全页基线与最终全页、Bencho 工具栏与实现局部在同一次图像输入中比较；Bencho 曲线与实际悬停状态也在同一次输入中检查。用途图使用相同快照，但证券曲线不是同类业务数据，比较只限线条/端点与重点反馈。手机无源图，按控件可用性和内容完整性检查。

### Findings 与修复记录

1. **[P2，已修复] 窄屏时间说明在“北京时间”中间断行。** `07-mobile-controls.png` 出现孤立的“间”；时间范围拆成完整时间片段和独立时区文字，允许按片段换行。`15-mobile-final.png` 已无单字悬挂，工具栏两行均在屏幕内。
2. **[P2，已修复] 全屏内批次查询菜单不可见。** 实际全屏操作中可见下拉数量为 0，Element Plus 默认挂载到全屏元素之外。将批次菜单和工具提示挂到预览根元素；`11-fullscreen-search.png` 及 DOM 复查证实菜单可见、处于 fullscreenElement 内，退出全屏正常。组件回归覆盖挂载目标。
3. 工具栏首次比较符合已确认方向：白色轻边界、浅灰滑动选中态、统一 44 px 操作区域、细分组线；没有再引入重边框、发光、背景纹理或第二张图表。最终比较无剩余可执行 P0/P1/P2。

### 五项必查表面

- **字体/排版：** 保持中文系统字体与原画布 28/15/14/12 px 层级，工具倍率 13 px 等宽数字，主数据和辅助信息区分。未拷贝 Bencho 商业字体或把业务中文字压成其英文组件字号。完整批次号及双单位在提示与详情可读，窄屏时间说明按片段换行。
- **间距/布局：** 上方只保留用途图例与批次查找，底部统一 456 × 58 悬浮工具；时间滑块单独一行。390 px 下工具栏为 322 × 109，按钮仍 44 px 高。桌面、平板、窄屏没有水平页面溢出，控件不覆盖时间刻度；画布仍占主要区域。
- **颜色/tokens：** 工具使用白/中性浅灰、柔和投影、深灰图标；用途色沿用既有映射，不改变业务含义。正常转料线 2 px、悬停 3.2 px、选中 3 px；留存线更弱，关联批次以透明度区分。按钮有 hover、focus-visible、pressed、disabled 状态，不以颜色替代名称。
- **图像/图标：** 没有需要生成的位图素材。图标全部复用 Element Plus；指针/四向平移图标是相应操作的现有库图标，未照搬 Bencho 绘图工具。图形是实际 ECharts SVG，可缩放、可点选，不是静态贴图、预录视频或自绘装饰插画。
- **文案/内容：** 清楚标出选择/平移、倍率、重播、动画开关与全屏；悬停有简短提示，页脚说明 V/H。快照明确非实时，真实数据使用实际批次。只对新路径和变化数字做动画；没有为视觉充实而添加时间、班组、转料或库存值。

### 功能、数据与回归

- 单个独立 Chrome 会话顺序检查，未使用 Codex 内置浏览器。悬停线宽实测 3.2 px，点击可读批次、双单位、用途、收发时间和交接间隔；明细关闭后可保留/取消关联高亮。
- 滚轮倍率由 100% 到 120%；按钮放大到 133%，重播后仍为 133%。平移模式拖动使时间起点由 12.5% 到 4.07%，不误开详情；V 切回选择，还原正常。全屏/退出与全屏内查找均通过。
- 浏览器内模拟 SSE 事件与只读接口响应：增加一个批次并修改一个数量，原 7 个图形对象全部保留，已有路径对象不替换、strokePercent 仍为 1，新路径从 0 绘制；倍率 133% 和草稿 `UNSENT` 保持。相同快照再推送，活动动画数为 0。模拟仅在该自动化会话内，不写数据库；随后移除拦截并整页重载，确认模拟全局变量消失，再查本地真实业务 26 个批次。
- 系统“减少动态效果”实测图形 animation=false、活动动画 0；播放途中关闭动画同样立即结束，未清空画布。切换页面为一个本班组 Canvas、无悬浮工具，返回为一个全链路 SVG。
- 前端全量 **64 文件 / 478 项通过**；全屏挂载与时间断行修复后，最终定向 **28 项通过**，TypeScript 与生产构建通过。保留已有大包体积、混合导入和两处无关测试警告，未作无关重构。
- 浏览器最终控制台 **Errors 0 / Warnings 0**。未做实体触控设备、极大量批次性能压力或线上验收，不将浏览器内模拟推送说成线上业务已执行。
- 自动化会话 `heatsink-bencho-refine-0919` 已关闭；所属 daemon 77896、Chrome 77898 及对应临时 profile 进程均核对退出。未关闭用户普通 Chrome，保留现有本地前后端服务。

Implementation checklist：参考比对、悬浮工具、选择/平移、悬停/高亮、增量更新、重播保留视图、减少动态效果、全屏菜单、响应式、前端回归和构建均完成。无剩余可执行 P0/P1/P2。

final result: passed

## 2026-09-19 本班组收发画布与管理员全链路分离 · 当前验收

范围：按本次确认图将班组收发历史改为单一时间画布；全链路放到管理员入口，并限制前端路由和后端接口。保留既有业务操作和全链路画布，不提交、推送或部署。

### 视觉目标与比较证据

- source visual truth：`/Users/zhangsen/.codex/generated_images/01a05c4b-c7cd-7f71-a20f-344c6cad7f5c/exec-eae8e8d0-ccde-44c7-8388-96802bb0ce4d.png`。
- 本地真实数据入口：`http://127.0.0.1:15174/team-workspaces/8?tab=history&serial_no=TEST-20260911-001`。
- 主对照：`output/playwright/team-timeline-build-20260919/19-clean-final.png`；局部工具栏：同目录 `source-controls-crop.png` 与 `20-controls-final.png`。
- 响应式：同目录 `14-2k-final.png`（2560 × 1440）、`15-laptop-final.png`（1366 × 768）、`09-mobile.png` 与 `17-mobile-controls.png`（390 × 844）。
- 真实数据及详情：`18-real-final.png`、`12-real-batch-detail.png`；管理员全链路：`13-admin-chain.png`。

源图与最终桌面截图均为 1672 × 941，截图采用 CSS 像素，无额外裁切或缩放；源图与实际完整页面在同一次图像输入中比较，源图工具栏裁切与实际工具栏也同次检查。2K 文件保留原始尺寸，查看工具缩小显示。不做 4K 测试。

为准确比较三种用途、线条和位置，主对照使用浏览器内只读 fixture（`mockup-fixture.json`）匹配示意的时间和数量；它不是实际库存，也没有写入数据库。检查后移除接口拦截并恢复真实页面。实际旧测试记录未登记用途，所以显示“未分类”，不为好看补造业务分类。源图是班组账号，实现截图使用管理员，所以既有只读标识、系统设置等权限差异有意保留；日期控件沿用现有 Element Plus 组件。工具栏比源图多动画开关。

### Findings 与修复记录

1. **[P1，已修复] 首次挂载为空白画布。** 图表实例初次 `getOption()` 尚无值，捕获缩放状态时异常；改为可选读取并补回归，真实页面、示意页面和后续切换均能绘制。
2. **[P1，已修复] 用途名称未显示。** 连续轴采用分数边界时不生成对应整数刻度；用途名称由实际批次行绘制，固定留出左侧标签区域，重复用途仍保留独立来源批次。
3. **[P2，已修复] 初稿顶端留白过多、轨道过窄。** 调整画布边距与行范围，对照后收进点、收发标签和余额上下层次接近源图；右侧留足双单位宽度，收进标签不会被避让规则隐藏。
4. **[P1，已修复] 日期区间的期初可能被误读为原收进量。** 区间开始前的已入账变动计入期初；日期内显示期初、真实增减和期末结存，顶部当前库存保持独立。不能把历史期末当成当前库存，数量与重量都做回归断言。
5. **[P2，已修复] 重播/更新丢失缩放及长历史缩放不足。** 同一图表更新保留视图；最小缩放范围随真实时间跨度变化，支持查看短时密集事件，不人为拉长时间。大倍率用紧凑文字显示，完整倍率放在提示内。
6. **[P2，已修复] 图例和密集事件细节。** 缩小圆点样式作用域，避免将丢失图标变成圆形；同一时间仅纵向错开，重叠标签适当隐藏但保留节点提示与查找入口。全屏打开批次前先退出全屏，避免抽屉被挡住。

### 五项必查表面

- **字体/排版：** 标题 28 px，图内事件标题约 16 px、件数/重量 15 px；沿用中文系统字体，数据、用途、时间和辅助说明分层。长名称截短但提示保留全文。
- **间距/布局：** 一行查询与当前库存，下方一张白色画布；按用途及来源批次分轨，不合并同用途的不同来源。没有额外 KPI 卡片或第二张图。桌面页脚工具完整可见，笔记本允许自然下滚；手机控件可见，宽时间图在自身容器横向滚动。
- **颜色/tokens：** 用途沿用项目既有映射；收进实心、转出空心、丢失红叉，除颜色外还有形状和文字。细线、轻网格、浅灰选中块、白紫页面，无新增装饰背景。
- **图像/图标：** ECharts SVG 根据真实批次、时间、件数和重量绘制，可缩放交互；没有把示意图当静态成品。图标复用 Element Plus，不新增位图素材。
- **文案/数据：** 上方当前库存与画布截至时间分别说明；未接收的批次提示尚未计入库存。已耗尽批次在最后事件结束，有剩余的延续到截止点。收发与余额均显示件数和重量，不只显示单个单位。

### 功能、权限与回归

- 独立 Chrome 单会话顺序检查，未使用 Codex 内置浏览器。真实页面滚轮 100% → 120%，还原回 100%；按钮放大到 133% 后重播保持该倍率。平移和选择模式、全屏进出及节点查找可用。
- 选择真实 `TL20260914000394` 批次可打开对应详情；全屏内选择先退出全屏再开详情。管理员正式全链路入口可读取真实样本的 26 个批次。
- 绘制动画连续 10 帧出现 10 个不同路径几何状态；系统减少动态效果时 animation=false，10 帧保持同一几何，不以整图渐显冒充画线。
- 2K、1672 和 1366 尺寸核对双单位、所有示意事件及余额可读；390 px 下页面 clientWidth 与 scrollWidth 都为 390，宽画布只在内部滚动，页脚工具可通过正常下滚访问。
- 真实本地样本两批当前合计 54 件／5.4 kg，与画布 44 件／4.4 kg 和 10 件／1 kg 一致。示意 fixture 与真实业务截图分开，不把示意数据声称为实时库存。
- 班组导航不显示全链路；直接访问全链路地址也受限，旧带 team_id 的追踪链接回到本班组历史。API 未登录 401、班组角色 403、管理员 200，由隔离后端测试验证；没有扩大其他管理页面的既有重定向规则。
- 前端全量 **67 文件 / 491 项通过**；后续细节与新增缩放回归由最终定向 **3 文件 / 19 项通过**覆盖（未将其声称为新增用例后的全量重跑）。后端收发历史/权限定向 **26 项通过**。最终 TypeScript 与生产构建通过，`git diff --check` 通过。
- SSE 保持原刷新接线，组件测试覆盖已提交搜索与草稿、更新和图表实例；本轮没有通过业务写入触发线上推送，也没有做线上验收。所有写入逻辑测试使用隔离数据库。
- 浏览器无新 JS 错误；班组页保留 3 条既有 Vue 多根节点 Transition 开发期警告。构建保留大包及动静态混合导入提示，后端保留依赖弃用警告。未做实体触控设备、极大量批次性能压力或 4K 验证。
- 自动化会话 `team-timeline-build-0919` 已关闭，daemon 84964、Chrome 84965 及对应临时 profile 进程均核对退出；未关闭用户普通浏览器，保留本地前后端预览服务。

Implementation checklist：确认图完整与局部比对、按用途/来源批次时间轨道、历史期初期末、双单位、绘制动画、缩放/平移、节点详情、全屏、减少动态效果、管理员隔离、响应式、回归与构建完成。

final result: passed

### 2026-09-19 发布前复验

按用户“提交部署”的新请求重新运行全量：后端 358 项通过；前端初次并发运行 6 项超时，改为单 worker、顺序文件运行后 67 文件 / 492 项全部通过，未放宽断言或测试超时。沿用已通过 TypeScript 与生产构建的同一应用代码。没有重新打开浏览器；线上迁移和发布结果另记于 `docs/server-deployment.md`。

## 2026-09-20 全链路时间泳道与真实接口

本轮范围：将用户确认的单一时间泳道接入现有管理员全链路接口。只读展示，不创建业务数据；未提交、推送或部署。

### 比对目标与证据

- Source visual truth：`/Users/zhangsen/.codex/generated_images/01a05c4b-c7cd-7f71-a20f-344c6cad7f5c/exec-0ea6bb1d-4291-4b41-bcef-bf3a8bf725b5.png`。
- Implementation：`/Users/zhangsen/Documents/code/python_code/HeatSinkREP/output/playwright/chain-swimlane-20260920/05-hover-final.png`；原图与最终实现均为 **1672 × 941** 像素，CSS viewport 同尺寸，截图使用 `scale: css` 统一为 1 像素/CSS px，不包含浏览器边框。
- State：管理员、白紫主题、流水号 `000012`、同日 08:00—18:00、8 个班组，悬浮“退火 → 研磨”。为了对齐参考时间和分批场景，只在任务独立 Chrome 内拦截这一条只读查询，明确标注“示意数据”；未导入数据库。真实业务 API 另外验证，不将示意图声称为实时数据。
- Full-view comparison：先将参考图与 `02-reference-matched.png` 放在同一图像输入比较，再与 `05-hover-final.png` 同输入复验。最终头部、图例、八条泳道、上下时间轴、滑块和悬浮工具位置保持一致。1672 px 原尺寸中标题、单位、节点与悬浮正文均可清楚读取，不需要额外裁剪才能判断这些细节。
- Responsive evidence：同目录 `08-laptop.png`（1366 × 768）、`09-mobile.png` / `10-mobile-controls.png`（390 × 844），以及动画完成后的 `11-2k-settled.png`（2560 × 1440）。未做 4K 检查。
- Live evidence：`13-live-density.png` 为真实本地接口 `TEST-20260911-001` 的跨天、多批次同班组画布；未修改这些记录的时间来让图形更好看。

### Findings 与修正历史

1. **[P1，已修复] 缺少可核对的历史停留余额。** 仅用当前结存无法判断转出前后各有多少料。接口补充批量加载的变更历史和 `observed_at`，画布按转出、改量、作废和丢失重建区间，并与台账核平。待接收只扣上序，不创建下序停留；数据不完整时提示而非推造。
2. **[P2，已修复] 初版缺少底部刻度，时间网格被泳道遮住，标题偏小。** 初次同尺寸比较见 `02-reference-matched.png`。修正图层、上下同步刻度与 28 px 标题，缩小时间两端无效留白；`04-refined.png` 和最终 `05-hover-final.png` 复验。
3. **[P1，已修复] 悬浮停留段会显示整批转料量。** 实际浏览器证明 ECharts tooltip 不带子图形的 `info`，原先单元测试模拟了不真实的参数。每个停留段改为独立数据项，测试对应真实 series 参数。浏览器验证同批 11:00 的本段结存 100 件 / 10 kg、13:00 的本段结存 30 件 / 3 kg；两处当前结存均为 30 件 / 3 kg。
4. **[P2，已修复] 悬浮字号偏小、细线难命中。** 改用转义后的 HTML 提示，正文 15 px、数量 17 px，长内容可换行/滚动；增加透明线段命中区域。最终 `05-hover-final.png` 保留上下序、双单位、真实时间、用途和当前结存。没有恢复抽屉。
5. **[P2，已修复] 真实密集记录标签互相覆盖、跨天日期重复。** `12-live-final.png` 暴露多条并行库存的标签落在邻轨，以及半日刻度显示相同日期。多轨标签放回自己的轨道，文字位于线条上方；密集转料标签缩小时隐藏，仍可悬浮和定位。日级视图最小刻度一天。`13-live-density.png` 中各批余额清楚分离，日期不再重复。
6. **[P2，已修复] 窄屏纵向空间不足。** 390 px 下保留 680 px 图面并允许下滚，工具分两行，不将八个班组压成重叠文字。四个验收宽度均无页面横向溢出；见手机两张截图。

### 五项必查表面与有意差异

- **字体/排版：** 使用项目中文系统字体 PingFang SC / Microsoft YaHei；桌面标题 28 px、泳道名 17 px、数量 15 px，提示中数量略加粗。字体渲染不是生成图内嵌文字，数字和完整流水号可选择/查询。
- **间距/布局：** 76 px 查询头部、一行图例、单张画布，无汇总卡、第二张图或长篇说明。2K 与笔记本可见底部工具；手机自然下滚。提示靠近鼠标并限制在图内，位置不固定在参考图的空白右上角，此差异是实际悬浮交互需要。
- **颜色/tokens：** 浅紫停留条、白紫交替泳道、细虚线网格；用途颜色继续使用现有业务映射，不照抄效果图里的随机配色，避免与本班组图产生同名不同色。空心转出、实心接收与文字同时表达语义，不只依赖颜色。
- **图形/图标：** 真实数据驱动 ECharts SVG，缩放不变糊；工具复用 Element Plus 图标。图中没有位图插画资产，也没有把整张效果图当背景。当前结存端用竖线收尾，避免参考图的实心圆被误认为新接收事件。
- **文案/数据：** “在库停留”不是加工耗时；“本段结存”与“当前结存”分开。实心入库点、真实历史分段及入库用途是业务语义所需。保留日期、双单位、待接收去向、历史异常提示。演示与真实数据明确区分。

### 功能与验证

- 浏览器实际验证：滚轮 100% → 120%，拖动平移有效，重置回完整视图，定位批次缩放到 1560%，全屏正常进出。悬停可查看不同区间，点击仅高亮真实关联路径，未创建抽屉；跨天视图继续支持放大至秒/毫秒。
- 绘线动画采集 24 帧得到 19 个不同 `strokePercent` 状态；减少动态效果设置下，浏览器媒体条件为 true、图表 animation 为 false。没有用整图淡入冒充路径绘制。
- 真实本地 API 返回 26 个批次、26 份历史；逐段重建无未核平批次。所有当前停留段合计 **770 件 / 77 kg**，与接口库存汇总一致。UI 验证没有创建/修改业务数据，示意拦截已移除。
- 前端全量 **69 文件 / 500 项通过**；最后密集标签和日期刻度修正后，定向 **4 文件 / 43 项通过**。最终 TypeScript 和生产构建通过。后端全链路、库存、班组业务相关 **36 项通过**，使用隔离测试数据库。
- 空态、精确流水号及前导零、时间异常、权限、查询草稿与 SSE 更新保留原接线并由回归测试覆盖。本轮未通过线上业务写入触发推送，也未执行线上验收或大规模压力测试。
- 最后业务页控制台检查 **0 errors / 0 warnings**。测试中的既有路由转义/扫描页 mock 警告、构建中的大包与混合导入提示、后端依赖弃用警告保留，未扩大修改范围。
- 仅使用独立 Chrome 会话 `chain-swimlane-0920`，未使用 Codex 内置浏览器。已关闭会话并核对 daemon 10193、Chrome 10196 和临时 profile `LnMzLa` 无残留进程；保留本地前后端服务。

Implementation checklist：确认图同尺寸复验、真实接口、可核平停留区间、分批/回流/丢失/作废、双单位、悬浮详情、批次定位、跨天缩放、绘线动画、减少动态效果、2K/窄屏、测试和构建、浏览器清理完成。无待处理 P0/P1/P2；海量批次与实体触控设备属于后续测试范围。

final result: passed

### 2026-09-20 全链路发布前复验

应用提交 `eb7b903`：前端全量 69 文件 / 500 项、后端全量 361 项通过，TypeScript 与生产构建通过。前端继续采用单 worker 顺序执行，没有重新启动浏览器。构建和测试中的既有告警未扩大修改范围。

新镜像在正式切换前只读核对线上 MySQL 的 25 个流水号、433 个批次、645 条审计事件，实际结存、丢失与历史事件一致。使用同一提交的前端归一化及时间画布模型处理这些快照，得到 485 个停留区间、0 个未核平批次，当前区间件数及重量逐流水号匹配台账。没有导入或修改物料数据。正式发布与备份结果见 [服务器部署记录](docs/server-deployment.md)。

## 2026-09-24 FlowERP 风格首页本地实现

范围：用户确认单张首页效果图后，将其实现为现有 Vue 首页，精修字重并将右下角快捷入口改为“重点关注”。本轮只做本地实现与验证，未提交、推送或部署。

### 比对目标与证据

- Source visual truth：`output/imagegen/home-flow-20260924/homepage-flow-v1.png`，1487 × 1058。
- Implementation：`output/playwright/home-flow-20260924/desktop.png`，CSS viewport **1487 × 1058、DPR 1**。另检查 1920 × 1080 和 390 × 844，证据为 `wide.png`、`mobile.png`。
- 全图对照：`comparison-full.png`；字体、数字和表格局部对照：`comparison-detail.png`。两者均在同一图像中展示参考与实现，已经人工查看。
- 真实业务交互使用现有 API、独立 SQLite 演示库：8 个班组，在库 17,298 件 / 1,729.8 kg，内部在途 1,648 件 / 164.8 kg，待接收 184 批。这些是本地种子数据，不代表线上库存；页面角标明确标记“本地预览 · 演示数据”。
- 为公平比较图表比例，对照截图仅在任务浏览器中替换只读 SSE 快照，使用确认图对应的 17,310 件 / 1,731 kg；未写入数据库。`empty.png` 另验证零库存、零待接收，保留独立的今日接收计数。完成后移除浏览器替换。
- 有意差异：保留现有正式公司标识；侧栏按此前要求收窄为 184 px；右下角按本轮方案改为待接收班组 / 在库加急；类型占比由真实数据决定，不补造分类数据来填满环图。

### Findings 与修正

1. **[P2，已修复] 中部卡片过高，底部内容超出确认图的首屏。** 压缩指标区和班组表格间距，重点关注列表独立滚动。最终 1487 × 1058 可见四条转料记录及页脚，见 `desktop.png`。
2. **[P2，已修复] 390 px 下隐藏的操作列表头产生页面横向溢出。** 改为表头 `aria-label`，表格仅在自身容器滚动。最终断言 `document.scrollWidth === innerWidth === 390`；记录于 `checks.json`，不能用修复前诊断文件作为通过证据。
3. **[P1，预览环境已解决] 本机未运行 RabbitMQ 时实时接口无法建立。** 独立预览入口限定指定 SQLite 文件，用进程内通知传输支持现有 REST / SSE；正式后端和 RabbitMQ 配置未改。此验证不覆盖生产 RabbitMQ 链路。

### 五项必查表面

- **字体/排版：** 自托管 Inter 与中文系统字体；标题 26 px / 550，模块标题 17 px / 550，正文 13–14 px。降低字重，件数和重量分层并按列对齐，长批次号保留完整提示和真实查询参数。
- **间距/布局：** 四项指标、班组库存、物料类型、待接收转料、重点关注；小屏指标改两列、内容纵向排列。侧栏折叠释放正文宽度，移动导航沿用现有可关闭抽屉。
- **颜色/tokens：** 浅灰绿背景、白色卡片、细边框、克制绿色主色。变更限定首页，未覆盖其他业务页主题。
- **图形/图标：** 原生 meter 展示相对重量，ECharts 展示类型重量比例，复用 Element Plus 图标和现有公司标识。系统减少动态效果时停止图表动画；没有新增静态整页图片或伪数据图表。
- **文案/数据：** 页面全部模块来自同一权威快照；在库、内部在途、待接收分别计数。全厂和每班组分类都可查看件数及重量，保留余料、废料与未知类型并核平。部分接收注明表内是整批数量；近期无待接收而历史仍有时，提示查看全部而不误报已全部接收。

### 功能与验证

- 独立 Chrome 顺序验证真实数据首帧、分类弹窗、Escape 关闭、侧栏折叠、关注类别切换、班组库存 / 待接收入口、批次详情抽屉、待接收筛选和移动导航。浏览器业务检查 **0 errors**，最终 `checks.json` 为 passed。
- 定向前端 **3 文件 / 22 项通过**：首页 9、侧栏 6、应用导航 7。覆盖快照更新、过期响应、卸载清理、页面隐藏、断线保留上次数据、初次失败、减少动态效果、空态、异常班组、双单位和部分接收。
- TypeScript 与生产构建、ESLint、格式检查、`git diff --check` 通过。保留原有动态 / 静态混合导入构建提示；未修改后端业务代码，未声称做过后端全量测试或线上验收。
- 最后一个任务自动化 Chrome PID **14410** 已关闭，`browserExited: true`，进程查询确认无残留；前序任务浏览器也已清理。没有使用 Codex 内置浏览器，没有关闭用户普通浏览器。
- 保留本地预览 `http://127.0.0.1:15184/`（自动进入演示管理员），API `http://127.0.0.1:18194/`。本地预览入口、数据库、截图和日志均位于被忽略的 `output/playwright/home-flow-20260924/`，不进入正式构建。

Implementation checklist：确认图全图与局部对照、中文字体、真实接口与跳转、双单位分类、空态与断线、响应式、定向测试、构建及浏览器清理完成。无待处理 P0/P1/P2；生产消息链路、海量数据压力和实体触控设备不在本轮验证范围。

final result: passed

### 2026-09-24 补正：普通笔记本首页一屏适配

用户指出实际窗口仍需下滚。上轮仅在较高画布上确认首屏，验收尺寸不足；不能据此认为普通笔记本已一屏适配。

- **[P2，已修复] 固定卡片高度导致桌面整页纵向滚动。** 修复前 1366 × 768 下，正文可用高度 712 px，内容高度 994 px。现在根据可用视口高度分配两排模块，调整指标卡和垂直留白；低窗口将环图与分类表并排，班组数据保持 14 px。没有缩放整页、隐藏班组或截掉底部模块。
- **[P2，已修复] 紧凑首版在 1280 × 720、1920 × 900 下第四条待接收被容器截断。** 重新分配上下两排空间并调整紧凑高度阈值；最终常用尺寸四条完整可见，自动化逐条检查行底部是否落在容器内。
- 修改仅涉及首页响应式 CSS。宽度低于 1100 px、极低窗口和手机保留自然滚动；650 px 高的桌面仍完整显示所有模块，但待接收记录可在卡片内滚动。连接 / 核平告警出现时优先完整展示，允许页面向下延伸。
- 最终独立 Chrome 检查 **10 组尺寸**：1100 × 700、1280 × 720、1366 × 700 / 768、1440 × 800、1920 × 900 / 1080、1487 × 1058、1366 × 650、390 × 844。前九组均满足正文 `scrollHeight === clientHeight`，八个班组可见；除 650 px 高窗口外，桌面四条待接收均无裁切。手机无页面横向溢出。
- 同时验证分类弹窗及 Escape、折叠侧栏后正文重新分配宽度、关注类别切换 / 空态、批次详情跳转；浏览器 0 errors。
- 证据目录：`output/playwright/home-fit-20260924/`。`checks.json` 保存逐尺寸高度与可见性断言；尺寸命名 PNG 为实际演示接口截图。`comparison.png` 将确认稿与 1487 × 1058 实现同时展示，已人工查看；另逐张查看 1100 × 700、1366 × 768、1440 × 800、手机截图。业务数据沿用隔离演示库，未为图形改写数据。
- 字体、图标、绿色配色、模块内容和已确认的大尺寸结构保留；此次有意改变低窗口布局来满足一屏目标。TypeScript、生产构建、ESLint、格式检查、diff 检查通过；此次为 CSS 调整，没有声称重新执行上轮 22 项逻辑测试。
- 最终任务浏览器 PID **15683** 已关闭，`browserExited: true`。前序诊断浏览器已清理；一次非交互终端无法接收输入，向本任务 Node PID 15261 发送 SIGINT 后核对其 Chrome PID 15269 退出，再继续验证。没有使用 Codex 内置浏览器。保留原本地前后端服务，未部署。

final result: passed

### 2026-09-26 首页发布前复验

按用户“提交部署”的请求重新执行前端全量：**71 文件 / 517 项通过**，单 worker 顺序运行。TypeScript、生产构建、ESLint、格式及差异检查通过。此轮没有重新打开浏览器；沿用上述十组尺寸与交互验收的同一应用代码。只发布前端，正式配置与登录行为保持原样，不包含本地自动登录入口、演示数据库或进程内通知替代。服务器切换和公网验证结果另记于 `docs/server-deployment.md`。

## 2026-09-26 普通业务页样式交接与复核

用户明确分工：“热沉物料系统 · 前端专员”负责页面内容，本任务负责样式。接收该任务的 24 个未提交样式文件，保留其已确认的首页浅灰绿方向：背景 `#f6f8f7`、白面板、主色 `#337d4d`，普通业务页使用 Inter 与中文系统字体；侧栏 184 / 64 px、顶栏 56 px、面板圆角 12 px。内容结构、字段、权限、接口和库存算法未改动；用途/物料类型的语义配色及机器人大屏独立玻璃样式保留。

### 复核与修正

- **[P2，已修复] 手机库存固定操作列遮占内容。** 760 px 以下将操作列恢复为随表格横向滚动，显示水平滚动条；桌面仍固定。390 px 初始视图可读取流水号和材质，滚至右侧后明细/出库操作可达，没有继续缩小字体。
- **[P2，已修复] 库房班组账号手机端按钮换行失衡。** 有“班组设置”时让入库、出库、刷新共用完整操作行；管理员只读状态维持原布局。390 px 三个操作按钮最终同一纵坐标 176 px。
- 浅色分析图表字体补齐为与业务页一致的字体栈；深色图表仍使用原字体，并更新对应字体回归断言。业务数据断言没有放宽。
- 核对 1440 × 900 库房、侧栏展开/收起、库房五个记录/历史页签、登录页、库房入库与出库表单、来源选料和合并打印预览；390 × 844 核对表格横滚、表单内容滚动及固定底部操作。表单均取消，未提交业务单据。
- 1152 × 720 用于模拟 1440 × 900 在 125% 缩放时的可用布局面积；这不是原生浏览器 125% 缩放/实体设备验收。表格保持自身横向滚动，没有页面横向溢出。

### 证据与边界

截图和日志在 `output/playwright/system-style-20260926/`（本地忽略目录）。本次人工查看：`warehouse-1440-final.png`、`sidebar-collapsed-final.png`、`mobile-inventory-final.png`、`mobile-inventory-right-edge.png`、`warehouse-125-percent-layout.png`、`outgoing-stable.png`、`login-1440.png`、`login-mobile.png`、`warehouse-intake-form.png`、`intake-mobile.png`、`outbound-form.png`、`outbound-form-mobile.png`、`outbound-mobile-bottom.png`、`print-preview.png`、`access-lock.png`、`access-mobile.png`。早期 resize 中间帧或标签尚未稳定的截图不能代替带 final / stable 的最终证据。

页面读取隔离演示数据库；正常登录和表单用真实演示账号。仅在测试浏览器中替换本地自动登录入口，以显示普通登录页；访问锁样式使用只读访问状态 fixture，未执行解锁请求。没有修改正式鉴权、配置或生产数据。打印只检查预览及样式隔离，未调用实体打印机；全链路仅沿用交接的空态截图，未宣称所有复杂图表数据状态都已验收。

前端单 worker 全量 **72 文件 / 523 项通过**（`serial-tests.log`）；随后字体断言和手机导航样式补正，定向 **2 文件 / 8 项通过**（`final-style-tests.log`）。最终 TypeScript、生产构建、ESLint、格式检查及 `git diff --check` 通过。独立 Chrome 无新增 JS 错误，保留既有 Vue 多根节点 Transition 开发期警告；没有使用 Codex 内置浏览器。

自动化会话 `stylehandoff26` 首轮 Chrome PID 35940 已关闭并核对退出；补查复用同名会话，结束后关闭 Chrome PID 37121。用户普通浏览器及原有本地预览服务保留。本轮只作本地样式提交，未推送或部署；内容任务后续调整模板后需要再次核对对应页面。

验收结论：本轮样式交接及上述窄屏修正通过；不等同于全部业务内容或全部设备验收。

## 2026-09-26 班组三级侧栏导航

按用户确认，将班组工作区横向页签移到“班组工作台 → 八个班组 → 功能”三级侧栏，保留八个班组名称。同一时间展开一个班组，库房额外显示入库记录；移除页内重复页签及库房统计下拉，操作按钮仍在内容区上方。共用功能配置处理高亮、标题、旧链接别名与查询参数；待接收角标复用当前页面的实时概览，不增加班组统计接口请求。没有修改字段、库存计算、后端、写入权限或生产数据。

- 前端单 worker 全量 **72 文件 / 526 项通过**，最终 TypeScript、生产构建、ESLint、格式和差异检查通过。覆盖八班组层级、唯一展开、查询参数高亮、旧链接、返回、收起 / 展开、权限、角标归属及移动端焦点。
- 独立 Chrome 检查 1440 × 900 展开与浮出三级菜单、1366 × 768 低窗口和 390 × 844 手机导航；八班组节点保留，页面没有横向溢出，较矮侧栏内部滚动。点击库房入库记录、研磨待接收、浮出菜单研磨出库记录、检验库存明细均到达对应页面。手机点击功能后收起导航，焦点回到正文；顶部位置提示同步。
- 人工查看 `output/playwright/team-navigation-20260926/desktop.png`、`compact-levels.png`、`mobile-open.png`、`short-desktop.png`。截图来自原有隔离预览服务及演示数据库，未提交业务单据。最后低窗口截图主要验证导航可达性，不作为库存数据加载完整性的依据。
- 浏览器无 JS 错误，保留原有 TeamWorkspacePage 多根节点 Transition 开发期警告。初次折叠后沿用旧元素引用、间隔操作浮出菜单导致两次自动化定位失败；重新取快照并连续执行悬停 / 点击后通过，没有为此改动业务功能。
- 使用 Playwright 技能及单一独立 Chrome 会话 `team-nav-20260926`，未使用 Codex 内置浏览器。结束后关闭会话，核对守护进程 41862 和 Chrome 41863 均退出；用户浏览器、其他任务的浏览器及预览服务未关闭。

本轮仅本地提交，尚未推送或部署。

## 2026-09-26 移除业务页顶部重复区域

按用户确认，桌面展开侧栏时取消全局日期、面包屑和班组页面单独的操作行；账号与退出登录、大屏入口移入侧栏固定底部。侧栏收起时保留 40 px 当前页面提示，手机保留 48 px 导航栏。班组操作通过插槽合入库存、材质、收发历史及各记录页的工具栏，移除常驻“仅查看”标签，但不改变创建、接收、班组设置等权限或库存字段。移除相应的日期定时器和废弃头部样式。

- 全量前端 **72 文件 / 528 项通过**，最后定向回归 **3 文件 / 44 项通过**；首轮并行运行构建与定向测试时有一项超过默认 5 秒，限制测试并发后完整重跑通过，未放宽超时或断言。最终 TypeScript、生产构建、ESLint、格式检查通过。
- 独立 Chrome 验证 1440 × 900、1366 × 768、390 × 844：展开、收起、手机导航及返回焦点正常，无页面横向溢出。1440 管理员库存页工具栏高 60 px、表格顶端 81 px；扫码与筛选同一行。按钮较多的班组账号及窄窗口正常换行，不缩小字体强塞。
- 使用隔离演示账号实际验证侧栏退出登录、库房班组重新登录、打开/取消手工入库、打开/取消出库选料、打开/关闭班组设置；材质归类和收发历史保留单组操作按钮。未提交业务单据、未写生产数据。
- 最终人工查看 `output/playwright/header-collapse-desktop.png`、`header-collapse-mobile.png`、`header-collapse-mobile-nav.png`、`header-collapse-warehouse-final.png`；另留 `header-collapse-compact.png` 和 `header-collapse-short.png`。初始库房加载中截图与账号菜单动画中间帧不作为最终内容验收依据。仅把预览脚本注入的“演示数据”角标挪到截图右下角，避免遮住账号，未修改应用内容。
- 浏览器无 JS 错误，保留既有 TeamWorkspacePage 多根节点 Transition 警告；HMR 后一次旧元素引用失效，重新取得快照后继续。没有使用 Codex 内置浏览器。
- 会话 `header-collapse-20260926` 已关闭，核对守护进程 45032、Chrome 45036 均退出；没有关闭用户浏览器、其他任务或共享预览服务。

本轮仅本地提交，未推送、未部署。

## 2026-09-26 侧栏展开层级优化

按用户确认，仅调整班组三级菜单的展开样式：一级右侧箭头弱化，班组箭头紧邻名称并从向右旋转为向下；展开分支使用浅灰绿底色，三级功能额外缩进，移除班组标题的绿色选中态，只保留当前功能高亮。复用 Element Plus 菜单和展开动画，过渡 180 ms；没有新增状态管理、网络请求、菜单内容或权限变更。

- 导航定向回归 **2 文件 / 18 项通过**，包括新增加的点击班组箭头不改变路由 / 选中功能断言。TypeScript、生产构建、ESLint、格式和差异检查通过；没有宣称重跑全量业务测试。
- 单一独立 Chrome 会话 `sidebar-fold-20260926` 验证 1440 × 900、1366 × 768、390 × 844。八班组保留、同一时间只展开一个分支、较矮侧栏内部滚动，无页面横向溢出；班组文字起点 74 px，三级功能起点 82 px。箭头距班组文字 7 px，减少动态效果下过渡降为 0.01 ms。
- 实际点击箭头关闭 / 整行展开库房，路由和选中功能不变；浮出菜单选择研磨出库记录正常；手机选择库存明细后导航关闭，焦点回到正文。未提交业务单据、未操作生产数据。
- 截图在 `output/playwright/sidebar-fold-*-20260926.png`。人工查看桌面、三级浮出菜单及手机展开截图。初次浮出菜单复查出现悬停定位超时，移出再悬停实际标题后通过；没有因此修改菜单行为。保留既有 TeamWorkspacePage 多根节点 Transition 开发警告，浏览器无 JS 错误。

本轮本地提交，未推送、未部署；验收后关闭独立浏览器，确认守护进程 49590 和 Chrome 49591 均退出，共享预览服务保留。

## 2026-09-26 材质归类面板高度

按用户确认，仅让“材质归类”的内容区和面板至少填满可用高度：空态在表体内居中，分页位于面板底部；有数据时保持自然行高，多行数据使用整页滚动。已有物料性质结存保留在分页上方。没有修改接口、库存字段、算法或生产数据，也没有给其他页面强加高度。

- 定向回归 **3 文件 / 39 项通过**，覆盖材质分页、空态切换、底部分页顺序和班组工作区行为。TypeScript、生产构建、ESLint、格式与差异检查通过。
- 单一独立 Chrome 检查 1440 × 900、1366 × 768、2560 × 1440、390 × 844 及收起侧栏。空面板底边与正文可用底边相差 0 px，分页与面板底边相差 1 px；空态居中，无页面横向溢出。手机首次截图包含侧栏过渡中间帧，等待其完全隐藏后重新截图验收。
- 隔离演示库的 6 种材质保留正常行高，分页在底部；浏览器只读响应替换为 25 种材质后，默认每页 10 行、下一页正常，切换 50 条/页回到第 1 页并展示全部 25 行。实测行高 69 px，整页可滚到分页，表格内部无纵向滚动。没有向数据库写入这些布局测试数据。
- 人工查看 `output/playwright/material-height-20260926/` 下的 `empty-1440.png`、`empty-2560.png`、`empty-mobile-stable.png`、`six-materials.png`、`many-bottom.png`。自动化中一次点击 Element Plus 隐藏输入层被文本层拦截，改点可见页数文本后通过；另一处断言漏去页码空格，修正测试脚本后通过，未因此修改应用。
- 浏览器无 JS 错误，保留既有 TeamWorkspacePage 多根节点 Transition 开发警告。使用 Playwright 技能及独立会话 `material-height-20260926`，未使用 Codex 内置浏览器；结束后关闭会话并核对守护进程 53063、Chrome 53064 均退出，共享预览服务保留。

本轮仅本地提交，未推送、未部署。
