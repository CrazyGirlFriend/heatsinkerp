# 已选方案落地与验收记录

## 2026-09-17：八班组待接收转料大屏

final result: passed

本节为最新验收。仅本地实现，尚未提交、推送或部署；下方保留历史记录。

### 追加：件数与重量同时展示

- 依用户要求，全厂在库、内部在途及每种具体材质均显示“件数 / 重量（kg）”，库存两项数值沿用 AnimatedMetric 和 SSE 更新。每条班组流转记录继续显示自身的件数与重量，没有改成整批总数或只显示重量。顶部每2秒切组、两侧连续滚动、机器人布局均未改变。
- 前端相关55项、TypeScript及生产构建通过；新增断言覆盖八个班组每条可见记录的件数/重量、零件数、三位小数以及数量重量同步推送。不涉及后端修改，未重复运行后端套件。
- 独立Chrome检查2560×1440，顶部数值均完整、流转数量单元格无截断、无文档溢出；390×844各材质组切换后指标区均236px高，数值无横向溢出。已查看 `output/playwright/robot-quantity-weight-2k.png` 和 `robot-quantity-weight-mobile.png`。沿用本地演示库，未写入业务数据、未部署。
- 检查会话 `heatsink-amounts-check` 使用独立Chrome并在结束后关闭，不使用内置浏览器。

### 追加：具体材质重量与两秒轮播

- 顶部总在库重量固定，右侧按完整材质名称/牌号显示真实在库重量，一组三种，每2秒向上切换一组。材质不足一组不自动空转，末组不重复填充；可手动翻页。铜钼、铜钼 CuMo70 等不同已录名称不擅自归并。数据由现有 SSE 实时推送，分页或暂停不暂停接收更新。
- 汇总复用物理库存口径：包含在库废料，排除内部在途、已确认对外出库、丢失及未纳入库存的历史记录；缺失材质归为“未填写材质”。后端覆盖超过八种材质、不同牌号、跨性质同材质合并、未知名称，以及入库/转出/部分接收/接收/丢失后的推送。
- 独立Chrome 2560×1440检查，稳定换页间隔实测2000/2017ms；6.5秒采样中左右转料位移持续变化，翻页按钮横向偏移0px。总在库区、机器人及左右八班组布局不变。最终实页截图 `output/playwright/robot-materials-2k.png`、`robot-materials-page-2k.png` 已打开查看；未测4K。
- 390×844检查发现末组数量少时内容高度变化，已固定轮播区域高度。三组切换后指标区均236px、下方位置均416px、文档宽390px。最终截图 `output/playwright/robot-materials-mobile.png` 已查看。
- 后端相关50项、前端相关54项通过，最终页面20项复跑通过；TypeScript和生产构建通过，既有图表/Three.js大包提示保留。未宣称重跑全量套件。最终大屏控制台0 errors、0 warnings；初次打开遇到本地后端重启间隙的连接失败，服务恢复后重新载入验证。
- 仅重启本任务本地后端加载新字段，演示数据未修改。独立Chrome会话 `heatsink-material-check` 检查后关闭，不使用内置浏览器，不触及用户或其他任务浏览器。

### 视觉基准与证据

- 确认稿：`docs/design/factory-live-pending-selected.png`（1672×941，用户确认含表头版本）。
- 本地入口：`http://127.0.0.1:15174/factory-live`；使用现有隔离 SQLite 演示库，未改写业务数据。
- 同尺寸实页：`output/playwright/robot-pending-final-reference-size.png`，CSS 1672×941、截图同尺寸、deviceScaleFactor 1。与确认稿在同一次图像输入中打开比较；不是依靠代码推断视觉。
- 2K 实页：`output/playwright/robot-pending-final-2k.png`，CSS/像素均为 2560×1440，deviceScaleFactor 1。设计坐标按 2560/1672 缩放；正文约24.5px、表头约23px，无文档溢出。未测试4K。
- 连续滚动追加证据：`output/playwright/robot-continuous-2k.png`，已打开查看。沿用上述2K布局，表头不动，三行高度窗口连续进出，无底部空档；截图在手动暂停时采集，滚动运动另由逐时采样验证。
- 局部：`output/playwright/robot-pending-team-detail.png`；与确认稿一起查看库房区表头、来源列、三行数据、设备和组名位置。原稿和实页的流水号/数量为不同演示数据，未将此计作视觉偏差。
- 窄屏：`output/playwright/robot-pending-mobile.png`，390×844；八班组保留，页面内部纵向滚动，文档宽度390。该模式为桌面大屏的可读回退，不要求保持左右两栏。

### 对照与修正历史

1. 初拍 `output/playwright/robot-pending-first-2k.png`：[P2] 中央机器人被放大，脚部与背景底座不匹配。修正机器人视窗至540×500设计像素及纵向位置，未替换现有三维模型、缩减动作或使用静态贴图。
2. 重拍同尺寸及2K证据，头部、托盘、脚部完整，整体高度接近确认稿；八组仍同屏、无底部信息区。当前无未解决P0/P1/P2。

### 五项视觉检查

- 字体：沿用苹方/微软雅黑中文字体；班组名28px、正文16px、表头15px设计字号。流水号正常阅读方向，过长内容省略且有完整标题/可点击详情，数字等宽。
- 间距：左右栏实测宽779.59px、高1201.15px（2K），对应分区高度均约299.52px；相对的表头及数据行基线一致。与生成图底部略压缩不同，代码明确四等分，符合用户对称要求。
- 颜色：保留海军蓝面板、青色来源列、浅青表头、白色数据；细分割线，不使用额外状态卡片或流转箭头。
- 图像：复用原工厂背景、八套设备PNG和真实WebGL机器人；设备身份不同。原资源与生成稿存在轻微造型/光照差异，这是保留用户现有机器人及背景的有意选择。未将效果图作为页面贴图。
- 内容：各组表头均为“流水号 / 来源班组 / 件数与重量”；实际来源是上序、分区是接收班组。仅待接收内部转料，不重复状态、材质和底部汇总。无数据时显示“暂无待接收转料”，未配置班组明确标注。

### 功能与验证

- 依用户最新要求，取消间歇换行，改为持续匀速上移：32设计像素/3秒，八组共用一个动画帧时钟，逐帧仅更新共享位移，跨过行高时再更新列表。独立Chrome 2K连续采样24个250ms区间，速度10.54–11.25设计像素/秒，两个换行衔接区间均移动约2.67像素，无跳回或停顿，表头位置不变。
- 手动暂停冻结在当前像素，续播不重置；悬停和点击班组仍继续移动，暂停列表时机器人仍运动。窗口只保留三行高度加一条进入缓冲；三条以内不移动、不重复填充。
- 首条转料与机器人activityKey关联；按班组名切换当前展示但不再停止轮播，点击流水号进入真实CK详情；全屏进入/退出成功。
- 2K八个三行高度窗口、24个列标题，无页面溢出；连续滚动使用每组4个DOM数据行形成衔接，不新增业务记录。悬停及焦点停留不触发全局暂停，页面隐藏/减少动态效果保护保留。
- SSE持续使用原认证通道；接收或作废后的新快照立即移除记录，暂停轮播不冻结业务数据。后端覆盖多流水号CK、同流水号合并、部分接收、作废、外部出库排除以及每组多于三条。
- 初次大屏验收：前端55文件431项全部通过；后端大屏、总览、实时推送专项46项通过。连续滚动追加验证：前端相关41项、TypeScript与生产构建通过，覆盖半行位移、跨行衔接、循环回首、原位暂停续播、推送不重置位移、短列表、卸载释放动画帧及班组点击不暂停；未重跑后端或全量套件。构建仍有项目既有大分包提示；初次测试中既有URL解码/模拟watch警告不涉及本页。
- 独立Chrome，控制台0 errors、0 warnings；未使用内置浏览器。自动化会话在验收后关闭，本地前后端保留供预览。

### 后续与范围

- P3：极窄屏的指标说明可能换行，不影响2K大屏；长流水号可通过点击详情查看。
- 需要发布时同时更新前后端，不需要数据库迁移。未进行线上写入或数据重置。

---

发布补记（2026-09-16）：下述库存自定义列及字段搜索已随 `20260916T040657Z` 配套上线，程序提交 `db89aef` 已推送。最终八班组 136 项线上 MySQL 查询、三路 SSE、资源哈希及数据不变检查通过；首次验收数量假设导致回退、修正后重发的完整记录见 `docs/server-deployment.md`。下述“本地，未部署”是开发验收当时状态。

## 库存明细按字段搜索与动态首列（2026-09-16，本地，未部署）

- 八个班组共用库存组件支持 17 个一级字段：文字包含匹配，汇总数量/重量按等于、不少于、不多于查询，加急状态选择，最近流转日期按工厂时区查询。综合文字扩展为六个资料字段；批次号、上下序仍只在详情，不进入字段列表。查询先在后端筛选流水号再分页，保留完整余额，不更改库存账务。
- 指定字段并提交后临时置于第一列，隐藏字段也会带出；流水号始终显示，操作保留最右侧。不修改账号/班组的浏览器列偏好，清空或关闭查询标签恢复原顺序；翻页、实时刷新及已有日期筛选保持有效。只更改搜索草稿时不提前换列。
- 后端全量 277 项通过；前端 55 文件、421 项通过。专项覆盖全部字段、混合资料值、完整余额、分页、班组隔离、日期边界、非法输入、隐藏列提升、不重复列、恢复排序、实时刷新及零值查询。动态列渲染曾产生 Element Plus 隐藏注册区中的空按钮，已改为直接条件插槽；对应十行/二十行断言通过。
- 独立 Chrome 实页：客户编号 TEST-C01 命中 8 个流水号，客户编号首列、流水号第二列；可用重量不少于 37 kg 命中 37.4/37.4/37.4/37.6 四条，重量首列；关闭标签恢复原六列。桌面 1920×1080、手机 390×844 文档宽度均等于视口，窄屏表格内部横向滚动，控制台 0 errors / 0 warnings。截图已打开检查，位于 `output/playwright/inventory-field-search-20260916/` 的 `customer-desktop.png`、`weight-desktop.png`、`weight-mobile.png`。
- 手机首拍截到了侧栏收起过渡，已稳定后覆盖重拍。自动化曾点到被可见文字覆盖的 Select 原生输入，以及读取手机隐藏侧栏超时；改为点击可见选项，不将采集超时视作产品缺陷。未使用 Codex 内置浏览器；仅操作隔离 SQLite 副本，没有修改线上数据。
- 自有会话 `heatsink_field_search_20260916` 已关闭，daemon 89500、Chrome 89503 及已记录子进程确认退出；临时前端 89471、后端 89444 已停止。补充测试的 props 类型错误及标签关闭事件缺少参数已修正；最终专项 15 项、TypeScript 与生产构建通过，既有大分包提示保留。未提交、推送或部署。

## 库存明细自定义列（2026-09-16，本地，未部署）

- 保留原六列默认展示，新增右上角“显示列”：勾选、上下排序、应用、取消和恢复默认。流水号与操作固定，候选列仅包含流水号资料和该班组汇总；不加入批次、上下序等二级记录，不修改后端查询、库存计算、详情或打印。
- 设置按账号 ID、班组 ID 分别保存在当前浏览器，清理网站存储或换电脑不自动同步。损坏/不可用存储回退默认，保存失败明确提示。显示列变化不重查列表、不清空筛选或跳页。
- 首轮前端全量 416 项通过；补充原位排序与取消后重开验证后，17 项专项通过。实页复验发现快速操作可能受弹层打开回调影响，改为打开状态同步初始化草稿，并增加回归测试；恢复默认后重新加载实页，确认为原六列。最终 TypeScript 和生产构建通过，既有大分包提示保留。
- 只用独立 Chrome 检查本地隔离 SQLite 数据副本，没有操作线上数据。1920×1080 实页完成列选择、排序、刷新持久化、切换班组隔离及恢复默认；390×844 弹层宽 320px、页面宽 390px，无文档横向溢出，列表可横向滚动。控制台 0 errors / 0 warnings。
- 实页截图在 `output/playwright/inventory-columns-20260916/`：`default-desktop.png`、`column-settings-desktop.png`、`selected-desktop.png`、`column-settings-mobile.png`。移动弹层首拍处于淡入过渡，已等待稳定并覆盖重拍，最终图片已查看。搜索范围经后端代码核对，已补入使用说明。
- 最后重复补拍桌面图时截图等待超时，未继续重开浏览器，保留此前成功的截图。自有会话 `heatsink_columns_20260916` 已关闭，daemon 86667、Chrome 86669 及其专用 profile 子进程确认退出；本次临时前后端服务也已停止，未关闭用户或其他任务的浏览器。
- 最终新增弹层时序回归后，全量第一次为 416 通过、1 项既有筛选测试超时（5 秒）；没有调大超时或跳过用例。关闭临时浏览器并降低至最多两个测试 worker 后重跑，55 个文件、417 项全部通过。

## 当前：全项目界面一致性审查（2026-09-14，本地）

final result: passed

发布补记：用户随后授权部署，已发布前端 `20260914T123620Z`。为刷新旧字体缓存，仅补充字体内容版本 URL 后再次构建；公网构建 JS/CSS、素材与字体哈希一致，健康检查及 SSE 通过，后端与数据库未重启。完整记录见 `docs/server-deployment.md`。

- 审查技能采用先截图、再定位、修改后同状态复拍的流程。完整结果和范围在 `docs/ui-consistency-review-20260914.md`，长期约定在 `docs/ui-style-guide.md`。
- 基准为用户已确认的浅白紫首页；本轮没有再调整首页波纹浓度、八设备图片、四列两排及玻璃卡片比例。普通业务页统一共用外壳、字体和颜色，打印和独立展示模式保留用途差异。
- 本轮证据目录 `output/playwright/project-consistency-20260914/`。`01`–`11` 是改前实页；`21`–`44` 是改后/复查实页。台账 `03-before-serials.png` 与 `21-after-serials.png` 均 1437×1095、DPR 1、同一账号和演示数据；`compare.png` 同输入全景与 `focus.png` 局部都已打开检查。`39-serial-detail.png` 是发现小字问题的中间态，以 `40-serial-detail-final.png` 为最终验收。
- 字体：中文从首页私有扩大为普通业务页共用 HeatSink Han，保留系统回退；首页 Inter 大数字不变。列表正文 16px、表头/控件 15px、辅助 14px；分析轴/图例 14px。流水号详情取消 small 元数据尺寸，最终计算字号 14px，明细 15px，默认分页 10 条。更新了字体子集与第三方说明。
- 布局/留白：所有普通路由侧栏统一 200px、收起 64px、顶栏 52px；二级图标和文字起点一致。保留列表 10/20/50/100、纵向阅读和窄屏表格内部横向滚动，未重新添加冗余摘要或图表。
- 配色：共用主色 #692ad6、文字 #211a32、辅助 #70647f、底色 #fcfbfe、表头 #eee9f4。清除了本轮发现的登录深蓝区、普通表格蓝灰表头和页签旧配色差异。成功/待处理/异常的绿橙红语义保留。
- 素材/内容：没有新增或重画图片；没有修改库存、确认规则、SSE、后端或业务记录。流水号和批次详情仍用表格；`36-print-style.png` 为真实打印 DOM 的 print 媒体检查，不是物理打印或复杂跨页认证。
- 已查看八个班组的台账、库房全部页签、转料/扫码/追踪、设置及新增账号弹窗、单据与流水号抽屉、普通分析。手机 390×844 和 2K 2560×1440 的首页/台账/转料/账号页文档宽度等于视口；独立机器人在 2K 正常，背景、模型及八班组保留，只有一个 canvas。分析大屏也已打开检查；未测 4K。
- 原始全量 52 文件/382 测试通过；随后补充流水号分页断言单测通过，最后取消 small 尺寸后 TypeScript/生产构建再次通过。既有测试桩警告及大分包提示保留。纯色文字对比已计算，未声称全 WCAG/屏幕阅读器认证，也未执行写入型业务流程。
- 本轮已检查范围无未解决 P0/P1/P2；P3 为紧凑弹层辅助文案与图表长流水号缩略。基于按钮显示文字编写的几次定位命中失败，改用实际 aria-label 后完成截图，未将超时当作产品故障。
- 全程独立 Chrome，未调用 Codex 内置浏览器。原会话进程自行退出后，先确认无残留且内存压力正常，才用同一会话名重开一个实例；没有同时堆积浏览器。最终控制台 Errors 0 / Warnings 0；`heatsink_consistency` 已关闭，58812/58814、61670/61673 及两个自有 profile 的进程均已退出。保留本地前后端服务，未关闭用户浏览器。仅本地修改，未部署。

## 当前：白紫背景再降三成（2026-09-14，本地）

final result: passed

- 用户先要求再淡一档，再明确要求在这一版上降低三成；基准为 `output/playwright/inventory-white-purple-20260914/06-lighter-desktop.png`，最终实页为同目录 `10-lighter-30-desktop.png`。两图均1437×1095、CSS视口1437×1095、DPR=1，同一管理员、真实演示数据及暂停状态，未更改库存或推送逻辑。
- 同输入全景 `11-lighter-30-comparison.png` 与库房2×局部 `12-lighter-30-detail.png` 均已实际打开检查。紫色波纹更淡，白色边缘、投影与文字仍清晰；浓淡差异为用户指定变化，不作为还原偏差。
- 字体：HeatSink Han / Inter、大小、字重、行距、数字和辅助文字颜色未改；没有新增换行或截断。布局：沿用四列两排、卡片大小、间距与内边距，没有压缩留白或移动内容。
- 配色：装饰背景不透明度0.78→0.546，侧栏白色覆盖22%→45.4%，均使原背景贡献再降低30%；汇总紫色透明度44%/32%→30.8%/22.4%，白色72%→80.4%，卡片紫色24%→16.8%。页面底色保持#fcfbfe。比例描述的是颜色图层强度，不是感知饱和度测量。
- 图像：全部设备与波纹继续使用原独立位图，仅调整表面透明度，没有覆盖或重绘素材。文案与内容：班组名、明细入口、待接收、真实库存与实时同步状态保持原样。
- 手机 `13-lighter-30-mobile.png`（390×844）已打开检查，文档宽390，无横向溢出；桌面宽1437、八卡齐全。沿用前轮2K布局，本次仅复查桌面和手机，不宣称重新测试全部业务与4K。
- 验收脚本首张桌面截图捕获到侧栏过渡，已等待侧栏完全展开后覆盖重拍；最初比较页通过Vite的/@fs访问未显示比较元素，改为本地file页面后复拍。两者属于采集问题，最终比较不含这些无效画面。
- 17项首页/实时推送相关测试、TypeScript与生产构建通过；控制台0错误/0警告。既有分包体积提示未改变。本轮无未解决P0/P1/P2，背景浓淡偏好仍可继续微调。
- 仅本地修改，未部署、未写业务数据库。复用唯一独立Chrome会话 `heatsink_purple_light`，未使用Codex内置浏览器；关闭并核对自有进程退出后保留本地前后端预览服务。

## 当前：白紫首页试样（2026-09-14，本地）

final result: passed

- 基准为已验收实页 `output/playwright/inventory-selected-20260914/15-final-desktop.png`，本轮按用户明确要求仅从蓝紫调成白紫，保留布局与功能，并非重新设计。颜色差异是指定变化，不作为还原偏差。
- 实页 `output/playwright/inventory-white-purple-20260914/01-desktop.png`，与原版均 1437×1095、CSS 视口 1437×1095、DPR=1、管理员相同业务数据、动画暂停，仅更新时间不同。同目录 `02-comparison.png` 为同输入并排，`03-detail.png` 为库房卡片 2× 比较，全部已实际查看。
- 字体：沿用本地 HeatSink Han / Inter，标题36px、班组22px、库存数字44px和所有字重、行距未改，没有新增换行或截断。
- 留白/布局：八卡仍为四列两排，首排 y=336、第二排 y=684、单卡高328px、宽282.75px，与原实页一致；未新增卡片、说明或控件。
- 配色：大面积底色从 #f0f6ff 改 #faf9fd，玻璃白色比例增加、反光与投影转淡紫；正文 #211a32、辅助 #70647f。内部在途数字改为紫色；保留待接收/加急琥珀色语义及少量设备蓝青色，不把整个界面染紫。
- 素材：内置 Image Gen 编辑原背景并保存 `frontend/public/assets/factory-inventory/ambient-white-purple.png`，1536×1024，原图构图保持、底色白且顶部紫色可见，未覆盖旧图；没有用 CSS 画波纹或替换设备。完整 prompt 与文件记录见 `docs/design-references/factory-inventory-white-purple.md`。
- 文案/数据：全部沿用既有正式名称、库存、待接收和实时连接状态，不增加“风格试样”之类页面文案，不写死图片示意数据。SSE/后端/数据库没有改动。
- 2K `04-2k.png`（2560×1440）及手机 `05-mobile.png`（390×844）已查看，文档宽度分别为2560、390，无横向溢出；八张设备图片无破图。未测4K。本次不重新认证全部业务与屏幕阅读器。
- 待接收可进入 `/team-workspaces/1?tab=pending` 并返回；动画播放时仅一个 canvas，暂停后仍“实时同步”。控制台0错误/0警告。22项相关前端测试、TypeScript和生产构建通过；既有大分包提示保留。首个自动化登录选择器漏了“登录”前缀导致一次等待超时，改为实际页面可访问名称后通过，不是登录功能故障。
- 本轮无P0/P1/P2；背景浓淡与设备是否统一紫色属于用户偏好，留作后续选择。当前仅本地试样，线上仍为上一轮 `20260914T102337Z`。只使用一个独立 Chrome 会话 `heatsink_white_purple`，已关闭；daemon54220、Chrome54221及其profile子进程确认退出，未使用Codex内置浏览器，本地预览服务保留。

## 当前：确认效果图还原（2026-09-14）

final result: passed

- 唯一视觉基准：`docs/design-references/factory-inventory-v2-selected.png`（用户重新附上的 exec-3935fd40 图片），不是先前较素的版本。
- 初次实页：`output/playwright/inventory-selected-20260914/01-desktop.png`。原图及实页均 1437×1095，CSS 视口 1437×1095，deviceScaleFactor=1；真实管理员、八班组数据、动效暂停，更新时间及“实时同步”为有意保留的动态差别。
- 同输入比较：`02-comparison.png` 为两图并排；`03-detail.png` 为库房卡片 2× 聚焦比较，均位于同一目录。已实际打开检查。
- [P2] 第一轮节奏偏下：首排卡片 y=340（目标333），第二排 y=692（目标678），数字/待接收随之偏下。调整标题行高、汇总内边距及卡片图标行高度。
- [P2] 汇总分割线偏右约36px；调整双栏比例及第二栏内边距。
- [P2] 顶部波纹下沿进入班组标题区，目标主要集中于汇总条；收紧背景装饰高度并保持图片/WebGL取景一致。
- [P2] 待接收标签误用深色正文；改回参考蓝灰层级。雕刻/退火主体需略放大，避免留白轮廓使其明显小于同组设备。
- 上述 P2 均已修复并复拍：`04-desktop.png`，与原图同输入并排的 `05-comparison.png`、2× 聚焦的 `06-detail.png` 已实际打开检查。顶部双汇总、蓝紫波纹、八个独立设备图标、四列两排卡片、44px 库存数字及奶油色待接收区均落实为真实组件，不是贴整张图。当前首排约 y=336、第二排 y=684，与参考的数像素差异列为 P3，不继续拉长本轮。
- 五处主要检查面：顶部标题/操作、双汇总、卡片图标与名称、库存/待接收与底部入口、展开/收起导航；文字尺寸、蓝灰辅助信息及透明表面层级均按确认图处理。继续使用本地思源黑体子集和 Inter；库房等设备为重新生成的独立素材，具体细节不宣称逐像素复刻，见 `docs/design-references/factory-inventory-v2-assets.md`。
- 响应式：`07-laptop.png`（1366×900）、`08-2k.png`（2560×1440）、`10-mobile-settled.png` 和 `13-mobile-bottom-settled.png`（390×844）已打开检查，文档宽度等于视口、八张图加载成功，手机单列且可滚动到底部。未测 4K。`09` 捕获到侧栏过渡、`11` 捕获到滚动渲染前空白，不作为通过证据；待侧栏停止、双动画帧完成后复拍，最后两班与底部可见。
- 真实交互：库房明细进入 `/team-workspaces/1?tab=serials`，待接收进入 `?tab=pending`，导航可返回总览；暂停只停动效，仍显示“实时同步”，播放后仅一个 WebGL canvas；系统减少动画时按钮禁用。手机导航可 Escape 关闭，焦点返回入口，桌面侧栏可收起/展开。第一次脚本未等待路由变化便回退，误退到 about:blank；修正验收脚本等待 URL 后全部通过，未改业务路由。
- 控制台 0 errors / 0 warnings，交互检查 pageerror 为空。全部前端测试和 TypeScript/生产构建通过，新增八种设备素材路径回归；既有测试环境 Vue 警告及 ECharts/Three 大分包提示保留。SSE 的数据接受、数字变化动画、暂停不断流、断线恢复仍由既有单测覆盖，本次未写业务数据制造演示变更。
- 本轮范围无未解决 P0/P1/P2；P3 为独立生成素材与参考原画细节差异、数像素节奏差异及大屏宽度下留白。不代表全部业务/屏幕阅读器专项认证。使用且只使用一个自有独立 Chrome 会话，不使用 Codex 内置浏览器。发布和最终资源清理另在本节末补记。
- 最终桌面实页 `15-final-desktop.png` 已查看，字体加载成功；`14-compact-settled.png` 为减少动画后的完整收起导航检查。自有 `heatsink_inventory_v2` 已关闭，daemon 46538、Chrome 46540 及该 profile 的子进程均确认退出；未关闭其他任务或用户浏览器，本地服务保留。
- 已发布前端 `20260914T102337Z`，后端保留 `20260914T092908Z`。公网 HTML、入口及首页代码、十张新图、原卡车、字体、机器人大屏模型和背景与已验收构建逐项哈希一致。后台及数据库容器/启动时间未变，所有容器 healthy。Nginx 实际 SSE 鉴权、八班组首帧及心跳复验通过；无业务写入，仅创建后注销验收登录会话。详情见 `docs/server-deployment.md`。

## 当前补充：首页库存实时推送（2026-09-14，已发布）

- 首页 `/` 已移除每 10 秒轮询，改为事务提交后通过 SSE 推送完整汇总。无需点击刷新，暂停仅停止动画；下方原“10 秒自动更新”改为实时连接状态。
- 后端通过 ORM 提交事件覆盖入库、转出、接收、丢失、加急、作废及相关配置。未提交、事务回滚和幂等重放不会发布业务变化；断线重连发送最新快照，不补造事件历史。
- 访问锁 Cookie 与账号令牌均沿用既有校验。已验证未登录拒绝、已打开连接的注销/访问锁失效处理、心跳不查询库存、订阅释放，以及保存点提交/回滚。通知通道仅支持当前单后端实例、单 worker，两个 Dockerfile 已明确固定；外部 SQL 或独立导数进程不在主动通知范围。
- 前端最终 52 个文件、380 项测试通过；TypeScript 与生产构建通过。后端原全量加首版推送测试共 180 项通过；补充确认/作废/注销和访问锁测试后，以 requirements-dev.txt 锁定的 FastAPI 0.116.1、SQLAlchemy 2.0.43 隔离环境复测推送、总览和访问保护，51 项通过。已有 Vue 测试警告、AnyIO 弃用提示及大分包提示仍存在，未作为本次相邻清理。
- 真实 HTTP 通过本地 Vite 入口 `127.0.0.1:5186` 接收 `text/event-stream` 首个库存快照，约 370ms，八班组、在库 17,334、在途 1,648。此数值是首帧读取耗时，不是生产变更推送 SLA。提交后的业务联动另由隔离事务测试验证，没有为验收修改本地演示库存。
- 本地业务行数仍为 433 条物料、376 张出库单、192 条丢失记录；仅建立并注销一个 HTTP 检查账号会话。本地后端已重启加载新代码，前端保持运行。
- 后续发布发现 MySQL 整数 SUM 返回 Decimal，修复 SSE JSON 编码并添加回归，最终相关测试 52 项通过。首次候选检查失败已自动回退，最终 `20260914T092908Z` 经生产 MySQL 和 Nginx 验证首帧、心跳、访问锁与账号鉴权通过，前后端均 healthy；24 张数据表切换前后内容一致，原业务数据保留。
- 本次没有使用任何浏览器。此前生成并确认的新版效果图尚待单独落地，不能把本次推送发布宣称为新视觉已完成。公网构建资源、旧大屏及机器人背景核对通过；运行边界见 `docs/inventory-realtime.md`。

---

## 当前补充：玻璃可见度与中间档背景（2026-09-14，本地）

- 用户指出上版看不出玻璃质感；本次重新读取实页并截图审查，仅针对首页表面效果，未重做布局、字体大小或业务逻辑。
- 步骤 1，原版检查：`output/playwright/glass-refinement-20260914/01-before.png`。玻璃填充不透明度 64–88%、底色 #f8fafd 接近白色，确实难以区分透底层；信息层级和真实数据展示正常。
- 步骤 2，增强玻璃：降低填充至 12–48%，22px 磨砂模糊，增加白色双层边缘反光与轻投影；悬停/更新高亮保留玻璃边缘。`02-after.png` 为初步较浓的蓝灰背景，用户明确要求不要从过淡跳到过浓，因此不作为最终背景。角落原背景图降至 20% 并淡出，未增加花纹或图案。
- 步骤 3，中间背景对照：`05-bg-light.png`、`06-bg-middle.png`、`07-bg-strong.png` 使用相同卡片、字号、真实数据和暂停状态，只替换背景底色；同尺寸 1680×1280 截取。`08-background-comparison.png` 将三版全景和等比例卡片局部放在同一画面，已查看。当前源码使用中间档 #eff2f9 → #ecf3f9 → #f0f4f9；辅助文字略加深至 #5c687e，保持透底后的可读性。审查仅确认可见层次改善，最终浓淡偏好由用户查看对照决定。
- 最终中间档：`09-middle-mobile.png`（390×844，单列）和 `10-middle-2k.png`（2560×1440，四列）；文档宽度分别为 390、2560，无横向溢出，pageerror 为空，未测 4K。无 backdrop-filter 时保留实色回退，不从截图宣称完整无障碍认证。
- 验证：11 项首页/数字动画相关测试通过，最终 TypeScript 与生产构建通过；无数据库、接口或数字更新逻辑改动，仍每 10 秒读取。未部署线上。原构建分包体积警告不变。
- 资源与证据：有窗口 Chrome 在视口变更后数次返回旧尺寸画面，失败/空白截图已拒绝；先关闭该会话及 PID 30208/30210，再以一个独立无界面 Chrome 重拍最终对照，未同时累积实例，也未使用 Codex 内置浏览器。最终 PNG 覆盖同名无效试拍，以当前已查看的文件为准。

---

## 当前补充：全厂库存首页、浅色玻璃与数据更新（2026-09-14，本地）

final result: passed

- 依据：`docs/design-references/factory-inventory-selected.png`，原图 1437 × 1095。遵循用户后续的“背景淡一点、字体和留白调整、卡片玻璃质感、数字随数据变化动画”要求，不把原稿的浓紫光带、粗黑字和示意数字当作必须照搬的内容。
- 布局：保留顶部在库/在途双汇总、八班组卡片、待接收和明细入口；宽桌面四列、中等宽度两列、手机单列，允许页面自然滚动。卡片内边距 28px、网格间距 24/26px、内容最大宽度 1760px；既有侧栏、旧数据分析页和独立机器人大屏保留。
- 字体：中文本地思源黑体子集，内部字体名 HeatSink Han；数字本地 Inter，使用等宽数字。主标题 28px/600、班组名 20px/500、库存数 36px/550，正文 16px、辅助文字 14px。与概念稿相比减少粗黑字、压低标题权重，保留可读层级。浏览器确认两个字体均 loaded；许可证、来源及子集脚本在 `frontend/THIRD_PARTY_NOTICES.md`。新增文案后已重新生成子集，源文件与构建输出均约 80KB。
- 背景与表面：背景插图整体透明度 45%，汇总条叠加 55% 白色遮层；不做深色皮肤。卡片为白色/浅蓝半透明填充、16px 磨砂模糊、白色内高光及低强度投影，正文不透明。无 backdrop-filter 的浏览器使用实色回退。库房、运输使用两张独立透明位图，班组使用 Element Plus 图标；普通 UI 图标与原稿 3D 示意图不同，统一于系统图标库。
- 数据口径：复用现有真实库存接口；在库不含在途，待接收按整批统计，在库流水号去重。每 10 秒轮询，不宣称毫秒级推送；隐藏页面暂停请求，重新可见立即读取，慢请求不重叠。失败保留上次成功数据并明确提示，不将错误当成零库存。更新时间显示到秒。
- 动态：库存件数、重量、待接收件重/批数、在库流水号及加急数均使用同一数值过渡组件，700ms 到达实际目标；首次加载直接显示实际数值，数据不变不重复播放。变化的班组边缘高亮 1400ms；暂停动效不暂停数据更新，减少动态效果时立即显示最新值。背景 WebGL 只用一个低功耗画布、至多 24 次渲染/秒；导航离开会移除画布、释放纹理和渲染器。

### 实页对比与交互证据

- 对比文件：`output/playwright/inventory-home-20260914/10-comparison.png`，同一图像中包含已选稿和真实实页的全景、库房卡片局部。全景统一显示宽 840px；卡片局部按相同整体缩放基准对齐比较。原稿为图片草案，字体与侧栏尺寸不做像素级等同承诺；字号、留白、背景、玻璃表面按后续要求调整。
- 最终真实数据截图：`02-final-desktop.png`（1680 × 1280）；`03-2k.png`（2560 × 1440）；`04-laptop.png`（1366 × 900）；`09-glass-mobile.png`（390 × 844）。全部八班组可访问，文档宽度均等于视口宽度，无横向溢出；桌面为四列，笔记本两列，手机单列。未测 4K。
- 修正：窄屏重量和 kg 曾分行，现按同一不可拆分数值组布局；汇总背景窄屏放大后偏花，增加白色遮层；早期手机截图 `05-mobile.png` 处于侧栏收起过渡，不作最终证据。后续等待过渡结束后，侧栏不遮挡页面，单位、按钮及主文字完整可见。
- 真实链接验证：库房待接收进入 `/team-workspaces/1?tab=pending` 且该页签 selected=true；明细进入 `?tab=serials` 且流水号台账 selected=true；旧分析页面 `/factory-analysis` 正常。离开首页后背景画布数量为 0。最初自动化连续后退发生导航时序问题，改为等待准确目标 URL 后重验，无应用代码绕过。
- 动态验证：`check-live-update.js` 仅拦截一次浏览器读响应，模拟库存 8882 → 8902，实际观察中间值 8884，最终值 8902，只有一张卡片高亮；`08-animation-qa-only.png` 是该受控动画测试截图，**不是实际业务库存证据**。检查后解除拦截并重载真实数据，未写入数据库。小数精度及下降方向由单元测试补充覆盖。
- 可访问性：键盘 Tab 可到达待接收链接并显示实线焦点环；链接含班组名，数字屏幕阅读标签直接提供真实目标值。系统减少动态效果时画布数量为 0、动效按钮 disabled=true；正常模式单画布、手动暂停可用。未进行完整屏幕阅读器或全部缩放倍率认证。

### 验证与交付边界

- 全量前端 51 个文件、369 项测试通过；增加 10 秒刷新/慢请求/增减动画覆盖后，本次相关 11 项再次通过。首轮新增测试没有正确驱动 jsdom 动画帧，改为受控动画时钟后通过，生产动画已另由真实 Chrome 验证。TypeScript 与最终生产构建通过。相关后端 24 项通过。
- 最后一次真实页面检查无 console error、warning 或 pageerror，中文及数字字体均成功加载。既有 ECharts/Three.js 分包体积警告保留；位图首载体积及低端设备帧率未做专项性能认证。
- 无未解决的本次范围 P0/P1/P2。使用一个自有独立 Chrome 会话 `heatsink_inventory_20260914`，未使用 Codex 内置浏览器；关闭后检查 daemon 26189、Chrome 26198 及自有 profile 进程已退出。保留本地预览服务供用户查看，未部署线上、未改生产数据。
- 本地首页：`http://127.0.0.1:5186/`；后端仅监听 `127.0.0.1:50570`，沿用本地演示库。

---

final result: passed

本次所选样式落地范围内，无未解决的 P0 / P1 / P2 发现。此结论不代表已重新验收全部生产业务或完成线上发布。

## 当前补充：转料记录分列、内容居中（2026-09-14，本地）

- 选定参考：`docs/design-references/transfer-register-option-1.png`（1681 × 937 px），即本轮第一张图。用户随后明确要求表格内容居中，因此七列表头、正文、流水号、数量及重量均居中，不照抄原图左对齐和数字右对齐。
- 修改范围：只调整转料列表及其移动视图；“班组流转”拆为“来源 / 去向”，移除列表圆点和箭头，沿用真实手工入库与外部去向的显示规则。数量/重量、日期/时间分行。扫码、状态/日期/班组筛选、加急标记、详情和分页保持原业务逻辑；原机器人、大屏、其他页面未修改。
- 字体：正文及状态 16px、辅助信息 14px、24px 行高；沿用系统中文无衬线字体及常规字重，普通记录行实测高 88px。七列 `text-align` 均实测为 `center`，流水号行和数量块也分别验证 `justify-content` / `align-items: center`。
- 布局与配色：表头灰蓝色 #e9edf4、细网格边线、白色表体；保留紫色交互、绿色完成状态及深琥珀色待确认状态，不依赖颜色单独传达状态。来源和去向列统一字号和独立边界，长文本保留悬停提示。无新增位图、图标、装饰卡片或 CSS 图案。
- 全景对比：源图与 `output/playwright/transfer-register-20260914/09-final-desktop.png`（1681 × 1000，1x）及 `10-table-detail.png`（1423 × 584，直接截取表头和六行）在同一图像输入中查看。源图是局部概念稿，不含实际系统侧栏和完整筛选区；实页保留既有外框，表格可用宽度为 1423px，不宣称整页像素级复刻。实际单据来自本地演示库，未用图片虚构内容覆盖数据库。
- 响应式：`04-laptop.png`（1366 × 900）、`05-2k.png`（2560 × 1440）、`07-mobile-settled.png`（390 × 844）均已查看，无文档级横向溢出。手机使用已有逐条列表形式，来源/去向带独立标签。`06-mobile.png` 捕获在侧栏关闭过渡中，已拒绝作为验收证据；以过渡结束后的 `07` 为准，侧栏 right=0，无遮挡。未测 4K。
- 交互：默认请求并显示 10 条；“下一页”切到 page=2，首条从 00433 变为 00423；页容量选择 20 条后回到第一页并实际加载 20 行，页面可向下滚动。详情点击和无结果搜索在中断前已经验证（`02-detail.png`、`03-empty.png`）。切换页容量的最初自动化点击命中了 Element Plus 的只读输入和重复文字，改用有语义的键盘展开后完成验证，未绕过或修改页面交互。
- 测试：相关 3 个文件共 29 项测试通过，包含新增来源/去向四种业务类型、全部列居中、单位/时区和详情入口回归；TypeScript 与生产构建通过。现有大型图表分包警告保留，不在本次列表调整中改构建策略。独立 Chrome 控制台为 0 errors / 0 warnings。
- 本地环境：恢复工作时原 `/tmp/heatsink-ui-refresh.6wmmJ4` 目录已不存在；未连接服务器或尝试清理线上数据。为验收创建新的隔离 SQLite：`output/playwright/transfer-register-20260914/preview.sqlite3`，通过现有种子流程生成 433 条转料、376 条出库、192 条丢失记录。这是新本地演示数据，不是旧临时库恢复。预览使用已有 Anaconda Python 环境，非生产锁定依赖环境。
- 本地入口 `http://127.0.0.1:5186/transfer-batches`，后端只监听 `127.0.0.1:50570`；代码和数据均未发布线上。
- 检查仅用独立 Chrome。会话 `heatsink_transfer_register_20260914` 已关闭；恢复后自有 daemon 7020 / Chrome 7021 及 profile `playwright_chromiumdev_profile-K7rzD7` 已检查退出，未关闭用户浏览器。本地预览服务保留。

本轮无未解决的 P0 / P1 / P2 发现；未进行整套业务或全部辅助技术认证。

final result: passed

## 当前补充：业务对比度（2026-09-13，本地）

- 根据用户页签截图及当前实页审查，修正两处层级：业务表头与正文过于接近；当前/悬停页签均为紫色透明底。完整审查、前后截图与可访问性边界见 `docs/contrast-review-20260913.md`。
- 业务表格共用灰蓝表头 #dfe5ef、深色字 #253248、650 字重及下分割线；清理班组、转料列表局部的 500 字重覆盖，正文和斑马纹相应加深。规则明确排除机器人大屏 .live-table，不改其他组件主题。
- 班组当前页签紫底白字，悬停为灰蓝底；保留数量徽标、aria-selected、面板关联、方向键与减少动态效果支持。未新增标题或卡片，也未修改业务逻辑。
- 当前颜色实测：表头文字/背景由 5.78:1 提升到 10.19:1，选中页签为 5.35:1。此处不是完整 WCAG 认证，原问题主要是层级和状态区分，不将原配色笼统判定为不可访问。
- 52 项相关前端测试、TypeScript 和生产构建通过。八个页签仍可见；实际按 ArrowRight 后选中与焦点都进入待接收。表格默认 20 条、分页与按钮保留。无后端改动，本轮不重跑后端或操作数据库。
- 1487×1058 前后同尺寸同悬停状态截图一起比较；1366×768 库存页及 2560×1440 台账无文档级横向溢出。证据为 `output/playwright/contrast-20260913/01-before-ledger-hover.png`、`02-after-ledger-hover.png`、`04-stock-1366.png` 和 `05-ledger-2k.png`，均已查看；`03-detail-contrast.png` 为直接浏览器截取页签与表头区域，非生成图。未测试 4K。
- 独立 Chrome 控制台 0 错误、0 警告；没有使用 Codex 内置浏览器。保持原本大屏及各项数据规则，本轮未发布服务器。
- 已退出登录并关闭会话 heatsink_contrast_20260913；PID 78670、78674 及 profile playwright_chromiumdev_profile-Du1Qy7 对应进程均确认退出。只清理本任务自有浏览器，本地服务保留。

## 当前补充：材质归类精简（2026-09-13，本地）

- 按用户绿色/红色标框精简所有班组共用的材质归类页面：移除材质库存分布图、单位切换及三张重复汇总卡；班组页签和下方材质结存表保留。大标题已在此前本地版隐藏，本轮未重新添加。
- 红框操作并入材质表右上角，保留手工入库、新建出库、扫码查询和刷新；按钮仍按原账号权限展示。表格七列原始数据、20/50/100 分页和点击材质跳到流水号筛选不变。数据分析和机器人页面不变。
- 删除本组件不再使用的图表导入、排序/单位状态、汇总卡样式及 navigate 事件，不修改共用图表组件或库存计算。
- 新增 3 项组件测试，连同工作台及数据分析共 32 项相关测试通过；TypeScript 和生产构建通过。构建仍有既有图表大包警告。本轮未重跑后端，原因是没有后端或数据库改动。
- 独立 Chrome 检查 1366×768 和 2560×1440：图表和汇总卡节点均为 0；本地六种材质完整显示，七列与分页保留，无文档级横向溢出；点击无氧铜 TU1 后地址带有正确材质筛选。截图位于 `output/playwright/material-table-20260913/01-warehouse-1366.png` 和 `02-warehouse-2k.png`，均已查看。未使用内置浏览器，未测 4K，未执行任何收发写操作。
- 控制台 0 错误、0 警告。页面初次自动化等待使用了缺少空格的标题名称，修正为快照提供的可访问名称后正常完成，不是页面加载错误。
- 已退出登录并关闭独立 Chrome 会话 `heatsink_materials_20260913`，PID 78396、78399 和 profile `playwright_chromiumdev_profile-7OcYqA` 均无残留进程；用户浏览器未受影响，本地服务继续运行。
- 本轮只更新本地，未发布服务器；用户说明及页面职责文档同步更新。

## 当前迭代：日期筛选与流水号加急（2026-09-13，本地）

- 选定目标：`docs/design-references/date-urgency-option-2.png`（1487 × 1058）。
- 首轮实拍：`output/playwright/date-urgency-20260913/02-calendar-before.png`（1487 × 1058，1x）。
- [P2，已解决] 周起始日不一致：补充 Day.js 中文 locale，最终日历为周一至周日。
- [P2，已解决] Esc 不能收起日期弹窗：补充关闭、返回触发按钮焦点及卸载清理；独立 Chrome 再次按 Esc，弹窗隐藏且焦点回到“流转日期：2026-09-11”。
- 最终参考对比：源图与 `05-calendar-after.png` 均为 1487×1058，在同一次图像输入中逐项核对侧栏、主查询行、日历、加急旗标、行内操作与分页；日历、旗标区域以原始分辨率聚焦检查。保留实际 20 条页容量和已有库存列，表格密度沿用当前项目，不照抄源图中的虚构数据。
- 布局与间距：日期按钮与关键词同一行，日历浮层宽 316px，工具栏按现有紧凑间距排列；材质和库存条件收入“更多筛选”。未恢复大标题、数量摘要或 KPI 卡片。1366×768 操作区自然换行，表体独立滚动、分页始终可见，无文档级横向溢出。
- 字体与表面：沿用当前系统字体、表格字号与白灰紫色 token，日历与小标签为 12–14px 层级；紫色选中日期，红色旗标及描边小标签。来源图更大的行距没有覆盖当前紧凑工作台，避免只显示十几条的回退。
- 图标与文案：复用 Element Plus Calendar、Flag 和方向图标，没有自绘 SVG 或新位图。“某一天/日期范围”“仅看加急”“标记加急/取消加急”均有明确业务含义；保留必要日期口径说明，不增加装饰文案。
- 与参考有意差异：保留真实只读权限和全部库存列；增加日期范围切换；本地样例在 9 月 11 日，系统当前日期为 9 月 13 日，因此不能照抄源图 9 月 12 日。默认不按加急重新排序。
- 实际操作：管理员标记 → 日期与仅看加急组合 → 查看原因 → 取消后从 1 条变为 0 条 → 再标记；仓库班组账号同样看到旗标和筛选，但标记/取消按钮均为 0，新建出库入口仍为 1。没有发起库存写操作。
- 关联页面：待接收、库存、出库、入库、丢失组合日期及加急分别返回 1、2、10、1、1 条；出库按完整 CK 列出普通及加急物料。`tab-*.png` 部分快照仍处于标签进入动画，出库最终效果以 `10-outgoing-settled.png` 为准，显示状态和“含加急”完整。
- 大屏：`12-live-urgency-2k.png` 为 2560×1440，八班组、原背景及机器人保持；近期表格真实显示两个“含加急 1”，第一行仍是最近入库记录，未被加急排序替换。暂停/退出、返回系统正常，未测 4K。
- 证据：`07-urgent-reason.png`（原因）；`08-calendar-2k.png`（2K 日历）；`09-ledger-1366.png`（笔记本）；`10-outgoing-settled.png`（整批）；`11-team-permissions.png`（班组权限）。本节图片均位于 `output/playwright/date-urgency-20260913/`。早期未打开弹窗的尝试截图不作验收依据。
- 自动化：后端全量 168 项通过；前端最终 45 文件、341 项通过，TypeScript 与生产构建通过。日期组件、权限、乐观并发、跨日 UTC+8、完整库存余额、整批保留均有覆盖。首次重跑仅传 maxWorkers 导致 Vitest 工作线程配置冲突，补充 minWorkers=1 后完整通过；未修改测试断言掩盖问题。
- 既有边界：构建仍提示 ECharts / Three.js 大包；测试夹具有既有 Vue watch 和特殊查询字符警告。独立 Chrome 页面控制台 0 错误、0 警告。本轮不是全部浏览器/屏幕阅读器认证，也没有在生产 MySQL 上执行迁移或验收。
- 数据：仅新增加急及审计表。本地保留 `TEST-20260911-001` 一条带“演示”原因的加急记录及三条标记/取消审计，不修改既有收发数量或单据版本。备份、接口及迁移见 `docs/date-urgency-filters.md`。
- 浏览器资源：仅使用一台任务自有独立 Chrome，未使用 Codex 内置浏览器。退出登录后关闭会话 `heatsink_dates_20260913`；PID 76786、76787 及 profile `playwright_chromiumdev_profile-MqV63d` 的进程均确认退出。没有关闭用户或其他任务浏览器，保留本地前后端服务。
- 本轮未发布服务器；之前的已完成记录保留如下。

## 视觉依据与比对方法

- 选定源图：`docs/design/ui-audit-reference-1.png`，1487 × 1058 px。
- 最终实页：`output/playwright/reference1-20260912/29-final-ledger.png`，1487 × 1058 CSS px，独立 Chrome、deviceScaleFactor 1，无裁剪、无缩放拼图。
- 源图与实页在同一次图像输入中查看，逐项比较侧栏、顶栏、标题、汇总条、页签、筛选、九列表格、分页。初稿及修正稿也各做过同输入比对。
- 真实权限优先：源图给管理员展示手工入库操作；实页管理员仅查看，库房账号才有该按钮。流水号、件数、重量、更新时间、待接收数量均来自现有演示数据库，不照抄图片的虚构数据。

## 发现与修正历史

1. 初稿 `01-ledger-initial.png`：208px 侧栏、56px 顶栏、一行库存汇总及表格结构符合方向；截图视口 1488 × 1056，随后校正为源图尺寸。
2. [P2，已解决，间距] 初稿实际行高 32.58px，第二十行部分被截断。去掉单元格链接按钮的边框及基线增高，实际行高降至约 30.31px。`02-ledger-refined.png` 与最终截图均完整显示 20 行。
3. [P2，已解决，字体] 初稿台账和菜单 13px 偏小；主要表格、页签、菜单改为 14px，保留数字右对齐。最终截图长流水号、规格、时间均未挤压相邻列。
4. [P2，已解决，详情间距] 流水号详情抽屉标题下存在不必要空隙；合并标题间距，统一 20px 内边距。入库弹窗去掉重复说明及装饰标题，保留确认后锁定等必要提示。

## 逐项设计复核

- 布局与留白：标题不再占一整块大卡片；账面库存、可用库存、待确认出库合并为一行；图表、材质归类与流水号台账保持独立页签。页面边距 20 × 24px，圆角、分割线和工具栏间距使用公共样式。
- 字体：沿用现有系统中文无衬线字体栈，标题 24px、主表和导航 14px；层级由字号和字重区分，不再依赖多段说明。表格默认 20 行，较短视口在表体内滚动，不减少页容量。
- 配色与表面：白色侧栏、浅灰工作区、浅紫选中态、紫色主操作；统一细边框和小圆角。移除常规业务按钮的立体渐变和夸张阴影，保留明确 hover / focus / disabled 状态。
- 图标与资产：复用 Element Plus 同一家族图标，无新位图资产、无自绘 SVG 装饰。品牌使用既有 Box 图标，与参考图的立方体有轻微差异，但不影响识别。原有班组设备图、Three.js 机器人和深色大屏未重绘。
- 文案：统一“流水号台账、库存明细、材质归类、扫码查询、流水号追踪”等入口；缩减重复页面说明。待接收与待确认出库仍明确区分；件数、重量在明细及提示中保留，未因简化表格删除业务信息。
- 可访问性：保留跳到正文、菜单语义、表单标签、页签键盘切换及移动菜单焦点约束。实测筛选可键盘展开、选择；移动菜单可用 Escape 关闭。台账长内容通过滚动/提示查看；本轮未进行完整屏幕阅读器或全部浏览器缩放认证。

## 页面与交互证据

所有截图位于 `output/playwright/reference1-20260912/`。

- 八个班组逐个访问，默认流水号台账均加载 20 条；库房账号查看其他班组时没有手工入库操作。管理员库房页也无录入按钮。
- 库房全部页签走查：待接收、库存明细、出库记录、入库记录、丢失记录、材质归类、数据分析，见 `06` 至 `12`；业务列表按页加载 20 条，材质归类为真实的 6 种材质。
- 侧栏展开/收起、自动展开当前组、两级结构走查，见 `13`、`14`。流转查询下的三个入口、流水号查询结果、全厂总览、班组与账号设置走查，见 `15` 至 `21`。
- 流水号详情、库房入库表单打开并取消；不提交入库、转料、丢失或账户修改。
- 页码切换：现有 25 个流水号，第二页 5 条；返回第一页 20 条。选择 50 条/页实际加载 25 条，再切回 20 条/页正常。
- 搜索无匹配值显示空状态，重置恢复数据。组合“有可用库存 + 铜钼 CuMo70”后返回 5 个流水号，URL 同时携带两个过滤条件；重置清空过滤。
- 响应式：1366 × 768、2560 × 1440、390 × 844 均无文档级横向溢出，分页可达；移动表格允许表内横向滚动。见 `23-laptop.png`、`24-2k.png`、`25-mobile.png`、`26-mobile-menu.png`。
- 动态大屏 2560 × 1440 普通和全屏模式均复核，八班组、机器人、首条联动信息、动态表格正常布局，进入/退出全屏成功。见 `27-live-2k.png`、`28-live-fullscreen.png`。遵从要求未测 4K。
- 最终台账同尺寸截图为 `29-final-ledger.png`。当前浏览器控制台查询结果：0 errors、0 warnings。

## 自动化与数据安全

- 前端：42 个测试文件，311 项通过；使用最多 2 个 worker 控制资源。日志 `/tmp/heatsink-reference1-tests-final.log`。
- 后端：相关物料分析测试 7 项通过。新增只读库存过滤在流水号聚合后、总数及分页前执行；覆盖全占用、仅剩重量、仅待接收、组合搜索、分页和非法参数。
- 前端最终生产构建及 TypeScript 检查通过。日志 `/tmp/heatsink-reference1-build-final.log`。已有 ECharts / Three.js 大包警告仍存在；测试中的既有 Vue 警告未在本轮扩展处理。
- 未清空、重置或追加演示业务数据；走查后仍为 433 条转料、376 条出库、192 条丢失记录。未改库存确认规则、未部署服务器。
- 全程使用一台任务自有独立 Chrome，未使用 Codex 内置浏览器。会话 `heatsink_reference1_20260912` 已关闭；PID 43368、43369 及专用 profile 对应进程均确认退出，未关闭用户浏览器。本地前后端服务保留。

## 已知范围边界

- 页面样式和共享组件已统一；原有批次编号体系、固定班组管理约束等业务层问题不在本次视觉选型实施范围内，未借机重构。
- 无法从静态参考图恢复精确字体文件与全部交互状态；使用现有系统字体和组件库，保留真实数据、权限与操作语义。
- 前一版大屏/工作台设计记录保存在 `docs/design-qa-before-reference1.md`。

## 后续：独立机器人大屏与背景恢复（2026-09-12）

- 按用户最新要求恢复原工厂背景与中央平台，不新增位图、不改三维机器人或恢复流转连线。
- `/factory-live` 从后台业务布局独立出来，保留访问锁与登录守卫。点击顶栏“大屏展示”即请求原生全屏；直接 URL、刷新或全屏请求被拒绝时也不显示后台顶栏和侧栏。返回系统后恢复原布局并释放机器人画布。
- 实页截图：`output/playwright/live-standalone-20260912/01-standalone-2k.png`、`02-one-click-fullscreen.png`、`03-laptop.png`、`04-mobile.png`。复核 2560×1440、1366×768、390×844；八班组完整，手机仅纵向滚动、无页面横向溢出。未测 4K。
- 新增入口、独立布局、全屏拒绝降级及返回测试；全量 42 个文件 314 项测试、TypeScript 及生产构建通过。独立 Chrome 控制台 0 错误、0 警告。
- 会话 `heatsink_live_restore_20260912` 已关闭，PID 68787、68788 及专用 profile 进程均确认退出；未使用 Codex 内置浏览器。本地前后端服务保留。
- 该前端版本已发布为 `20260912T142714Z`，后端与 MySQL 不变。线上另按用户截图、经备份与事务校验清理五个旧 DEMO 班组及其明确关联样例；现用八班组的 433 条物料、376 张出库单、192 条丢失记录逐条保持原样，详见部署历史。

## 后续：内部转出即时扣库（2026-09-12）

- 用户修正库存口径，班组内部转料提交即从源班组扣除，未接收单列内部在途；接收仅入下序，改单/作废同步调整，原本班对外确认规则不变。
- 班组汇总明确“当前库存、可用库存、内部在途”；库存图表不再把内部待接收量叠加成源班组库存。全厂总览和机器人显示在库与内部在途，保留既有背景、动画、八班组及独立全屏入口。
- 独立 Chrome 截图为 `output/playwright/transit-stock-20260912/01-factory.png`、`02-robot-2k.png`、`03-warehouse-final.png`。2K 大屏八组完整，1366×768 台账仍加载 20 行、无文档级横向溢出；初拍 `03-warehouse.png` 是加载态，不作验收图。未测试 4K。
- 159 项后端、315 项前端及生产构建通过。新增回归覆盖即时扣库、下序待接收、库存图汇总、整批确认及幂等不重扣、修改/作废回退、提交/确认跨日统计，以及前端库存与在途同时刷新。
- 会话 `heatsink_transit_20260912` 已完整关闭，PID 71924、71925 和专用 profile 进程均确认退出，未使用内置浏览器。页面控制台 0 错误、0 警告，本地后端保留运行。

## 后续：精简班组头部与新建出库入口（2026-09-12，本地）

- 按用户红框确认范围移除班组页重复的大标题、顶部流水号数量与库存汇总栏。页签保留键盘操作，隐藏的一级标题保留辅助阅读语义；数量继续显示在表格分页区。三个库存汇总统一放到数据分析，业务数据和计算口径未改。
- 各页签复用同一操作区：新建出库、扫码查询、刷新；库房另有手工入库，管理员和其他班组账号仅查看。台账与各记录页操作区放入查询栏，窄视口自动换行；图表页沿用其控制区。
- 新建出库打开本班库存选择窗口，默认 20 条，可切换 50/100 条；搜索仅本班可用库存，支持跨页勾选，最多 100 个来源。下一步复用原单条/批量出库表单与 CK 整批详情，不另设提交接口。取消选料、换班组或权限变化关闭操作，不生成单据。
- 新增回归覆盖各页签入口、管理员/跨班组/停用账号权限、跨页选择、100 批上限、搜索、失败重试、取消后异步刷新、路由变更丢弃旧数据，以及从台账创建后打开整批详情。43 个测试文件共 333 项通过，TypeScript 与生产构建通过；既有图表大包警告保留。上一轮机器人测试的可空余额访问补为可选访问，断言数值及大屏实现未改变。
- 独立 Chrome 检查：1366×768 台账、选料、出库表单与管理员库存页，2560×1440 台账；台账/库存均按 20 条加载、无文档级溢出。仓库八个页签均有单一操作区，其他班组和管理员无创建入口。实际选料进入表单后取消，未提交入库、出库或丢失。控制台 0 错误、0 警告。
- 正式截图位于 `output/playwright/compact-workspace-20260912/01-ledger-1366.png` 至 `06-admin-stock.png`，另有 `tab-5.png`（材质归类）和 `tab-6.png`（数据分析）。快速切页采集的 `tab-0.png` 至 `tab-4.png` 可能含加载过渡态，不作验收证据。
- 会话 `heatsink_compact_20260912` 已关闭；PID 73679、73680 及专用 profile `playwright_chromiumdev_profile-ewxJpq` 进程确认退出。未使用 Codex 内置浏览器，未干预其他任务 Chrome，本地 5186/50570 服务保留。
- 本轮只完成本地实现与检查，未部署服务器，未删除或改写业务数据。用户说明同步更新了按钮位置与选料流程。

## 后续：待接收搜索与扫码同排（2026-09-13，本地）

- 将搜索、查询、扫码输入和“查看来料”合入现有工具栏，保留日期、加急及班组操作；本页隐藏重复的“扫码查询”按钮，其他页签不变。移除“扫码后核对整批明细”常驻提示，仅出错时显示扫描错误。
- 独立 Chrome 实测 1366×768：工具栏高 56px，各控件垂直中心一致，搜索与扫码确实同排。390×844 按容器宽度换行，两种尺寸均无文档级横向溢出。截图为 `output/playwright/pending-inline-20260913/01-desktop.png`、`02-toolbar.png`、`03-mobile.png`。
- 工作台与页签相关 29 项测试、TypeScript 和生产构建通过；原有图表大包警告仍存在。未改扫描接口、确认流程、库存或演示数据，未部署。
- 会话 `heatsink_scanrow_20260913` 已退出登录并关闭，PID 79183、79188 和专用 profile `playwright_chromiumdev_profile-PLF4cb` 进程均确认退出。未使用内置浏览器；本地前后端服务保留。

## 后续：侧栏展开与收起对齐（2026-09-13，本地）

- 修复普通一级菜单被 Element Plus 的 20px 默认缩进覆盖、分组标题却使用 12px 的差异。统一菜单间距变量、图标宽度和文字间距；一级行高 44px、行间隔 4px，二级仍保留 38px 行高和原缩进。
- 收起模式同时处理普通菜单的 tooltip 容器与分组标题，去除容器额外内边距，图标居中。1366×768 实測：展开时四个一级图标中心均为 x=34、文字起点均为 x=54；收起时四个图标中心均为 x=31.5，和侧栏中心一致。
- 独立 Chrome 走查收起后的悬停子菜单、扫码页跳转、再次展开和 390×844 移动抽屉；移动端同样对齐，无文档级横向溢出。正式截图为 `output/playwright/sidebar-align-20260913/03-after-collapsed.png`、`04-after-expanded.png`、`05-mobile.png`。`02-before-collapsed.png` 采集时与热更新重叠，不作修改前证据。
- 导航相关 28 项测试、TypeScript 和生产构建通过，既有图表大包警告保留。只改侧栏样式，权限、路由、业务数据未改，未部署服务器。
- 会话 `heatsink_nav_20260913` 已退出登录并关闭，PID 79676、79679 及专用 profile `playwright_chromiumdev_profile-Md3Sya` 对应进程均确认退出。未使用内置浏览器，本地前后端服务保留。

## 后续：班组工作台字体与间距样板（2026-09-13，本地）

- 按 Product Design 截图审查流程先采集台账、待接收基线，再调整。完整对照、分步检查和字体/间距规格见 [样板审查记录](docs/workspace-typography-review.md)。
- 台账工具栏 104px → 60px，单行数据 40px，控件 36px；记录编号、核心数值和辅助信息分别设置字重、字号、颜色及对齐。保留已有紫色主题、表头对比度、选中态和 20/50/100 分页，未恢复冗余标题或汇总卡。
- 规则限定在班组工作台。另修正库存勾选列、加急标记空间、日期完整提示及材质数量/重量排列；数据、业务接口、侧栏和机器人未改。没有部署。
- 45 项相关测试、TypeScript 和最终生产构建通过；1366×768、2560×1440、390×844 实页复核。截图目录 `output/playwright/workspace-type-20260913/`，最终主要证据为 `05`、`08`、`10`、`13`、`15`、`16`、`17`；窄屏保留表内横向滚动，未测 4K。
- 独立 Chrome 会话 `heatsink_type_20260913` 已退出登录并关闭；PID 80307、80312 及 profile `playwright_chromiumdev_profile-fK8j4P` 对应进程均确认退出。未使用内置浏览器，未关闭其他任务服务，本地前后端保留。

## 后续：参考实页字号、十行阅读版（2026-09-13，本地）

### 目标与最终规则

- 用户授权登录参考网站，只读取布局与样式，不修改参考站内容。按后续反馈将流水号台账从密集九列表改为六列阅读版，允许整个工作页面上下滚动。
- 默认 **10 条/页**，可选 **10 / 20 / 50 / 100**。增加页容量时表格自然增高；侧栏和系统顶栏保留原布局，不用固定高度表体截住记录。本规则先用于流水号台账，其他记录页分页规则未改。
- 六列为流水号、材质、规格、可用件数、可用重量、操作。待接收、转出待确认和最近更新移到流水号详情；保留真实件数及重量，未删数据库字段或改库存计算。

### 对照与迭代

- 参考地址：`https://shweb.gov.thucdg.tech/point-management/cognitive-impairment-support-centers`。在 1440×1000、100% 缩放下读取：正文和表头均为 14px，正文 400、表头 500 字重，数据行约 57px。控件声明 AlibabaSans 字体，但未确认每个中文字形实际回退字体，未下载或复制字体文件。
- 本地初版正文 14px、表头/操作 13px、行高 40px。试样正文改为 16px、表头/操作 15px、辅助信息 14px，沿用已有系统字体栈；减轻编号和数字字重，正文加深，选中与操作继续使用紫色。
- 初稿发现 Element Plus 小号表格覆盖正文，以及重量表头截字，已分别修复。用户随后明确表示仍拥挤，故再减少三列、取消表格固定高度，行高实测约 63px；最后按用户要求将默认容量从 20 改为 10。
- 对照截图与当前截图在同一轮视觉检查中检查。不是复制参考站业务页面：保留物料系统字段、日期筛选、加急、扫码和现有权限，未引入参考站的水印、联系人内容或新增图像资产。

### 设计与交互检查

- 字体/间距：正文 16px、普通字重、24px 行内行高，六列间距充足。次要信息不再占用三个常驻列。页签与业务操作并排，筛选单独一行；不恢复重复标题、统计卡片。
- 颜色/图标/内容：浅灰表头与白色正文区分，鼠标悬停浅紫，操作链接和加急状态保留。使用已有 Element Plus 图标，无自绘装饰、无新图片或字体依赖。文字保留完整业务含义。
- 滚动：1440×1000 选择 20 条时工作区可视高度 944px、内容高度 1528px；实测页滚动到 584px 可见末尾与分页。表体自身可视高度等于内容高度 1256px，不再形成独立纵向滚动窗口。切换回 10 条实际显示 10 行。
- 响应式：1440×1000、2560×1440、390×844 均无文档级横向溢出；手机表格保留表内横向查看，页面负责纵向滚动。2K 下选择 20 条仍可整页滚动，未测 4K。1024×768 在前一轮字号试样也检查过，本轮未宣称完成所有浏览器缩放或屏幕阅读器认证。
- 功能：搜索单一流水号、重置、日期弹层打开/Escape 关闭、流水号详情、出库库存选择后取消、分页往返、10/20 行切换均实测。移入详情的三项信息可见；选料加载 20 个来源后取消，未提交业务操作。未改管理员加急权限。
- 可访问性：保留隐藏一级标题、页签角色和键盘切换、现有 focus-visible、表单标签及缩减动画规则。窄屏支持内容滚动，不强行缩小文字。

### 证据与验证

截图目录：`output/playwright/reference-type-20260913/`。

- `04-reference-list-full.png`：登录后参考页面；`05-before.png`：本地基线。
- `06`、`07`：已替换的初稿，不作为最终验收图。`08`、`09`、`10` 为中途大字号九列试样，后被用户最新六列要求覆盖。
- `11-ledger-390.png` 含尺寸切换过渡态，不作为移动验收图；`17-reading-390.png` 为稳定移动阅读布局。
- `14-reading-top-1440.png`、`15-reading-bottom-1440.png`：六列页面上/下滚动；`16-reading-details.png`：三项次要字段保留在详情；`17-reading-2560.png`：2K 阅读布局。
- `18-final-ten-rows.png`：最终默认十行实页；`19-twenty-scroll-bottom.png`：切换二十行后滚到页尾。
- 最终 6 个测试文件、48 项通过；TypeScript 与生产构建通过。新增覆盖六列、无固定表高、默认十行及二十行切换、次要字段在详情可达。既有图表/机器人构建大包警告未扩展处理。
- 本轮仅本地试样，未部署，未清理、重置、追加或修改业务数据。独立 Chrome 检查，未使用 Codex 内置浏览器。
- 最终控制台 0 errors、0 warnings。会话 `heatsink_reference_type_20260913` 已退出本地登录并关闭，最后一组 PID 86523、86526 及 profile `playwright_chromiumdev_profile-fhcond` 对应进程均已确认退出；前一组 PID 83771、83774 在继续检查前也已确认退出。同一时刻仅使用本任务一个浏览器，未关闭其他任务或用户普通浏览器，本地 5186/50570 服务保留。

final result: passed
